---
type: question
version: 19
pinned_commit: 135b867a530cac2e3796d87c852b53bef40f0077
verified: false
verified_by_agent: not yet
---

# How the REPACK Command Works in PostgreSQL 19, and Its 58 Feature-Scope Commits (unverified)

## Contents

- [Question](#question)
- [Short Answer](#short-answer)
- [Syntax and Modes](#syntax-and-modes)
- [Source Layout](#source-layout)
- [Blocking REPACK (the simple rewrite)](#blocking-repack-the-simple-rewrite)
  - [Multi-table discovery and recheck](#multi-table-discovery-and-recheck)
- [REPACK CONCURRENTLY (online rewrite via logical decoding)](#repack-concurrently-online-rewrite-via-logical-decoding)
  - [Step by step](#step-by-step)
  - [How concurrent changes are captured and replayed](#how-concurrent-changes-are-captured-and-replayed)
  - [Error handling and worker teardown](#error-handling-and-worker-teardown)
  - [WAL retention and `effective_wal_level`](#wal-retention-and-effective_wal_level)
- [Restrictions on CONCURRENTLY](#restrictions-on-concurrently)
- [GUC: `max_repack_replication_slots`](#guc-max_repack_replication_slots)
- [I/O Impact and Throttling](#io-impact-and-throttling)
  - [Where the writes and reads come from](#where-the-writes-and-reads-come-from)
  - [Settings that change the load](#settings-that-change-the-load)
  - [Recipes for a gentler REPACK](#recipes-for-a-gentler-repack)
  - [How to watch it](#how-to-watch-it)
- [Progress reporting](#progress-reporting)
- [Tests](#tests)
- [Feature-Scope Commits](#feature-scope-commits)
  - [Foundational](#foundational)
  - [CONCURRENTLY infrastructure](#concurrently-infrastructure)
  - [Correctness fixes](#correctness-fixes)
  - [Error messages, docs, style](#error-messages-docs-style)
  - [Tests](#tests-1)
- [Context Reviewed](#context-reviewed)
- [Evidence Map](#evidence-map)
- [Source References](#source-references)
- [Open Questions](#open-questions)
- [Related Pages](#related-pages)

## Question

Follow AGENTS.md.
In PostgreSQL 19, do a comprehensive explanation of how `pg_repack` works, explain all commits to the feature.

> Scope note (agreed with the asker): the third-party `pg_repack` extension is **not** in the pinned `raw/postgres-19/` checkout, so it cannot be cited under the wiki evidence rules. PostgreSQL 19 instead adds an **in-core `REPACK` command** (with a `CONCURRENTLY` option) that brings pg_repack/pg_squeeze-style online table rewriting into core. This page documents that in-core feature.

Follow-up question (2026-09-02), after prompt hygiene; the asker chose "correct and restate":

In PostgreSQL 19, for the question page "How the REPACK Command Works in PostgreSQL 19, and Its 58 Feature-Scope Commits": does REPACK have any way to throttle it, to make it nice on I/O?

> Answered in [I/O Impact and Throttling](#io-impact-and-throttling), source-and-history scope at the asker's choice: no v19 server was built, so that section carries no measurements.

## Short Answer

`REPACK` is a new top-level SQL command in PostgreSQL 19 that rewrites a table into a fresh relfilenode to reclaim dead-tuple space, optionally clustering rows by an index. It **absorbs the functionality of `VACUUM FULL` and `CLUSTER`** into one command: physical-order rewrite (like `VACUUM FULL`) and index-order rewrite (like `CLUSTER`) are just two modes of `REPACK`, and the two legacy commands are kept but de-emphasized [repack.c#overview](../../../../raw/postgres-19/src/backend/commands/repack.c#L1-L22) [ac58465e commit message](#feature-scope-commits). The command is documented as "rewrite a table to reclaim disk space" [ref/repack.sgml#refpurpose](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L17-L20).

`REPACK` has two execution modes:

- **Blocking (default).** Takes `AccessExclusiveLock`, builds a new heap, copies live tuples in the chosen order, rebuilds indexes via `REINDEX`, then swaps relfilenodes. This is the classic `CLUSTER`/`VACUUM FULL` path, now reached through `repack.c` [repack.c#rebuild_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L1092-L1117).
- **`CONCURRENTLY`.** Takes only `ShareUpdateExclusiveLock` for almost all of the work, so readers and writers keep using the table. It captures concurrent DML with **logical decoding** through a dedicated background worker and a temporary replication slot, replays those changes onto the new heap, and only upgrades to `AccessExclusiveLock` for the brief final relfilenode swap [repack.c#overview](../../../../raw/postgres-19/src/backend/commands/repack.c#L7-L21) [ref/repack.sgml#CONCURRENTLY](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L221-L234).

On the follow-up question: **`REPACK` cannot be throttled.** It has no cost-based delay, no I/O-related option, and no dedicated GUC; the only levers are the ones that make it move less data, keep it out of shared buffers, or split it into smaller pieces you schedule yourself. See [I/O Impact and Throttling](#io-impact-and-throttling).

This page tracks **58 feature-scope commits (2026-03-10 to 2026-08-29)**: commits whose subject/body explicitly references REPACK, adjacent prerequisite/support commits used by that history, and tree-wide fixes that changed REPACK-specific code or text. Álvaro Herrera committed most of them, and the commit-message `Author:` trailers credit most of the code to Antonin Houska and to Herrera himself, building on Houska's `pg_squeeze` extension. The tables under [Feature-Scope Commits](#feature-scope-commits) name each commit's **committer**, adding "patch by …" where the commit message names a different author.

## Syntax and Modes

The grammar produces a single `RepackStmt` node for `REPACK` and `CLUSTER`, distinguished by a `RepackCommand` tag [parsenodes.h#RepackStmt](../../../../raw/postgres-19/src/include/nodes/parsenodes.h#L4063-L4078) [gram.y#RepackStmt](../../../../raw/postgres-19/src/backend/parser/gram.y#L12534-L12627). `VACUUM (FULL)` still parses as `VacuumStmt`; when the `FULL` option is present, `vacuum.c` routes execution into the same rewrite engine with `REPACK_COMMAND_VACUUMFULL` [gram.y#VacuumStmt](../../../../raw/postgres-19/src/backend/parser/gram.y#L12647-L12679) [vacuum.c#VACUUMFULL-repack](../../../../raw/postgres-19/src/backend/commands/vacuum.c#L2302-L2305):

```
REPACK [ ( option [, ...] ) ] [ table [ ( column [, ...] ) ] [ USING INDEX [ index_name ] ] ]
REPACK [ ( option [, ...] ) ] USING INDEX
```

where `option` is `VERBOSE`, `ANALYZE`, or `CONCURRENTLY` for `REPACK`; `CLUSTER` accepts only `VERBOSE` [ref/repack.sgml#synopsis](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L22-L36) [repack.c#ANALYZE-option](../../../../raw/postgres-19/src/backend/commands/repack.c#L267-L276).

The table name is a plain `qualified_name` plus an optional column list. Until `f23de46e15b` the production reused `vacuum_relation`, so the parser silently accepted `REPACK ONLY t` and `REPACK t *` even though neither is documented and neither changed what the command did; the production now spells out `qualified_name opt_name_list` and builds the `VacuumRelation` itself, so both spellings are syntax errors [gram.y#RepackStmt](../../../../raw/postgres-19/src/backend/parser/gram.y#L12534-L12627) [ref/repack.sgml#synopsis](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L22-L36). `VACUUM`'s own grammar still takes `vacuum_relation`, so `VACUUM (FULL) ONLY t` is unaffected [gram.y#vacuum_relation](../../../../raw/postgres-19/src/backend/parser/gram.y#L12727-L12732).

Key forms:

| Form | Effect |
|---|---|
| `REPACK t` | Rewrite `t` in physical order (like `VACUUM FULL`) [ref/repack.sgml#Description](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L42-L50). |
| `REPACK t USING INDEX idx` | Rewrite `t` ordered by `idx`, and mark `idx` as the clustered index (like `CLUSTER`) [ref/repack.sgml#clustering](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L79-L93). |
| `REPACK t USING INDEX` | Use the table's already-clustered index; error if none [repack.c#determine_clustered_index](../../../../raw/postgres-19/src/backend/commands/repack.c#L2501-L2542). |
| `REPACK` (no table) | Process every table/matview the caller has `MAINTAIN` on; not allowed in a transaction block or with `CONCURRENTLY` [ref/repack.sgml#no-table](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L52-L60). |
| `REPACK USING INDEX` (no table) | Process all tables that have a clustering index configured [ref/repack.sgml#no-table-using-index](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L95-L99). |
| `REPACK (CONCURRENTLY) t` | Online rewrite; requires an explicit single non-partitioned table [repack.c#multi-table-guard](../../../../raw/postgres-19/src/backend/commands/repack.c#L345-L361). |

`utility.c` dispatches `T_RepackStmt` to `ExecRepack()`, and reports the command tag as `REPACK` unless the statement is a legacy `CLUSTER` [utility.c#ExecRepack](../../../../raw/postgres-19/src/backend/tcop/utility.c#L866-L867) [utility.c#CMDTAG](../../../../raw/postgres-19/src/backend/tcop/utility.c#L2910-L2914). `VACUUM (FULL)` reaches the same engine by calling `cluster_rel(REPACK_COMMAND_VACUUMFULL, …)` from `vacuum.c` [vacuum.c#cluster_rel](../../../../raw/postgres-19/src/backend/commands/vacuum.c#L2303-L2305). To run `REPACK`, the caller needs the `MAINTAIN` privilege on the table [repack.c#repack_is_permitted_for_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L2344-L2395) [ref/repack.sgml#Notes](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L355-L358).

The generic privilege reference now lists `REPACK` among the commands authorized by `MAINTAIN`, and the predefined `pg_maintain` role grants that right on all relations [ddl.sgml#MAINTAIN](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L2544-L2556) [user-manag.sgml#pg_maintain](../../../../raw/postgres-19/doc/src/sgml/user-manag.sgml#L655-L670). At runtime, membership in `pg_maintain` adds `ACL_MAINTAIN` when the relation ACL is checked [aclchk.c#pg_class_aclmask_ext](../../../../raw/postgres-19/src/backend/catalog/aclchk.c#L3437-L3446).

## Source Layout

```text
src/backend/commands/repack.c            ExecRepack, cluster_rel, the rewrite engine
                                         (formerly cluster.c; renamed in c0b53ec0)
src/backend/commands/repack_worker.c     background worker that does the logical decoding
src/backend/replication/pgrepack/        "pgrepack" logical-decoding output plugin
  pgrepack.c                             change_cb spills tuples to per-relation files
src/include/commands/repack.h            ExecRepack/cluster_rel API, CLUOPT_* flags
src/include/commands/repack_internal.h   ConcurrentChangeKind, RepackDecodingState,
                                         DecodingWorkerShared
doc/src/sgml/ref/repack.sgml             REPACK reference page
```

The output plugin is built as `pgrepack` and linked into the backend (`backend_targets += pgrepack`), not shipped as a `contrib` extension; the worker loads it by the fixed name `"pgrepack"` [pgrepack/meson.build](../../../../raw/postgres-19/src/backend/replication/pgrepack/meson.build#L13-L18) [repack_worker.c#PGREPACK_PLUGIN](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L31). The plugin refuses any use outside the REPACK worker, and since `5d47df21e89` the user-visible rejection happens **before the plugin library is loaded**. `StartupDecodingContext()` special-cases the plugin name `"pgrepack"` and, unless the caller passed `for_repack` and `AmRepackWorker()` is true, errors with `unsupported use of logical decoding plugin "pgrepack"` / `DETAIL: This plugin can only be used by REPACK (CONCURRENTLY).`; only then does it call `LoadOutputPlugin()` [logical.c#StartupDecodingContext-plugin-check](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L179-L259) [logical.c#pgrepack-special-case](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L188-L203). `repack_startup()` keeps only a defensive `elog(FATAL, "unexpected pgrepack startup outside of repack worker")` for the case that check misses (commit `cd7b204b` introduced the original rejection there) [pgrepack.c#repack_startup](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L50-L80).

Every other output plugin must be named in the new `output_plugin_libraries` GUC or the same function errors with `library "%s" may not be used as an output plugin` [logical.c#StartupDecodingContext-plugin-check](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L179-L259). That GUC is `PGC_SUSET` with boot value `"pgoutput, test_decoding"`, so a superuser `SET` applies to the session/transaction with no restart or reload, while a `postgresql.conf` change needs a configuration reload [guc_parameters.dat#output_plugin_libraries](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2311-L2318). Despite the file/dir name `pgrepack`, the user-facing command is `REPACK`; the third-party extension is unrelated.

## Blocking REPACK (the simple rewrite)

`ExecRepack()` parses the option list (`VERBOSE`/`ANALYZE`/`CONCURRENTLY`), sets the `CLUOPT_*` bitmask in `ClusterParams`, picks the lock level, and either processes one relation or builds a work list for the multi-table forms [repack.c#ExecRepack](../../../../raw/postgres-19/src/backend/commands/repack.c#L250-L447) [repack.h#CLUOPT](../../../../raw/postgres-19/src/include/commands/repack.h#L24-L36). Since `d7db169fa13`, `ANALYZE` is accepted only for `REPACK` itself: `ExecRepack()` raises `ANALYZE option not supported for %s` for `CLUSTER (ANALYZE)`, exactly as it already did for `CONCURRENTLY`. `VACUUM (FULL, ANALYZE)` is unaffected because it does not pass through `ExecRepack()` [repack.c#ANALYZE-option](../../../../raw/postgres-19/src/backend/commands/repack.c#L267-L276) [vacuum.c#VACUUMFULL-repack](../../../../raw/postgres-19/src/backend/commands/vacuum.c#L2302-L2305). Multiple tables are each processed in their own transaction (like `VACUUM`) to avoid holding many strong locks at once, which is why the multi-table and `CONCURRENTLY` forms cannot run inside a transaction block [repack.c#per-table-txn](../../../../raw/postgres-19/src/backend/commands/repack.c#L227-L249).

`cluster_rel()` is the per-relation driver. It starts progress reporting, switches to the table owner's userid under a security-restricted context, sets `search_path` to `pg_catalog, pg_temp`, rechecks the relation in the multi-table case, validates the clustering index, and then calls `rebuild_relation()` [repack.c#cluster_rel](../../../../raw/postgres-19/src/backend/commands/repack.c#L489-L670) [ref/repack.sgml#search_path](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L360-L364). The lock level is `AccessExclusiveLock` for blocking mode and `ShareUpdateExclusiveLock` for concurrent mode [repack.c#RepackLockLevel](../../../../raw/postgres-19/src/backend/commands/repack.c#L456-L463).

`rebuild_relation()` (blocking path):

1. If an index was given, mark it `indisclustered` [repack.c#mark_index_clustered](../../../../raw/postgres-19/src/backend/commands/repack.c#L1037-L1039).
2. `make_new_heap()` creates a transient `pg_temp_<oid>` heap with the old heap's tuple descriptor, reloptions, access method, and (if needed) a TOAST table [repack.c#make_new_heap](../../../../raw/postgres-19/src/backend/commands/repack.c#L1131-L1205).
3. `copy_table_data()` copies live tuples into the new heap. It chooses **sequential-scan-and-sort** vs **index scan** vs **plain seqscan** via `plan_cluster_use_sort()` for a btree clustering index, computes freeze/multixact cutoffs, and hands the actual copy to the table AM's `table_relation_copy_for_cluster()` [repack.c#copy_table_data](../../../../raw/postgres-19/src/backend/commands/repack.c#L1391-L1426).
4. `finish_heap_swap()` swaps the relfilenodes of old and new heaps, rebuilds the indexes (`reindex = true`), applies the new `relfrozenxid`/`relminmxid`, and drops the transient heap [repack.c#finish_heap_swap-call](../../../../raw/postgres-19/src/backend/commands/repack.c#L1108-L1116).

Predicate (SSI) locks on tuples/pages are promoted to a relation lock before the rewrite, because tuples move [repack.c#TransferPredicateLocks](../../../../raw/postgres-19/src/backend/commands/repack.c#L628-L639).

### Multi-table discovery and recheck

The no-table forms and partition fan-out first collect lightweight `RelToCluster { tableOid, indexOid }` records. They do **not** retain a lock on each target while building that list, so each OID pair is only a candidate and may become stale before its turn [repack.c#RelToCluster](../../../../raw/postgres-19/src/backend/commands/repack.c#L86-L96) [repack.c#get_tables_to_repack](../../../../raw/postgres-19/src/backend/commands/repack.c#L2110-L2213) [repack.c#get_tables_to_repack_partitioned](../../../../raw/postgres-19/src/backend/commands/repack.c#L2215-L2341).

For each candidate, `ExecRepack()` starts a new transaction and obtains the final mode from `RepackLockLevel()` with `try_relation_open()`. It silently skips an OID that was dropped or now names something other than a plain table or materialized view. With that target locked, `cluster_rel_recheck()` checks the caller's `MAINTAIN` privilege, remote-temporary-table status, index existence, and — where required — whether the index is still marked `indisclustered` [repack.c#per-table-open](../../../../raw/postgres-19/src/backend/commands/repack.c#L406-L439) [repack.c#cluster_rel_recheck](../../../../raw/postgres-19/src/backend/commands/repack.c#L672-L726).

`repack_is_permitted_for_relation()` runs on both sides of that boundary, and since `9bb8e16bd53` it takes an `already_locked` flag that says which side is calling. The two unlocked discovery loops pass `false`, so `pg_class_aclcheck_ext()`'s `is_missing` result still means "dropped meanwhile" and the candidate is dropped from the list silently. `cluster_rel_recheck()`, which already holds the operation lock, passes `true`; the function then asserts `CheckRelationOidLockedByMe(relid, AccessShareLock, true)` on entry and asserts `!already_locked` if the relation turns up missing, so a vanished relation under a held lock is a bug rather than a skip. Either way, an existing relation the caller lacks `MAINTAIN` on still produces the `permission denied to execute %s on "%s", skipping it` WARNING [repack.c#repack_is_permitted_for_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L2344-L2395) [repack.c#cluster_rel_recheck-permission](../../../../raw/postgres-19/src/backend/commands/repack.c#L682-L689). The commit follows the same fix made for `vacuum_is_permitted_for_relation()` in `824d5f6241ea`, and also names REPACK in `RangeVarCallbackMaintainsTable`'s comment [tablecmds.c#RangeVarCallbackMaintainsTable](../../../../raw/postgres-19/src/backend/commands/tablecmds.c#L20125-L20136).

Commit `133eba078f7` made these rules consistent. The earlier whole-database list path briefly acquired transaction-scoped `AccessShareLock`s, but the database transaction committed immediately after list construction and therefore released them before processing. The commit removed those ineffective locks and hardened the locked per-table path against drops and OIDs that now identify a non-table relkind. It added no direct test or documentation change.

Commit `69fe2514fdd` then moved partitioned-table and partitioned-index resolution into `get_tables_to_repack_partitioned()`. The caller passes the already-open, `AccessExclusiveLock`-protected parent; the helper resolves `USING INDEX`, collects leaf candidates without locking them, closes and unlocks the parent, and returns the list [repack.c#get_tables_to_repack_partitioned](../../../../raw/postgres-19/src/backend/commands/repack.c#L2215-L2341). For a concurrently disappearing leaf index, it now calls `IndexGetRelation(..., true)` and silently skips an invalid result instead of raising a cache-lookup error [repack.c#partition-index-disappearance](../../../../raw/postgres-19/src/backend/commands/repack.c#L2293-L2308). This remains discovery-time hardening; each surviving table still undergoes the locked per-table recheck above.

## REPACK CONCURRENTLY (online rewrite via logical decoding)

The concurrent path keeps the table online by combining a low-strength lock with logical decoding. The cast of characters:

| Component | Role |
|---|---|
| **Leader backend** | Runs `REPACK`; holds `ShareUpdateExclusiveLock`, copies data, applies captured changes, does the final swap [repack.c#rebuild_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L1001-L1091). |
| **Decoding worker** | `bgworker` running `RepackWorkerMain`; owns a temporary logical slot and decodes WAL into spill files [repack_worker.c#RepackWorkerMain](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L59-L164). |
| **`pgrepack` output plugin** | Decoding callbacks; writes each insert/update/delete tuple of the target relation to the current spill file [pgrepack.c#change_cb](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L111-L180). |
| **Temporary replication slot** | `pg_repack_<pid>`, `RS_TEMPORARY` so it is dropped on error/exit; pins the WAL/snapshot start point [repack_worker.c#slot](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L217-L223). |
| **DSM + spill fileset** | `DecodingWorkerShared` in DSM coordinates the two processes; a `SharedFileSet` holds the exported snapshot and change files [repack_internal.h#DecodingWorkerShared](../../../../raw/postgres-19/src/include/commands/repack_internal.h#L61-L117). |

### Step by step

1. **Precondition checks.** `check_concurrent_repack_requirements()` enforces `wal_level >= replica`, rejects catalogs, TOAST tables, non-permanent (temp/unlogged) tables, and `REPLICA IDENTITY NOTHING`/`FULL`, and obtains the **identity index** (replica-identity index or a non-deferrable primary key). Without a usable identity index, concurrent mode is refused [repack.c#check_concurrent_repack_requirements](../../../../raw/postgres-19/src/backend/commands/repack.c#L865-L956).
2. **Start the worker before any XID is assigned.** The leader becomes a lock-group leader, then `start_repack_decoding_worker()` sets up the DSM, registers the `bgworker`, and blocks until the worker reports `initialized` [repack.c#start_repack_decoding_worker](../../../../raw/postgres-19/src/backend/commands/repack.c#L1001-L1027) [repack.c#start_repack_decoding_worker-impl](../../../../raw/postgres-19/src/backend/commands/repack.c#L3621-L3703). The worker creates the temporary slot, enables logical decoding, restricts decoding to the target relation (and its TOAST relation), builds an initial historic snapshot, and exports it to a file [repack_worker.c#setup](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L194-L291).
3. **Initial copy under the exported snapshot.** The leader reads the exported snapshot with `get_initial_snapshot()`, pushes it active, and runs `copy_table_data()` against that MVCC snapshot, so the initial copy is a consistent point-in-time image [repack.c#get_initial_snapshot](../../../../raw/postgres-19/src/backend/commands/repack.c#L1029-L1073) [repack.c#get_initial_snapshot-impl](../../../../raw/postgres-19/src/backend/commands/repack.c#L3765-L3813). Meanwhile the worker keeps decoding WAL in a loop, and later advances the slot's restart/confirmed LSN at WAL-segment boundaries so processed WAL can be recycled [repack_worker.c#decode_loop](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L152-L164) [repack_worker.c#WAL-recycling](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L374-L403).
4. **Build new indexes** on the new heap (instead of reindexing the old one), since this is slow and is done before the exclusive lock [repack.c#build_new_indexes](../../../../raw/postgres-19/src/backend/commands/repack.c#L3417-L3448).
5. **Catch-up #1 (still under `ShareUpdateExclusiveLock`).** `process_concurrent_changes()` tells the worker an `lsn_upto`, waits for the next spill file, and `apply_concurrent_changes()` replays it onto the new heap. Doing this first minimizes how long the exclusive lock is later held [repack.c#catch-up-1](../../../../raw/postgres-19/src/backend/commands/repack.c#L3270-L3283).
6. **Lock upgrade + catch-up #2.** The leader takes `AccessExclusiveLock` on the table, all its indexes, and its TOAST relation, transfers predicate locks, flushes WAL, and applies the remaining changes with `done = true` so the worker exits [repack.c#lock-upgrade](../../../../raw/postgres-19/src/backend/commands/repack.c#L3286-L3340).
7. **Swap.** `swap_relation_files()` swaps each index's storage, then `finish_heap_swap()` swaps the heap (and TOAST), dropping the transient relation. The old OID, grants, and dependencies are preserved [repack.c#swap](../../../../raw/postgres-19/src/backend/commands/repack.c#L3353-L3404).

### How concurrent changes are captured and replayed

The `pgrepack` plugin's `change_cb` only ever sees the target relation (other relations are filtered out during decoding by `change_useless_for_repack()`), and writes a compact record per change: a one-byte kind (`i`/`u`/`U`/`d`), the heap tuple bytes, and any out-of-line external attributes spilled separately to stay under `MaxAllocSize` [pgrepack.c#repack_store_change](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L191-L306) [repack_worker.c#change_useless_for_repack](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L499-L536) [repack_internal.h#ConcurrentChangeKind](../../../../raw/postgres-19/src/include/commands/repack_internal.h#L24-L32).

`apply_concurrent_changes()` reads the spill file and replays each change onto the new heap, bumping the command counter between dependent changes [repack.c#apply_concurrent_changes](../../../../raw/postgres-19/src/backend/commands/repack.c#L2562-L2682):

- **INSERT** → `table_tuple_insert(..., TABLE_INSERT_NO_LOGICAL)` plus index maintenance. The `NO_LOGICAL` flag stops these replayed rows from being decoded again [repack.c#apply_concurrent_insert](../../../../raw/postgres-19/src/backend/commands/repack.c#L2688-L2703).
- **UPDATE/DELETE** → locate the existing tuple in the new heap via the identity index (`find_target_tuple()`), then `table_tuple_update`/`table_tuple_delete` with `NO_LOGICAL`. For updates, unchanged TOAST pointers are re-pointed at the new relation's TOAST table via `adjust_toast_pointers()` [repack.c#apply_concurrent_update](../../../../raw/postgres-19/src/backend/commands/repack.c#L2709-L2773) [repack.c#toast-pointers](../../../../raw/postgres-19/src/backend/commands/repack.c#L2655-L2665).

The identity-index scan key is precomputed in `initialize_change_context()`, which resolves the equality strategy from the index's operator family (so it works for non-btree identity indexes too — see commit `36f52a59`) [repack.c#initialize_change_context](../../../../raw/postgres-19/src/backend/commands/repack.c#L3039-L3178). When replay reconstructs a spilled tuple, `restore_tuple()` checks that every separately stored varlena attribute is consumed and errors if the spill file contains too few or too many separately stored attributes; commit `e2a8cabc` added the leftover-attribute check [repack.c#restore_tuple](../../../../raw/postgres-19/src/backend/commands/repack.c#L2785-L2852).

### Error handling and worker teardown

The leader runs `rebuild_relation()` inside `PG_ENSURE_ERROR_CLEANUP(stop_repack_decoding_worker_cb)` so the worker and its slot are cleaned up even on `FATAL` exit (e.g. `pg_terminate_backend()`), and `stop_repack_decoding_worker()` terminates the worker, detaches the error queue, cancels CV sleeps, and frees the DSM [repack.c#ensure-cleanup](../../../../raw/postgres-19/src/backend/commands/repack.c#L650-L658) [repack.c#stop_repack_decoding_worker](../../../../raw/postgres-19/src/backend/commands/repack.c#L3711-L3760). Worker errors are forwarded to the leader through a shared-memory message queue and re-thrown with a "REPACK decoding worker" context line, using the `PROCSIG_REPACK_MESSAGE` interrupt and `ProcessRepackMessages()` [repack.c#ProcessRepackMessages](../../../../raw/postgres-19/src/backend/commands/repack.c#L3847-L3974) [repack_worker.c#RepackWorkerShutdown](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L169-L179).

### WAL retention and `effective_wal_level`

Normally a replication slot pins `restart_lsn` so WAL can't be recycled. `REPACK` does not need crash-safe replay (a crash just restarts the whole operation), so the worker advances the slot's restart/confirmed LSN as it crosses each WAL-segment boundary, letting old WAL be recycled while `REPACK` runs (commit `45b02984`) [repack_worker.c#WAL-recycling](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L374-L403). Because the temporary slot is logical, the worker calls `EnsureLogicalDecodingEnabled()` at setup and `ReplicationSlotDropAcquired(true)` at teardown so `effective_wal_level` does not stay stuck at `logical` afterward (commit `2af1dc89`) [repack_worker.c#enable-decoding](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L211-L224) [repack_worker.c#cleanup](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L293-L304).

Commit `10389a7e15e` does not change that recycling behavior. It fixes the worker's `DEBUG1` message to print the confirmed LSN with the standard `%X/%08X` shape through `LSN_FORMAT_ARGS()`, including zero-padding the low half [repack_worker.c#confirmed-LSN-debug](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L394-L402).

At `wal_level = replica`, the worker can also be one of two backends concurrently trying to enable logical decoding: every concurrent REPACK creates its temporary slot and calls `EnsureLogicalDecodingEnabled()` [repack_worker.c#enable-decoding](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L210-L224). Commit `99e47536bbf` closes the activation race by checking `logical_decoding_enabled` again after the process-signal barrier and after reacquiring `LogicalDecodingControlLock`; a backend that lost the race returns without writing a redundant status-change WAL record [logicalctl.c#EnableLogicalDecoding](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L342-L426). Without that recheck, another logical-slot creator could enable decoding while the REPACK worker waited, and the redundant record could later reach `xlog_decode()`, which treats a decoded status change as an error [decode.c#xlog_decode](../../../../raw/postgres-19/src/backend/replication/logical/decode.c#L153-L172). The injection-point test pauses one activation, lets another create a slot, resumes the first, and verifies that decoding from the second slot succeeds [051_effective_wal_level.pl#concurrent-activation](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L385-L426).

Commit `6a80179f6b0` then closed the opposite race, a *deactivation* that interleaves with slot creation. `EnsureLogicalDecodingEnabled()`'s recovery branch no longer asserts that decoding is still enabled; it re-checks after the slot exists — so a concurrent deactivation can see the slot's `in_use` flag — and raises `logical decoding on standby requires "effective_wal_level" >= "logical" on the primary` with detail `Logical decoding was concurrently disabled during the logical replication slot creation.` [logicalctl.c#EnsureLogicalDecodingEnabled](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L287-L336). Slot synchronization takes a separate path: `update_and_persist_local_synced_slot()` keeps the temporary slot, logs `could not synchronize replication slot "%s"`, and lets the next cycle retry [slotsync.c#update_and_persist_local_synced_slot](../../../../raw/postgres-19/src/backend/replication/logical/slotsync.c#L712-L745). Scope matters here: that branch runs only while `RecoveryInProgress()`, i.e. for slot creation on a standby, and a REPACK worker rewrites a table and so runs only on a writable server — the error is therefore not on the worker's path, even though the worker calls the same `EnsureLogicalDecodingEnabled()` [repack_worker.c#enable-decoding](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L211-L224). The commit also adds a recovery-race block to the same TAP test, driven by the new `replication-slot-create-begin` injection point [051_effective_wal_level.pl#deactivation-races](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L515-L671) [slot.c:423](../../../../raw/postgres-19/src/backend/replication/slot.c#L423).

## Restrictions on CONCURRENTLY

`REPACK (CONCURRENTLY)` is rejected for any of: an `UNLOGGED` or temporary table; a partitioned table; a table without a primary key or index-based replica identity (including deferrable PKs); a system catalog or TOAST table; execution inside a transaction block; or when `max_repack_replication_slots` leaves no slot free [ref/repack.sgml#cannot-use](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L259-L302) [repack.c#check_concurrent_repack_requirements](../../../../raw/postgres-19/src/backend/commands/repack.c#L865-L956). It is documented as **not MVCC-safe** [ref/repack.sgml#warning](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L304-L310). After the `01a80f06` revert (see commits), only **one** `REPACK (CONCURRENTLY)` may run at a time across the whole instance.

## GUC: `max_repack_replication_slots`

Concurrent `REPACK` consumes a logical replication slot for its lifetime. Rather than forcing users to inflate `max_replication_slots`, v19 adds a dedicated pool sized by `max_repack_replication_slots` (default `5`, min `0`) [guc_parameters.dat#max_repack_replication_slots](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2108-L2114) [slot.c#decl](../../../../raw/postgres-19/src/backend/replication/slot.c#L163-L164). `ReplicationSlotCreate(..., repack = true, ...)` allocates from the extra `[max_replication_slots, max_replication_slots + max_repack_replication_slots)` band of the slot array, so REPACK slots never compete with ordinary replication slots [slot.c#ReplicationSlotCreate](../../../../raw/postgres-19/src/backend/replication/slot.c#L372-L465).

**Scope:** `max_repack_replication_slots` is `PGC_POSTMASTER` — it **requires a server restart** [guc_parameters.dat#context](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2108-L2108). The decoding worker also needs a free `max_worker_processes` slot, or `REPACK (CONCURRENTLY)` errors out [repack.c#worker-slots](../../../../raw/postgres-19/src/backend/commands/repack.c#L3676-L3680).

## I/O Impact and Throttling

**PostgreSQL 19 gives `REPACK` no throttle.** No command option, no reloption, and no GUC makes it pause or rate-limit itself. `ExecRepack()` accepts exactly three options — `VERBOSE`, `ANALYZE`, `CONCURRENTLY` — and none of them concerns I/O [repack.c#ExecRepack-options](../../../../raw/postgres-19/src/backend/commands/repack.c#L262-L297).

Unlike lazy (non-`FULL`) `VACUUM`, the rewrite path has **no cost-based delay**. `vacuum_delay_point()` [vacuum.h#vacuum_delay_point](../../../../raw/postgres-19/src/include/commands/vacuum.h#L390) is called from `vacuumlazy.c` [vacuumlazy.c:1333](../../../../raw/postgres-19/src/backend/access/heap/vacuumlazy.c#L1333), `analyze.c` [analyze.c:940](../../../../raw/postgres-19/src/backend/commands/analyze.c#L940) and the index AMs' vacuum routines [nbtree.c:1725](../../../../raw/postgres-19/src/backend/access/nbtree/nbtree.c#L1725), and from nowhere in `repack.c` [repack.c#cluster_rel](../../../../raw/postgres-19/src/backend/commands/repack.c#L489-L670), the table-AM copy routine [heapam_handler.c#heapam_relation_copy_for_cluster](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L593-L962), or `rewriteheap.c` [rewriteheap.c#raw_heap_insert](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L596-L680). The only interruption point in the copy loop is `CHECK_FOR_INTERRUPTS()` [heapam_handler.c:721](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L721). So `vacuum_cost_delay`, `vacuum_cost_limit` and the autovacuum cost knobs do nothing for `REPACK`, `CLUSTER` or `VACUUM FULL`.

Nothing schedules a rewrite for you either: the only callers of `cluster_rel()` are `ExecRepack()` [repack.c#cluster_rel-call](../../../../raw/postgres-19/src/backend/commands/repack.c#L2455-L2463) and `VACUUM (FULL)` [vacuum.c#VACUUMFULL-repack](../../../../raw/postgres-19/src/backend/commands/vacuum.c#L2302-L2305), and autovacuum never sets `VACOPT_FULL` [autovacuum.c#at_params.options](../../../../raw/postgres-19/src/backend/postmaster/autovacuum.c#L2913-L2923). What you *can* control is how much data the command moves, how much of it passes through shared buffers, how much WAL it writes, and when and in how many pieces it runs.

### Where the writes and reads come from

| Work | Blocking `REPACK` | `REPACK (CONCURRENTLY)` |
|---|---|---|
| Read the old heap | Sequential scan under `SnapshotAny`, or an index scan when `USING INDEX` picks one [heapam_handler.c#scan-choice](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L659-L704) | Same two shapes, but under an MVCC snapshot from the decoding worker [repack.c#get_initial_snapshot](../../../../raw/postgres-19/src/backend/commands/repack.c#L1029-L1035) |
| Dirty the old heap while reading | Yes. Every tuple is checked with `HeapTupleSatisfiesVacuum()` under an **exclusive** buffer lock so hint bits stick; the source comment concedes "rewriting most of the old table during VACUUM FULL doesn't exactly help" [heapam_handler.c#hint-bits](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L778-L797) | Yes, differently. An MVCC snapshot re-enables page-at-a-time mode, so each page goes through `heap_page_prune_opt()`, which can prune, dirty and WAL-log it [heapam.c#heap_prepare_pagescan](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L619-L640) [pruneheap.c#heap_page_prune_opt](../../../../raw/postgres-19/src/backend/access/heap/pruneheap.c#L272-L337). The blocking scan never reaches that call because `SnapshotAny` disables page mode [heapam.c#pagemode-off](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L1209-L1213) |
| Write the new heap | Through `bulk_write.c`, which **bypasses shared buffers** and calls `smgrextend()` directly [rewriteheap.c#smgr_bulk_start_rel](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L262-L265) [bulk_write.c#overview](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L6-L25) [bulk_write.c#smgr_bulk_flush](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L241-L312) | `heap_insert()` through the buffer manager with a `BAS_BULKWRITE` ring [heapam_handler.c#heap_insert_for_repack](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L2366-L2394) [heapam.c#GetBulkInsertState](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L1935-L1948) |
| WAL for the new heap | Full-page images batched up to `XLR_MAX_BLOCK_ID` = 32 pages per record [bulk_write.c#MAX_PENDING_WRITES](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L47) [xlogrecord.h:241](../../../../raw/postgres-19/src/include/access/xlogrecord.h#L241) [bulk_write.c#log_newpages](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L253-L275); skipped entirely under `wal_level = minimal` [bulk_write.c#smgr_bulk_start_rel](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L86-L92) [rel.h#RelationNeedsWAL](../../../../raw/postgres-19/src/include/utils/rel.h#L631-L642) | Ordinary per-tuple insert WAL. `wal_level = minimal` is not an option here — `CONCURRENTLY` needs logical decoding [ref/repack.sgml#CONCURRENTLY](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L221-L234) |
| TOAST | Rewritten. Any tuple with an out-of-line attribute goes back through the toaster [rewriteheap.c#raw_heap_insert-toast](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L606-L634), and each chunk is a plain `heap_insert()` with **no** bulk-insert state, so TOAST writes land in shared buffers with no ring [toast_internals.c#chunk-insert](../../../../raw/postgres-19/src/backend/access/common/toast_internals.c#L300-L315). Value OIDs are preserved and the two TOAST tables are later swapped by content [repack.c#swap-toast-by-content](../../../../raw/postgres-19/src/backend/commands/repack.c#L1314-L1349) | Rewritten as well, and without the swap-by-content shortcut: the `!concurrent` test excludes it and the caller asserts as much [repack.c#swap-toast-by-content](../../../../raw/postgres-19/src/backend/commands/repack.c#L1314-L1349) [repack.c#assert-no-swap-by-content](../../../../raw/postgres-19/src/backend/commands/repack.c#L1075-L1078) |
| Sort spill | Only when scan-and-sort is chosen; `tuplesort_begin_cluster()` gets `maintenance_work_mem` and spills beyond it [heapam_handler.c#tuplesort](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L651-L657). Documented peak temp space is up to **double the table size** plus the index sizes [ref/repack.sgml#Notes-on-Resources](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L142-L167) | Same |
| Indexes | Rebuilt *after* the file swap, by `reindex_relation()` inside `finish_heap_swap()` [repack.c#finish_heap_swap-reindex](../../../../raw/postgres-19/src/backend/commands/repack.c#L1926-L1966) | Built *before* the lock upgrade, as new indexes on the new heap [repack.c#build_new_indexes](../../../../raw/postgres-19/src/backend/commands/repack.c#L3221-L3237) |
| Extra, concurrent only | — | The worker decodes WAL and spills every captured change to a `BufFile` in the shared file set [repack_worker.c#decode_concurrent_changes](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L340-L358) [pgrepack.c#repack_store_change](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L191-L302); the leader then reads that file back and replays it. The docs call out the extra temporary space [ref/repack.sgml#CONCURRENTLY-temp-space](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L245-L257) |

Two consequences worth naming:

- **Index rebuilds bypass shared buffers too.** A btree build writes its pages with the same `bulk_write.c` interface as the heap rewrite [nbtsort.c#bulkstate](../../../../raw/postgres-19/src/backend/access/nbtree/nbtsort.c#L1153) [nbtsort.c#_bt_blwritepage](../../../../raw/postgres-19/src/backend/access/nbtree/nbtsort.c#L618-L644).
- **The index-scan copy path is the least considerate one.** `heapam_index_fetch_tuple()` reads each heap page with a plain `ReadBuffer()` — no access strategy, no read stream, no I/O combining — and prunes it on first pin [heapam_indexscan.c#heapam_index_fetch_tuple](../../../../raw/postgres-19/src/backend/access/heap/heapam_indexscan.c#L231-L263). The sequential path gets both a ring buffer and a read stream (below).

### Settings that change the load

The sequential scan of the old heap is the one part with built-in restraint. `initscan()` gives it a `BAS_BULKREAD` ring **and** enables synchronized scanning, but only when the relation is larger than `NBuffers / 4` [heapam.c#initscan](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L360-L411); `table_beginscan()` supplies the `SO_ALLOW_STRAT`/`SO_ALLOW_SYNC` flags that permit both [tableam.h#table_beginscan](../../../../raw/postgres-19/src/include/access/tableam.h#L942-L951). In v19 that ring is not a fixed 256 kB: it grows by `io_combine_limit * effective_io_concurrency` blocks, capped by the backend pin limit and by `NBuffers / 8` [freelist.c#GetAccessStrategy](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c#L425-L500) [freelist.c#GetAccessStrategyWithSize](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c#L511-L541). The scan reads through a `READ_STREAM_SEQUENTIAL` stream [heapam.c#read_stream_begin_relation](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L1275-L1305), whose look-ahead comes from `effective_io_concurrency` — not `maintenance_io_concurrency`, because the stream does not pass `READ_STREAM_MAINTENANCE` [read_stream.c#max_ios](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c#L795-L834).

| Setting | What it does to `REPACK` | Apply scope |
|---|---|---|
| `maintenance_work_mem` | Sizes the cluster sort and each index build; more memory means fewer temp-file passes, not a slower command [heapam_handler.c#tuplesort](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L651-L657) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#maintenance_work_mem](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L1951-L1959) |
| `enable_indexscan = off` | Forces scan-and-sort for `USING INDEX`: `plan_cluster_use_sort()` short-circuits to "use sort" before costing anything [planner.c#enable_indexscan-shortcircuit](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7093-L7095) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#enable_indexscan](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L936-L941) |
| `effective_io_concurrency` | Read-ahead depth of the sequential scan's read stream, and part of the BULKREAD ring size [read_stream.c#max_ios](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c#L795-L834) [freelist.c#GetAccessStrategy](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c#L425-L500) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#effective_io_concurrency](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L847-L855) |
| `io_combine_limit` | Maximum blocks merged into one read [read_stream.c#io_combine_limit](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c#L917-L944) | `PGC_USERSET` -> session/transaction, bounded by the `PGC_POSTMASTER` `io_max_combine_limit` (restart) [guc_parameters.dat#io_combine_limit](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L1364-L1382) |
| `synchronize_seqscans` | Lets the rewrite's scan join another scan already in flight instead of issuing its own reads; it is why `rs_startblock` may be non-zero [heapam.c#sync-scan-startblock](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L446-L460) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#synchronize_seqscans](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2942-L2946) |
| `max_parallel_maintenance_workers = 0` | Makes every index rebuild serial. `reindex_index()` passes `parallel = true` [index.c#reindex-index_build](../../../../raw/postgres-19/src/backend/catalog/index.c#L3856-L3862), `index_build()` asks the planner for workers [index.c#plan_create_index_workers](../../../../raw/postgres-19/src/backend/catalog/index.c#L3068-L3078), and `plan_create_index_workers()` returns 0 when the GUC is 0 [planner.c#plan_create_index_workers](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7199-L7219) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#max_parallel_maintenance_workers](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2046-L2052) |
| `temp_tablespaces` | Moves the sort file *and* the `CONCURRENTLY` change spool onto another device; the shared file set distributes over the configured tablespaces [fileset.c#FileSetInit](../../../../raw/postgres-19/src/backend/storage/file/fileset.c#L38-L66) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#temp_tablespaces](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L3069-L3077) |
| `temp_file_limit` | Hard ceiling that aborts the command rather than slowing it: `temporary file size exceeds "temp_file_limit"` [fd.c#temp_file_limit](../../../../raw/postgres-19/src/backend/storage/file/fd.c#L2265-L2275) | `PGC_SUSET` -> session/transaction, superuser to raise [guc_parameters.dat#temp_file_limit](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L3059-L3067) |
| `wal_level = minimal` | The largest single reduction: the new heap is not WAL-logged at all, because it was created in the same transaction [rel.h#RelationNeedsWAL](../../../../raw/postgres-19/src/include/utils/rel.h#L631-L642). It rules out `CONCURRENTLY`, which rejects anything below `replica` [repack.c#check_concurrent_repack_requirements](../../../../raw/postgres-19/src/backend/commands/repack.c#L865-L956) | `PGC_POSTMASTER` -> **restart** [guc_parameters.dat#wal_level](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L3505-L3510) |
| `wal_skip_threshold` | Under `wal_level = minimal`, decides fsync-at-commit versus WAL-logging the whole new relation [storage.c#wal_skip_threshold](../../../../raw/postgres-19/src/backend/catalog/storage.c#L787-L826) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#wal_skip_threshold](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L3587-L3594) |
| `backend_flush_after` | Non-zero values ask the kernel to write back evicted dirty buffers sooner, smoothing the write burst — but only for pages that go through the buffer manager, so it misses the blocking rewrite and the index builds. Default 0 [bufmgr.c#ScheduleBufferTagForWriteback](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c#L2630-L2639) [pg_config_manual.h:161](../../../../raw/postgres-19/src/include/pg_config_manual.h#L161) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#backend_flush_after](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L311-L319) |
| `logical_decoding_work_mem` | `CONCURRENTLY` only. The `pgrepack` plugin registers no stream callbacks [pgrepack.c#_PG_output_plugin_init](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L38-L46), so a transaction over this limit is serialized to `pg_replslot/<slot>/xid-*.spill` [reorderbuffer.c#ReorderBufferCheckMemoryLimit](../../../../raw/postgres-19/src/backend/replication/logical/reorderbuffer.c#L3930-L3968) [reorderbuffer.c#ReorderBufferSerializedPath](../../../../raw/postgres-19/src/backend/replication/logical/reorderbuffer.c#L4957-L4969) — inside `PGDATA`, not in `temp_tablespaces` | `PGC_USERSET`, but the decoding worker is a plain background worker that does not inherit the leader's session values [repack.c#RegisterDynamicBackgroundWorker](../../../../raw/postgres-19/src/backend/commands/repack.c#L3661-L3675) [repack_worker.c#BackgroundWorkerInitializeConnectionByOid](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L104-L106), so set it in `postgresql.conf` (reload) or per database/role [guc_parameters.dat#logical_decoding_work_mem](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L1927-L1935) |
| `statement_timeout`, `lock_timeout` | Bound the damage rather than pace it. The statement timeout is armed once per client statement and REPACK's internal per-table transactions do not reset it [postgres.c#statement-timeout-arming](../../../../raw/postgres-19/src/backend/tcop/postgres.c#L2804-L2811) [postgres.c#enable_statement_timeout](../../../../raw/postgres-19/src/backend/tcop/postgres.c#L5293-L5310) [repack.c#per-table-txn](../../../../raw/postgres-19/src/backend/commands/repack.c#L227-L249) | `PGC_USERSET` -> session/transaction [guc_parameters.dat#statement_timeout](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2892-L2900) [guc_parameters.dat#lock_timeout](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L1609-L1617) |

Settings that look relevant and are not: `vacuum_cost_delay` / `vacuum_cost_limit` and their autovacuum twins (no delay point on this path, above); `maintenance_io_concurrency` (the cluster scan's stream is not a maintenance stream [read_stream.c#max_ios](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c#L795-L834)); and `enable_sort`, which the reference page still recommends for avoiding the sort file [ref/repack.sgml#Notes-on-Resources](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L142-L167) but which no longer changes the decision — see [Open Questions](#open-questions).

### Recipes for a gentler REPACK

- **Do one table, or one partition, at a time.** The bare `REPACK;` form sweeps every table the caller has `MAINTAIN` on, one transaction each [repack.c#per-table-txn](../../../../raw/postgres-19/src/backend/commands/repack.c#L227-L249); naming a single relation lets you space the bursts out yourself, which is the only real pacing mechanism available.
- **Prefer physical order.** `REPACK t` with no `USING INDEX` resolves to no index at all [repack.c#determine_clustered_index](../../../../raw/postgres-19/src/backend/commands/repack.c#L2501-L2541), so it always takes the plain sequential path [repack.c#use_sort-choice](../../../../raw/postgres-19/src/backend/commands/repack.c#L1384-L1395) — the only path with a ring buffer and read-ahead.
- **If you must cluster on an index, force scan-and-sort** with a session-level `enable_indexscan = off`, and pay for it in temp space. The reverse trade — index scan to save temp space — costs you the ring buffer and adds pruning writes to the old heap.
- **Cap the index rebuild's concurrency** with `max_parallel_maintenance_workers = 0` if the parallel builds are what saturate the device.
- **Move temp files off the data device** with `temp_tablespaces`, which covers both the sort file and the `CONCURRENTLY` change spool.
- **Do not reach for `CONCURRENTLY` to reduce I/O.** It protects availability, and it lets old WAL recycle while it runs [repack_worker.c#WAL-recycling](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L374-L403), but it adds decoding, spilling and replay on top of the same rewrite, and it gives up both the buffer-bypassing write path and the TOAST swap-by-content shortcut.

### How to watch it

`pg_stat_progress_repack` reports the phase and the block and tuple counters (see [Progress reporting](#progress-reporting)), and `REPACK (VERBOSE)` prints the chosen method plus removable/nonremovable row counts and a `pg_rusage` line [repack.c#verbose-report](../../../../raw/postgres-19/src/backend/commands/repack.c#L1441-L1453).

`pg_stat_io` has a blind spot here. `IOOP_WRITE` and `IOOP_EXTEND` are only ever counted inside the buffer manager [bufmgr.c#IOOP_EXTEND](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c#L3040-L3046) [bufmgr.c#IOOP_WRITE](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c#L4607-L4626) [localbuf.c#IOOP_WRITE](../../../../raw/postgres-19/src/backend/storage/buffer/localbuf.c#L212-L217), so the blocking rewrite's heap and index writes — which go straight to `smgrextend()` — never appear there. Reads of the old heap do, under context `bulkread` or `normal`, and the `CONCURRENTLY` copy's writes appear under `bulkwrite`.

## Progress reporting

Each backend running `REPACK` reports through `pg_stat_progress_repack`, exposing the command, phase, clustering-index OID, heap tuples scanned/inserted/updated/deleted, block counts, and index-rebuild count [system_views.sql#pg_stat_progress_repack](../../../../raw/postgres-19/src/backend/catalog/system_views.sql#L1349-L1378) [progress.h#PROGRESS_REPACK](../../../../raw/postgres-19/src/include/commands/progress.h#L77-L106). The phases include the concurrent-only `catch-up` and `swapping relation files` steps [progress.h#phases](../../../../raw/postgres-19/src/include/commands/progress.h#L97-L106). The old `pg_stat_progress_cluster` view is preserved as a compatibility wrapper over the same data [system_views.sql#pg_stat_progress_cluster](../../../../raw/postgres-19/src/backend/catalog/system_views.sql#L1380-L1398). While catching up, the leader waits on the `REPACK_WORKER_EXPORT` wait event [wait_event_names.txt#REPACK_WORKER_EXPORT](../../../../raw/postgres-19/src/backend/utils/activity/wait_event_names.txt#L156).

Commit `dc8628e55d4` made the surrounding documentation say the same thing the view does. `pg_stat_progress_repack` is now documented as holding a row for each backend running `REPACK`, `CLUSTER`, **or** `VACUUM FULL` — its `relid` is "the table being processed" and its `command` is one of the three — and the `VACUUM` progress section points at both the new view and the compatibility one [monitoring.sgml#pg_stat_progress_repack](../../../../raw/postgres-19/doc/src/sgml/monitoring.sgml#L7086-L7093) [monitoring.sgml#vacuum-full-progress](../../../../raw/postgres-19/doc/src/sgml/monitoring.sgml#L7344-L7351). The same commit records `REPACK` in the `ACCESS EXCLUSIVE` lock-mode list, noting that `CONCURRENTLY` takes that lock only while swapping the table and index files, and makes the `ctid` note cover any table-rewriting operation rather than `VACUUM FULL` alone [mvcc.sgml#ACCESS-EXCLUSIVE](../../../../raw/postgres-19/doc/src/sgml/mvcc.sgml#L1095-L1109) [ddl.sgml#ctid](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L1566-L1576).

## Tests

Because `REPACK (CONCURRENTLY)` needs `wal_level >= replica`, its functional tests live in `contrib/test_decoding` rather than the core regression suite (commit `4b2aa4b3`). The SQL test covers partition ownership checks, `attmissingval` preservation, the full matrix of rejection cases (partitioned, catalog, TOAST, temp, unlogged, `REPLICA IDENTITY NOTHING`, no PK/RI, deferrable PK), and a check that the `pgrepack` plugin cannot be driven directly through the SQL logical-decoding interface (added by commit `cd7b204b`) [test_decoding/sql/repack.sql](../../../../raw/postgres-19/contrib/test_decoding/sql/repack.sql#L1-L84). That direct-use case still runs `pg_create_logical_replication_slot('s_repack', 'pgrepack')` and still expects the same `ERROR`/`DETAIL` pair, but its expected output lost the `CONTEXT:  slot "s_repack", output plugin "pgrepack", in the startup callback` line, because `5d47df21e89` moved the rejection out of the plugin's startup callback [test_decoding/sql/repack.sql#direct-use](../../../../raw/postgres-19/contrib/test_decoding/sql/repack.sql#L79-L84) [test_decoding/expected/repack.out#direct-use](../../../../raw/postgres-19/contrib/test_decoding/expected/repack.out#L104-L113). The same commit added `output_plugin_libraries` coverage to `test_decoding`'s `permissions` test: with the GUC set to `pgoutput`, both a `REPLICATION` role and the default role get `ERROR: library "test_decoding" may not be used as an output plugin` [permissions.sql#output_plugin_libraries](../../../../raw/postgres-19/contrib/test_decoding/sql/permissions.sql#L18-L24) [permissions.out#output_plugin_libraries](../../../../raw/postgres-19/contrib/test_decoding/expected/permissions.out#L32-L42). Timing-sensitive concurrency (a transaction modifying the table during the rewrite) is driven by injection points in isolation specs `repack`, `repack_toast`, `repack_temporal`, and `repack_temporal_multirange`, hooked at `repack-concurrently-before-lock` [repack.c#injection-point](../../../../raw/postgres-19/src/backend/commands/repack.c#L3264-L3268).

## Feature-Scope Commits

These are the 58 REPACK feature-scope commits in the pinned `REL_19_STABLE` checkout (`135b867a`), grouped by topic. Scope includes commits whose subject/body explicitly references REPACK, prerequisite/support commits cited by that history, and tree-wide fixes that changed REPACK-specific code or documentation. Broad commits that only touch shared infrastructure files are excluded unless their REPACK-specific effect is listed here.

### Foundational

| Commit | Date | Committer | What it does |
|---|---|---|---|
| `ac58465e` | 2026-03-10 | Á. Herrera | **Introduce the REPACK command.** Adds the `REPACK` statement that absorbs `VACUUM FULL` and `CLUSTER` as physical-order vs index-order modes; keeps but de-emphasizes the legacy commands [repack.c#overview](../../../../raw/postgres-19/src/backend/commands/repack.c#L1-L22). |
| `a630ac5c` | 2026-03-12 | Á. Herrera | Document the `command` column of `pg_stat_progress_repack`. |
| `c0b53ec0` | 2026-04-06 | Á. Herrera | **Rename `cluster.c` to `repack.c`** (and the header), since the code now centers on `REPACK`. |
| `28d534e2` | 2026-04-06 | Á. Herrera | **Add the `CONCURRENTLY` option.** Initial copy under `ShareUpdateExclusiveLock` + MVCC snapshot, a temporary slot, a decoding worker that stashes changes, and replay before the final swap. Based on Houska's `pg_squeeze` [repack.c#overview](../../../../raw/postgres-19/src/backend/commands/repack.c#L7-L21). |

### CONCURRENTLY infrastructure

| Commit | Date | Committer | What it does |
|---|---|---|---|
| `33bf7318` | 2026-04-04 | Á. Herrera | Make `index_concurrently_create_copy` more general, so REPACK can reuse index-copy machinery. |
| `caec9d9f` | 2026-04-05 | Á. Herrera | Let `index_create` suppress `index_build` progress reporting (used when rebuilding indexes on the new heap). |
| `e76d8c74` | 2026-04-07 | Á. Herrera | **Add `max_repack_replication_slots`** so REPACK reserves slots from its own pool instead of `max_replication_slots` [slot.c#ReplicationSlotCreate](../../../../raw/postgres-19/src/backend/replication/slot.c#L372-L465). |
| `0d3dba38` | 2026-04-07 | Á. Herrera | Allow logical-replication snapshots to be database-specific (attempt to let multiple REPACKs coexist). |
| `01a80f06` | 2026-05-23 | Á. Herrera | **Revert `0d3dba38`** as fundamentally flawed; restricts REPACK (CONCURRENTLY) to one process at a time instance-wide. |
| `2af1dc89` | 2026-05-27 | Á. Herrera | Disable logical decoding after REPACK so `effective_wal_level` doesn't stay at `logical`; adds a flag to `ReplicationSlotDropAcquired()` [repack_worker.c#cleanup](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L293-L304). |
| `38470c2c` | 2026-05-29 | Á. Herrera | Advance `restart_lsn` more eagerly in `LogicalConfirmReceivedLocation` (supports the WAL-recycling change). |
| `45b02984` | 2026-05-30 | Á. Herrera | **Allow old WAL recycling during REPACK CONCURRENTLY** by moving the slot forward each WAL segment [repack_worker.c#WAL-recycling](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L374-L403). |
| `6a80179f6b0` | 2026-07-30 | M. Sawada | **Re-check the logical-decoding status after the slot exists** so a concurrent deactivation cannot leave an undecodable slot: the recovery path in `EnsureLogicalDecodingEnabled()` raises an error instead of asserting [logicalctl.c#EnsureLogicalDecodingEnabled](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L287-L336), slot synchronization skips persisting such a slot [slotsync.c#update_and_persist_local_synced_slot](../../../../raw/postgres-19/src/backend/replication/logical/slotsync.c#L712-L745), and `ReplicationSlotCreate()` gains a `replication-slot-create-begin` injection point [slot.c:423](../../../../raw/postgres-19/src/backend/replication/slot.c#L423). |

### Correctness fixes

| Commit | Date | Committer | What it does |
|---|---|---|---|
| `5dbb63fc` | 2026-04-20 | Á. Herrera | REPACK no longer requires the `REPLICATION` or `LOGIN` role attributes; the worker connects with `BGWORKER_BYPASS_ROLELOGINCHECK` [repack_worker.c#connect](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L104-L106). |
| `832e220d` | 2026-04-27 | Á. Herrera | Don't allow **deferrable primary keys** as the identity index (a modified tuple may not yet be indexed) [repack.c#deferrable-pk](../../../../raw/postgres-19/src/backend/commands/repack.c#L926-L953). |
| `6ca631b9` | 2026-04-28 | Á. Herrera | Fix processing of **toasted tuples** in the change spill/replay path. |
| `b5f92b8e` | 2026-05-04 | Á. Herrera | Fix an off-by-one in the repack index loop (from `28d534e2`). |
| `eb2e2eb4` | 2026-05-05 | Á. Herrera | **Don't lose `attmissingval` columns**: the tuple-reform fast path discarded fast-default values during any VACUUM FULL/CLUSTER/REPACK. |
| `a0a0c0c2` | 2026-05-05 | Á. Herrera | Skip **other sessions' temp tables** in the no-table forms of REPACK/CLUSTER/VACUUM FULL to avoid blocking on them [repack.c#skip-temp](../../../../raw/postgres-19/src/backend/commands/repack.c#L2145-L2155). |
| `36f52a59` | 2026-05-11 | Á. Herrera | Fix REPACK with **`WITHOUT OVERLAPS`** (GiST) replica-identity indexes: derive the equality strategy from the opfamily instead of hard-coding btree [repack.c#eq-strategy](../../../../raw/postgres-19/src/backend/commands/repack.c#L3137-L3164). |
| `a120ecf5` | 2026-05-18 | Fujii Masao | Fix REPACK **option parsing**: `CONCURRENTLY OFF` was rejected, and repeated options weren't last-wins. |
| `0160143a` | 2026-05-19 | Á. Herrera | Fix the decoding worker not being cleaned up on **`FATAL` exit** of the leader. |
| `1588d89a` | 2026-05-26 | Á. Herrera | Restructure worker teardown so the DSM segment is always freed even if init fails partway [repack.c#stop_repack_decoding_worker](../../../../raw/postgres-19/src/backend/commands/repack.c#L3711-L3753). |
| `5bcc3fbd` | 2026-04-07 | Á. Herrera | Fix a valgrind failure. |
| `a3b069ef` | 2026-04-07 | Á. Herrera | Avoid a different-size pointer-to-integer cast. |
| `2cff3637` | 2026-04-08 | Á. Herrera | Simplify a `memcpy` target declaration. |
| `2fd84e22` | 2026-04-16 | Fujii Masao | Use `XLogRecPtrIsValid()` consistently for WAL-position checks. |
| `05c401d5` | 2026-04-16 | Á. Herrera | Add a missing initialization. |
| `5d48d3b1` | 2026-05-29 | Á. Herrera | Remove an unnecessary signal-handler change (bgworkers already use `die()`). |
| `cd7b204b` | 2026-06-09 | Á. Herrera | **Disallow direct use of the `pgrepack` plugin.** Reject driving the output plugin outside `REPACK (CONCURRENTLY)` (direct use caused assertion failures and production crashes from bogus memory lifetime); also moves `output_writer_private` allocation into `repack_startup()`, always sets `->relid`, and renames the worker's plugin-name macro `REPL_PLUGIN_NAME` to `PGREPACK_PLUGIN` [pgrepack.c#repack_startup](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L50-L80). |
| `e2a8cabc` | 2026-06-16 | Á. Herrera | **Check leftover separately stored TOAST attributes during concurrent REPACK replay.** `restore_tuple()` now errors if the spill file contains too few or too many separately stored varlena attributes after reconstructing a tuple [repack.c#restore_tuple](../../../../raw/postgres-19/src/backend/commands/repack.c#L2785-L2852). |
| `fb284f2f9bd` | 2026-07-03 | Á. Herrera | **Fix REPACK CONCURRENTLY for stored generated columns** (backpatch-through 19). Concurrent replay must recompute a generated column when a tuple is updated/inserted during the rewrite, which needs the transient table's `pg_attrdef` rows; `rebuild_relation` now copies the attribute defaults onto the new heap in concurrent mode via the new `copy_attribute_defaults()` [repack.c#copy-attrdefs-call](../../../../raw/postgres-19/src/backend/commands/repack.c#L1056-L1062) [repack.c#copy_attribute_defaults](../../../../raw/postgres-19/src/backend/commands/repack.c#L3534-L3614). |
| `5e450df50dc` | 2026-07-03 | Á. Herrera | **Initialize the change-replay range table more honestly** (backpatch-through 19). `initialize_change_context` now builds a one-entry range table whose `updatedCols` bitmapset marks every non-dropped column updated, installed via `ExecInitRangeTable`, so `ExecInsertIndexTuples` errs toward flagging index updates; this only affects btree unchanged-column optimizations and does not compromise correctness [repack.c#initialize_change_context](../../../../raw/postgres-19/src/backend/commands/repack.c#L3039-L3178). |
| `133eba078f7` | 2026-07-10 | Á. Herrera | **Do not lock candidate tables while building a whole-database REPACK list.** The old transaction-scoped discovery locks ended at the database commit immediately after list construction and did not protect later processing. The final per-table transaction now uses `try_relation_open()`, skips a dropped target or an OID that now identifies a non-table relkind, and rechecks it while holding the real operation lock; discovery-time permission checks also tolerate concurrent drops [repack.c#per-table-open](../../../../raw/postgres-19/src/backend/commands/repack.c#L406-L439) [repack.c#get_tables_to_repack](../../../../raw/postgres-19/src/backend/commands/repack.c#L2110-L2213) [repack.c#repack_is_permitted_for_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L2344-L2395). |
| `69fe2514fdd` | 2026-07-20 | Á. Herrera | **Contain partitioned-target discovery and tolerate a vanished leaf index.** `get_tables_to_repack_partitioned()` now owns `USING INDEX` resolution and parent close/unlock; `IndexGetRelation(..., true)` lets it skip an index whose table mapping vanished before the unlocked leaf list was built [repack.c#get_tables_to_repack_partitioned](../../../../raw/postgres-19/src/backend/commands/repack.c#L2215-L2341). |
| `99e47536bbf` | 2026-07-27 | M. Sawada; patch by S. R. Sadipiralla | **Prevent redundant logical-decoding activation WAL under concurrent slot creation.** A REPACK worker that loses the enablement race now observes the winner after the barrier and returns instead of writing a second status-change record that a newly created slot could try to decode [logicalctl.c#EnableLogicalDecoding](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L342-L426) [051_effective_wal_level.pl#concurrent-activation](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L385-L426). |
| `5d47df21e89` | 2026-08-10 | N. Misch; patch by J. Champion | **CVE-2026-6471: add the `output_plugin_libraries` GUC** and move the `pgrepack` direct-use rejection into `StartupDecodingContext()`, ahead of loading the plugin library [logical.c#StartupDecodingContext-plugin-check](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L179-L259) [guc_parameters.dat#output_plugin_libraries](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2311-L2318). |
| `d7db169fa13` | 2026-08-13 | N. Bossart; patch by Z. Parragi | **Reject `CLUSTER (ANALYZE)`**, which `ac58465e06` had enabled unintentionally; `ExecRepack()` now accepts `ANALYZE` only for `REPACK` itself [repack.c#ANALYZE-option](../../../../raw/postgres-19/src/backend/commands/repack.c#L267-L276). |
| `9bb8e16bd53` | 2026-08-19 | Á. Herrera; patch by B. Rupireddy | **Restrict the tolerate-a-dropped-relation ACL path to the unlocked discovery callers.** `repack_is_permitted_for_relation()` takes an `already_locked` argument: the two discovery loops pass `false` and keep skipping a vanished OID, while every locked caller passes `true` and gets assertions that the relation is locked and cannot go missing [repack.c#repack_is_permitted_for_relation](../../../../raw/postgres-19/src/backend/commands/repack.c#L2344-L2395) [repack.c#cluster_rel_recheck-permission](../../../../raw/postgres-19/src/backend/commands/repack.c#L682-L689). |
| `f23de46e15b` | 2026-08-29 | N. Bossart | **Disallow `ONLY` and `*` in REPACK.** The production reused `vacuum_relation`, so the parser accepted an inheritance qualifier that `repack.c` never read; it now takes `qualified_name opt_name_list` and matches the documented synopsis [gram.y#RepackStmt](../../../../raw/postgres-19/src/backend/parser/gram.y#L12534-L12627) [ref/repack.sgml#synopsis](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L22-L36). |

### Error messages, docs, style

| Commit | Date | Committer | What it does |
|---|---|---|---|
| `8fb95a8a` | 2026-04-07 | Á. Herrera | doc: add a `REPACK (CONCURRENTLY)` example [ref/repack.sgml#example](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L400-L405). |
| `d3bba041` | 2026-04-21 | M. Paquier | Tree-wide typo/grammar fixes touching REPACK text. |
| `d14f69a3` | 2026-04-22 | P. Geoghegan | Harmonize function parameter names across v19, including repack. |
| `3bf63730` | 2026-05-13 | Á. Herrera | Fix style in a few REPACK `ereport`s. |
| `43649b6a` | 2026-05-28 | Á. Herrera | Add an upfront, REPACK-specific error when `wal_level < replica` instead of the cryptic slot-requirement message [repack.c#wal_level-check](../../../../raw/postgres-19/src/backend/commands/repack.c#L872-L878). |
| `497e92dc` | 2026-05-28 | Á. Herrera | Fix minor issues in repack `ereport()`s. |
| `378dffaf` | 2026-05-28 | Á. Herrera | Improve REPACK (CONCURRENTLY) error messages further. |
| `da8889ccd7e` | 2026-07-06 | R. Haas | Tree-wide (backpatch-through 19): use `PG_MODULE_MAGIC_EXT` (carrying the module name and version) instead of plain `PG_MODULE_MAGIC` in the newly added modules, including the `pgrepack` output plugin [pgrepack.c#module-magic](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L21-L24). |
| `8a84ddd8c63` | 2026-07-16 | Fujii Masao; patch by Shinya Kato | Add `REPACK` to the generic `MAINTAIN` privilege description and to the predefined `pg_maintain` role's command list; the runtime `ACL_MAINTAIN` membership path already covered the command [ddl.sgml#MAINTAIN](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L2544-L2556) [user-manag.sgml#pg_maintain](../../../../raw/postgres-19/doc/src/sgml/user-manag.sgml#L655-L670) [aclchk.c#pg_class_aclmask_ext](../../../../raw/postgres-19/src/backend/catalog/aclchk.c#L3437-L3446). |
| `10389a7e15e` | 2026-07-20 | Á. Herrera; patch by kenny | Fix the REPACK worker's confirmed-LSN `DEBUG1` message to use `%X/%08X` and `LSN_FORMAT_ARGS()` [repack_worker.c#confirmed-LSN-debug](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L394-L402). |
| `fd56954c9fe` | 2026-08-12 | Á. Herrera; patch by N. Sontakke | Document `TABLE_DELETE_NO_LOGICAL` and `TABLE_UPDATE_NO_LOGICAL` (added by `28d534e2`) in `tableam.h`, and drop the stale `pg_attribute_unused()` on `heap_update()`'s `options` argument [tableam.h#table_tuple_delete-options](../../../../raw/postgres-19/src/include/access/tableam.h#L1531-L1537) [tableam.h#table_tuple_update-options](../../../../raw/postgres-19/src/include/access/tableam.h#L1573-L1577) [heapam.c#heap_update](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L3266-L3270). |
| `dc8628e55d4` | 2026-08-26 | Fujii Masao; patch by M. Pegoraro | Align the surrounding documentation with REPACK: `ctid` changes under any table-rewriting operation, `VACUUM FULL`/`CLUSTER`/`REPACK` are described together as space-reclaiming rewrites, `pg_stat_progress_repack` is documented as covering all three commands, and `REPACK` is named in the `ACCESS EXCLUSIVE` lock list with its `CONCURRENTLY` exception [ddl.sgml#ctid](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L1566-L1576) [maintenance.sgml#table-rewriting-commands](../../../../raw/postgres-19/doc/src/sgml/maintenance.sgml#L168-L181) [monitoring.sgml#pg_stat_progress_repack](../../../../raw/postgres-19/doc/src/sgml/monitoring.sgml#L7086-L7093) [mvcc.sgml#ACCESS-EXCLUSIVE](../../../../raw/postgres-19/doc/src/sgml/mvcc.sgml#L1095-L1109). |

### Tests

| Commit | Date | Committer | What it does |
|---|---|---|---|
| `be142fa0` | 2026-04-07 | Á. Herrera | Fix tests under `wal_level=minimal`. |
| `4b2aa4b3` | 2026-04-23 | Á. Herrera | Move the REPACK (CONCURRENTLY) test out of the stock regression suite into `test_decoding` [test_decoding/sql/repack.sql](../../../../raw/postgres-19/contrib/test_decoding/sql/repack.sql#L1-L6). |
| `e5035950` | 2026-05-15 | Fujii Masao | psql: fix tab completion for REPACK boolean options. |
| `47ad2233` | 2026-05-27 | M. Sawada | Fix `051_effective_wal_level.pl` on builds without injection points. |
| `2670cc29` | 2026-05-29 | Á. Herrera | Cover additional error cases and corner conditions in `repack.c`; move some cases to `test_decoding`. |
| `5c808b8c5c3` | 2026-07-30 | M. Sawada | Run the two logical-decoding-activation background `psql` sessions in `051_effective_wal_level.pl` with `on_error_stop => 0` and drop the in-script `\q`, so `quit()` no longer writes to a closed pipe after the cancellation error [051_effective_wal_level.pl:442](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L442) [051_effective_wal_level.pl:474](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L474). |

## Context Reviewed

- `ref/repack.sgml` — command reference, modes, CONCURRENTLY semantics, restrictions, resource notes [ref/repack.sgml](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L1-L452).
- `ddl.sgml`, `user-manag.sgml`, and `aclchk.c` — generic `MAINTAIN` documentation, predefined `pg_maintain`, and its runtime `ACL_MAINTAIN` mapping [ddl.sgml#MAINTAIN](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L2544-L2556) [user-manag.sgml#pg_maintain](../../../../raw/postgres-19/doc/src/sgml/user-manag.sgml#L655-L670) [aclchk.c#pg_class_aclmask_ext](../../../../raw/postgres-19/src/backend/catalog/aclchk.c#L3437-L3446).
- `repack.c` — `ExecRepack`, `cluster_rel`, `rebuild_relation`, `copy_table_data`, concurrent finish/apply, worker control [repack.c](../../../../raw/postgres-19/src/backend/commands/repack.c#L1-L32).
- `repack_worker.c` — worker main loop, decoding setup, WAL recycling, change filtering [repack_worker.c](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L1-L536).
- `pgrepack.c` — output-plugin callbacks and tuple spill format [pgrepack.c](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L1-L306).
- `logicalctl.c`, `decode.c`, `slotsync.c`, and `051_effective_wal_level.pl` — on-demand logical-decoding activation, the decoded status-change error boundary, the slot-sync skip path, and concurrent activation/deactivation coverage [logicalctl.c#EnableLogicalDecoding](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L288-L426) [decode.c#xlog_decode](../../../../raw/postgres-19/src/backend/replication/logical/decode.c#L153-L172) [slotsync.c#update_and_persist_local_synced_slot](../../../../raw/postgres-19/src/backend/replication/logical/slotsync.c#L712-L745) [051_effective_wal_level.pl#concurrent-activation](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L385-L426).
- `logical.c` and `tableam.h` — the output-plugin gate that rejects direct `pgrepack` use and enforces `output_plugin_libraries`, and the documented `TABLE_UPDATE_NO_LOGICAL` / `TABLE_DELETE_NO_LOGICAL` options REPACK's replay passes [logical.c#StartupDecodingContext-plugin-check](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L179-L259) [tableam.h#table_tuple_update-options](../../../../raw/postgres-19/src/include/access/tableam.h#L1573-L1577).
- `repack.h` / `repack_internal.h` — API, `CLUOPT_*`, `DecodingWorkerShared`, change kinds [repack_internal.h](../../../../raw/postgres-19/src/include/commands/repack_internal.h#L1-L122).
- Grammar/parse/dispatch — `parsenodes.h`, `gram.y`, `utility.c`, `vacuum.c` [gram.y#RepackStmt](../../../../raw/postgres-19/src/backend/parser/gram.y#L12534-L12627).
- GUC/slot pool — `guc_parameters.dat`, `slot.c` [slot.c#ReplicationSlotCreate](../../../../raw/postgres-19/src/backend/replication/slot.c#L372-L465).
- Observability — `system_views.sql`, `progress.h`, `wait_event_names.txt`.
- Tests — `contrib/test_decoding/sql/repack.sql`, `src/test/modules/injection_points` repack specs.
- I/O path, for the 2026-09-02 follow-up — `rewriteheap.c` and `bulk_write.c` (buffer-manager-bypassing writes, batched newpage WAL, `RelationNeedsWAL`), `heapam.c`/`freelist.c`/`read_stream.c`/`tableam.h` (BULKREAD ring sizing, syncscan, the sequential read stream and its `effective_io_concurrency` source), `heapam_indexscan.c` and `pruneheap.c` (the un-strategised index-scan fetch and on-access pruning), `toast_internals.c` (TOAST chunk inserts), `nbtsort.c` and `catalog/index.c` (index rebuild writes and parallel worker request), `planner.c` (`plan_cluster_use_sort`, `plan_create_index_workers`), `costsize.c` (`cost_sort` and `disabled_nodes`), `storage.c` (`wal_skip_threshold`), `fileset.c` and `reorderbuffer.c` (the two spill locations), `bufmgr.c`/`localbuf.c` (where `pg_stat_io` counts writes), `autovacuum.c` (no `VACOPT_FULL`), and `postgres.c` (statement-timeout arming) [rewriteheap.c#raw_heap_insert](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L596-L680) [bulk_write.c#overview](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L6-L25) [freelist.c#GetAccessStrategy](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c#L425-L500) [heapam_indexscan.c#heapam_index_fetch_tuple](../../../../raw/postgres-19/src/backend/access/heap/heapam_indexscan.c#L231-L263) [planner.c#plan_cluster_use_sort](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7077-L7180) [costsize.c#cost_sort](../../../../raw/postgres-19/src/backend/optimizer/path/costsize.c#L2200-L2226).
- `git log --regexp-ignore-case --grep=repack` plus source-path and adjacent logical-decoding history, checked against the then-pinned `REL_19_STABLE` checkout `99e47536bbf1a165f5dc8d504f928821ebc8df6a` for the 50 feature-scope commits counted at that pin. The 2026-07-09 repin from `cdae794a` (the former `master` post-`REL_19_BETA1` pin, now the point where `REL_19_STABLE` diverged from `master`/20devel) added three REPACK feature-scope commits in `cdae794a..01c544e1`: `fb284f2f9bd` (stored generated columns) and `5e450df50dc` (change-replay range table), both backpatched through 19, plus the tree-wide `da8889ccd7e` (`PG_MODULE_MAGIC_EXT`). The 2026-07-13 repin reviewed all 12 commits in `01c544e1..8055e337`; only `133eba078f7` changed REPACK, raising the count from 45 to 46. The 31-commit `8055e337..3aa54433` range changed no REPACK implementation, worker, output-plugin, header, direct test, or command-reference file. Its one REPACK-scoped commit is `8a84ddd8c63`, which adds REPACK to generic `MAINTAIN` and `pg_maintain` documentation and raises the count from 46 to 47. The 52-commit `3aa54433..99e47536` range adds `69fe2514fdd` (partitioned-target discovery), `10389a7e15e` (LSN debug formatting), and `99e47536bbf` (shared logical-decoding activation race), raising the count to 50. The other 49 commits in that range do not change REPACK-specific source, tests, documentation, or its adjacent logical-decoding behavior. The 2026-08-17 repin reviewed all 152 commits in `99e47536..67342a148` and adds five: `6a80179f6b0` (deactivation-versus-slot-creation races), `5c808b8c5c3` (background-`psql` cleanup in the same TAP test), `5d47df21e89` (`output_plugin_libraries` plus the relocated `pgrepack` rejection), `fd56954c9fe` (`tableam.h` `NO_LOGICAL` documentation), and `d7db169fa13` (reject `CLUSTER (ANALYZE)`), raising the count from 50 to 55. The other 147 commits change no REPACK implementation, worker, output-plugin, header, direct test, or command-reference file; the only other commit touching a shared helper REPACK calls is `9f6fb119191`, which makes `index_create_copy()` propagate a deferrable source index's `indimmediate = false` and adds an unrelated `REINDEX CONCURRENTLY` isolation spec, so it stays outside this page's scope [repack.c#build_new_indexes](../../../../raw/postgres-19/src/backend/commands/repack.c#L3417-L3447).
- 2026-09-01 repin, range `67342a148..135b867a530`: reviewed all 104 commits and changed paths. Three are REPACK feature-scope and raise the count from 55 to 58 — `9bb8e16bd53` (the `already_locked` ACL argument, the only change to `repack.c` in the range), `dc8628e55d4` (the `ctid`/rewrite/progress/lock documentation alignment across `ddl.sgml`, `maintenance.sgml`, `monitoring.sgml`, and `mvcc.sgml`), and `f23de46e15b` (`ONLY`/`*` removed from the `RepackStmt` production). `repack_worker.c`, `pgrepack.c`, `repack.h`, `repack_internal.h`, `ref/repack.sgml`, `logicalctl.c`, `logical.c`, `slot.c`, and the `test_decoding` repack test are byte-identical across the range. `ec51b80b348` adds an injection-point spec for an unrelated RI fast-path race, and `3e8bcc8644f` (revert of `ALTER TABLE ... MERGE/SPLIT PARTITION(S)`) is what moved the `gram.y` anchors by 52 lines without touching `RepackStmt` itself. No other commit changes REPACK implementation, worker, output-plugin, header, direct test, or command-reference text.

## Evidence Map

| Claim | Source |
|---|---|
| REPACK absorbs VACUUM FULL + CLUSTER | [repack.c#L1-L22](../../../../raw/postgres-19/src/backend/commands/repack.c#L1-L22), `ac58465e` |
| `MAINTAIN` authorizes REPACK; `pg_maintain` supplies it on all relations | [ddl.sgml#L2544-L2556](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L2544-L2556) [user-manag.sgml#L655-L670](../../../../raw/postgres-19/doc/src/sgml/user-manag.sgml#L655-L670) [aclchk.c#L3437-L3446](../../../../raw/postgres-19/src/backend/catalog/aclchk.c#L3437-L3446), `8a84ddd8c63` |
| Two lock levels: AEL vs SUEL | [repack.c#L456-L463](../../../../raw/postgres-19/src/backend/commands/repack.c#L456-L463) |
| Multi-table candidates are collected unlocked, then opened and rechecked under the operation lock | [repack.c#L375-L439](../../../../raw/postgres-19/src/backend/commands/repack.c#L375-L439) [repack.c#L672-L726](../../../../raw/postgres-19/src/backend/commands/repack.c#L672-L726) [repack.c#L2110-L2405](../../../../raw/postgres-19/src/backend/commands/repack.c#L2110-L2405), `133eba078f7` |
| Partitioned discovery owns parent close/unlock and skips a vanished leaf-index mapping | [repack.c#L2215-L2341](../../../../raw/postgres-19/src/backend/commands/repack.c#L2215-L2341), `69fe2514fdd` |
| Only the unlocked discovery callers tolerate a dropped relation; locked callers pass `already_locked` and assert | [repack.c#L2344-L2395](../../../../raw/postgres-19/src/backend/commands/repack.c#L2344-L2395) [repack.c#L682-L689](../../../../raw/postgres-19/src/backend/commands/repack.c#L682-L689), `9bb8e16bd53` |
| `REPACK ONLY t` and `REPACK t *` are syntax errors; `VACUUM (FULL) ONLY t` is not | [gram.y#L12534-L12627](../../../../raw/postgres-19/src/backend/parser/gram.y#L12534-L12627) [gram.y#L12727-L12732](../../../../raw/postgres-19/src/backend/parser/gram.y#L12727-L12732) [ref/repack.sgml#L22-L36](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L22-L36), `f23de46e15b` |
| Docs cover all three rewrite commands together: progress views, the AEL list with the CONCURRENTLY exception, and `ctid` | [monitoring.sgml#L7086-L7093](../../../../raw/postgres-19/doc/src/sgml/monitoring.sgml#L7086-L7093) [mvcc.sgml#L1095-L1109](../../../../raw/postgres-19/doc/src/sgml/mvcc.sgml#L1095-L1109) [ddl.sgml#L1566-L1576](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml#L1566-L1576), `dc8628e55d4` |
| CONCURRENTLY preconditions / identity index | [repack.c#L865-L956](../../../../raw/postgres-19/src/backend/commands/repack.c#L865-L956) |
| Worker starts before XID; one initial snapshot | [repack.c#L1001-L1073](../../../../raw/postgres-19/src/backend/commands/repack.c#L1001-L1073) |
| Decoding setup, temp slot, target-only filter | [repack_worker.c#L194-L291](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L194-L291) |
| Change spill format | [pgrepack.c#L191-L306](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L191-L306) |
| Replay with NO_LOGICAL + identity-index lookup | [repack.c#L2562-L2773](../../../../raw/postgres-19/src/backend/commands/repack.c#L2562-L2773) |
| Spilled tuple reconstruction validates separately stored attributes | [repack.c#L2785-L2852](../../../../raw/postgres-19/src/backend/commands/repack.c#L2785-L2852) |
| Lock upgrade + double catch-up + swap | [repack.c#L3264-L3404](../../../../raw/postgres-19/src/backend/commands/repack.c#L3264-L3404) |
| WAL recycling during REPACK | [repack_worker.c#L374-L403](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L374-L403), `45b02984` |
| Confirmed-LSN debug output uses the standard padded format | [repack_worker.c#L394-L402](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L394-L402), `10389a7e15e` |
| Concurrent logical-decoding activation writes only one status-change record | [repack_worker.c#L210-L224](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L210-L224) [logicalctl.c#L342-L426](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L342-L426) [051_effective_wal_level.pl#L385-L426](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L385-L426), `99e47536bbf` |
| A concurrent deactivation is caught after slot creation: recovery-only error, slot sync skips persisting (`6a80179f6b0`) | [logicalctl.c#L287-L336](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c#L287-L336) [slotsync.c#L712-L745](../../../../raw/postgres-19/src/backend/replication/logical/slotsync.c#L712-L745) [051_effective_wal_level.pl#L515-L671](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl#L515-L671) |
| Direct `pgrepack` use is rejected before the library loads; other output plugins need `output_plugin_libraries` (`PGC_SUSET`) (`5d47df21e89`) | [logical.c#L179-L259](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L179-L259) [logical.c#L188-L203](../../../../raw/postgres-19/src/backend/replication/logical/logical.c#L188-L203) [pgrepack.c#L50-L80](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L50-L80) [guc_parameters.dat#L2311-L2318](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2311-L2318) |
| `CLUSTER (ANALYZE)` is rejected; `VACUUM (FULL, ANALYZE)` is unaffected (`d7db169fa13`) | [repack.c#L267-L276](../../../../raw/postgres-19/src/backend/commands/repack.c#L267-L276) [vacuum.c#L2302-L2305](../../../../raw/postgres-19/src/backend/commands/vacuum.c#L2302-L2305) |
| `max_repack_replication_slots` = PGC_POSTMASTER | [guc_parameters.dat#L2108-L2114](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat#L2108-L2114) |
| Progress view + phases | [system_views.sql#L1349-L1398](../../../../raw/postgres-19/src/backend/catalog/system_views.sql#L1349-L1398), [progress.h#L77-L106](../../../../raw/postgres-19/src/include/commands/progress.h#L77-L106) |
| Tests live in test_decoding (wal_level) | [repack.sql#L1-L6](../../../../raw/postgres-19/contrib/test_decoding/sql/repack.sql#L1-L6), `4b2aa4b3` |
| No cost-delay throttle in the rewrite path; `vacuum_delay_point()` lives only on the VACUUM/ANALYZE/index-vacuum paths | [vacuum.h:390](../../../../raw/postgres-19/src/include/commands/vacuum.h#L390) [vacuumlazy.c:1333](../../../../raw/postgres-19/src/backend/access/heap/vacuumlazy.c#L1333) [repack.c#L489-L670](../../../../raw/postgres-19/src/backend/commands/repack.c#L489-L670) [heapam_handler.c#L593-L962](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L593-L962) [rewriteheap.c#L596-L680](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L596-L680) |
| Only three REPACK options, none about I/O; no autovacuum caller | [repack.c#L262-L297](../../../../raw/postgres-19/src/backend/commands/repack.c#L262-L297) [autovacuum.c#L2913-L2923](../../../../raw/postgres-19/src/backend/postmaster/autovacuum.c#L2913-L2923) |
| Blocking rewrite writes bypass shared buffers; WAL as 32-page batched FPIs; no WAL at all under `wal_level = minimal` | [rewriteheap.c#L262-L265](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L262-L265) [bulk_write.c#L6-L25](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L6-L25) [bulk_write.c#L253-L309](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c#L253-L309) [xlogrecord.h:241](../../../../raw/postgres-19/src/include/access/xlogrecord.h#L241) [rel.h#L631-L642](../../../../raw/postgres-19/src/include/utils/rel.h#L631-L642) [storage.c#L787-L826](../../../../raw/postgres-19/src/backend/catalog/storage.c#L787-L826) |
| Large seqscan reads via BULKREAD ring plus syncscan; ring size scales with `io_combine_limit * effective_io_concurrency` | [heapam.c#L385-L411](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L385-L411) [tableam.h#L942-L951](../../../../raw/postgres-19/src/include/access/tableam.h#L942-L951) [freelist.c#L436-L541](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c#L436-L541) [heapam.c#L1275-L1305](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L1275-L1305) [read_stream.c#L795-L834](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c#L795-L834) |
| The blocking copy dirties the old heap with hint bits; the concurrent copy prunes it instead | [heapam_handler.c#L778-L797](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c#L778-L797) [heapam.c#L626-L640](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L626-L640) [heapam.c#L1209-L1213](../../../../raw/postgres-19/src/backend/access/heap/heapam.c#L1209-L1213) [pruneheap.c#L272-L337](../../../../raw/postgres-19/src/backend/access/heap/pruneheap.c#L272-L337) |
| Index-scan copy path uses a bare `ReadBuffer()`: no strategy, no read stream | [heapam_indexscan.c#L244-L263](../../../../raw/postgres-19/src/backend/access/heap/heapam_indexscan.c#L244-L263) |
| TOAST is rewritten in both modes; chunks are inserted with no bulk-insert state | [rewriteheap.c#L606-L634](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c#L606-L634) [toast_internals.c#L300-L315](../../../../raw/postgres-19/src/backend/access/common/toast_internals.c#L300-L315) [repack.c#L1314-L1349](../../../../raw/postgres-19/src/backend/commands/repack.c#L1314-L1349) |
| `enable_indexscan = off` forces scan-and-sort; index rebuilds honour `max_parallel_maintenance_workers` | [planner.c#L7093-L7095](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7093-L7095) [index.c#L3856-L3862](../../../../raw/postgres-19/src/backend/catalog/index.c#L3856-L3862) [planner.c#L7214-L7219](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7214-L7219) |
| CONCURRENTLY spills changes to `temp_tablespaces` and, past `logical_decoding_work_mem`, to `pg_replslot` | [repack_worker.c#L340-L358](../../../../raw/postgres-19/src/backend/commands/repack_worker.c#L340-L358) [fileset.c#L38-L66](../../../../raw/postgres-19/src/backend/storage/file/fileset.c#L38-L66) [pgrepack.c#L38-L46](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c#L38-L46) [reorderbuffer.c#L3930-L3968](../../../../raw/postgres-19/src/backend/replication/logical/reorderbuffer.c#L3930-L3968) [reorderbuffer.c#L4957-L4969](../../../../raw/postgres-19/src/backend/replication/logical/reorderbuffer.c#L4957-L4969) |
| `pg_stat_io` cannot see the blocking rewrite's writes | [bufmgr.c#L3040-L3046](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c#L3040-L3046) [bufmgr.c#L4607-L4626](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c#L4607-L4626) [localbuf.c#L212-L217](../../../../raw/postgres-19/src/backend/storage/buffer/localbuf.c#L212-L217) |

## Source References

- [repack.c](../../../../raw/postgres-19/src/backend/commands/repack.c) — `ExecRepack`, `cluster_rel`, `rebuild_relation`, `copy_table_data`, concurrent finish/apply, worker control (formerly `cluster.c`).
- [repack_worker.c](../../../../raw/postgres-19/src/backend/commands/repack_worker.c) — decoding background worker: setup, decode loop, WAL recycling, change filtering.
- [pgrepack.c](../../../../raw/postgres-19/src/backend/replication/pgrepack/pgrepack.c) — `pgrepack` logical-decoding output plugin and tuple spill format.
- [logicalctl.c](../../../../raw/postgres-19/src/backend/replication/logical/logicalctl.c) / [decode.c](../../../../raw/postgres-19/src/backend/replication/logical/decode.c) / [slotsync.c](../../../../raw/postgres-19/src/backend/replication/logical/slotsync.c) / [051_effective_wal_level.pl](../../../../raw/postgres-19/src/test/recovery/t/051_effective_wal_level.pl) — on-demand logical-decoding activation, its decoder error boundary, the slot-sync skip on concurrent deactivation, and the activation/deactivation race tests around REPACK's temporary slot.
- [logical.c](../../../../raw/postgres-19/src/backend/replication/logical/logical.c) / [tableam.h](../../../../raw/postgres-19/src/include/access/tableam.h) — `StartupDecodingContext()`'s output-plugin gate (`pgrepack` special case plus `output_plugin_libraries`) and the table-AM `NO_LOGICAL` update/delete options used by concurrent replay.
- [repack.h](../../../../raw/postgres-19/src/include/commands/repack.h) / [repack_internal.h](../../../../raw/postgres-19/src/include/commands/repack_internal.h) — public API, `CLUOPT_*`, `ConcurrentChangeKind`, `RepackDecodingState`, `DecodingWorkerShared`.
- [ref/repack.sgml](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml) — `REPACK` reference page.
- [ddl.sgml](../../../../raw/postgres-19/doc/src/sgml/ddl.sgml) / [user-manag.sgml](../../../../raw/postgres-19/doc/src/sgml/user-manag.sgml) / [aclchk.c](../../../../raw/postgres-19/src/backend/catalog/aclchk.c) — generic `MAINTAIN` documentation, predefined `pg_maintain`, and runtime role-to-ACL mapping.
- [parsenodes.h](../../../../raw/postgres-19/src/include/nodes/parsenodes.h) / [gram.y](../../../../raw/postgres-19/src/backend/parser/gram.y) / [utility.c](../../../../raw/postgres-19/src/backend/tcop/utility.c) / [vacuum.c](../../../../raw/postgres-19/src/backend/commands/vacuum.c) — `RepackStmt`/`RepackCommand`, grammar, dispatch, and `VACUUM FULL` routing.
- [slot.c](../../../../raw/postgres-19/src/backend/replication/slot.c) / [guc_parameters.dat](../../../../raw/postgres-19/src/backend/utils/misc/guc_parameters.dat) — `max_repack_replication_slots`, `maintenance_work_mem`, `enable_indexscan`, and the slot pool.
- [heapam_handler.c](../../../../raw/postgres-19/src/backend/access/heap/heapam_handler.c) / [heapam.c](../../../../raw/postgres-19/src/backend/access/heap/heapam.c) / [tableam.h](../../../../raw/postgres-19/src/include/access/tableam.h) — the table-AM heap copy, sort sizing, and the BULKREAD/BULKWRITE ring-buffer behavior relevant to I/O impact.
- [system_views.sql](../../../../raw/postgres-19/src/backend/catalog/system_views.sql) / [progress.h](../../../../raw/postgres-19/src/include/commands/progress.h) / [wait_event_names.txt](../../../../raw/postgres-19/src/backend/utils/activity/wait_event_names.txt) — `pg_stat_progress_repack`, phases, wait event.
- [test_decoding/sql/repack.sql](../../../../raw/postgres-19/contrib/test_decoding/sql/repack.sql) — functional and rejection-case tests.
- [rewriteheap.c](../../../../raw/postgres-19/src/backend/access/heap/rewriteheap.c) / [bulk_write.c](../../../../raw/postgres-19/src/backend/storage/smgr/bulk_write.c) / [nbtsort.c](../../../../raw/postgres-19/src/backend/access/nbtree/nbtsort.c) — the buffer-manager-bypassing write path used by the blocking heap rewrite and by btree index builds, its batched newpage WAL, and its fsync registration.
- [heapam_indexscan.c](../../../../raw/postgres-19/src/backend/access/heap/heapam_indexscan.c) / [pruneheap.c](../../../../raw/postgres-19/src/backend/access/heap/pruneheap.c) / [freelist.c](../../../../raw/postgres-19/src/backend/storage/buffer/freelist.c) / [read_stream.c](../../../../raw/postgres-19/src/backend/storage/aio/read_stream.c) — the read side: un-strategised index fetches, on-access pruning, ring-buffer sizing, and read-stream look-ahead.
- [toast_internals.c](../../../../raw/postgres-19/src/backend/access/common/toast_internals.c) / [storage.c](../../../../raw/postgres-19/src/backend/catalog/storage.c) / [fileset.c](../../../../raw/postgres-19/src/backend/storage/file/fileset.c) / [reorderbuffer.c](../../../../raw/postgres-19/src/backend/replication/logical/reorderbuffer.c) — TOAST chunk writes, `wal_skip_threshold` at commit, and the two places `REPACK (CONCURRENTLY)` can spill to disk.
- [planner.c](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c) / [costsize.c](../../../../raw/postgres-19/src/backend/optimizer/path/costsize.c) / [catalog/index.c](../../../../raw/postgres-19/src/backend/catalog/index.c) — `plan_cluster_use_sort()`, `cost_sort()`'s `disabled_nodes` handling, and the parallel index-build request.
- [bufmgr.c](../../../../raw/postgres-19/src/backend/storage/buffer/bufmgr.c) / [localbuf.c](../../../../raw/postgres-19/src/backend/storage/buffer/localbuf.c) / [autovacuum.c](../../../../raw/postgres-19/src/backend/postmaster/autovacuum.c) / [postgres.c](../../../../raw/postgres-19/src/backend/tcop/postgres.c) — where `pg_stat_io` counts writes, the absence of `VACOPT_FULL` in autovacuum, and statement-timeout arming.

## Open Questions

- **Exact WAL/lock deadlock window.** The code comments warn that a concurrent `CREATE INDEX` (which conflicts with `ShareUpdateExclusiveLock`) can deadlock the worker against the leader's transaction; the precise reproduction and whether `deadlock.c` always detects it via the lock group is asserted in comments but not proven by a test here [repack.c#deadlock-note](../../../../raw/postgres-19/src/backend/commands/repack.c#L1015-L1027).
- **`REPLICA IDENTITY FULL` support.** The code explicitly rejects `FULL` as "not implemented yet"; whether a later commit on `REL_19_STABLE` past this pin adds it is out of scope for this checkout [repack.c#replident](../../../../raw/postgres-19/src/backend/commands/repack.c#L910-L924).
- **The reference page's `enable_sort` advice appears stale, and source wins.** `ref/repack.sgml` says that if the scan-and-sort method's peak temporary space "is intolerable, you can disable this choice by temporarily setting `enable_sort` to `off`" [ref/repack.sgml#Notes-on-Resources](../../../../raw/postgres-19/doc/src/sgml/ref/repack.sgml#L142-L167). In this checkout that setting cannot change the decision. `plan_cluster_use_sort()` compares only `total_cost` between the seqscan-and-sort path and the index path [planner.c#cost-comparison](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7166-L7180), while `cost_sort()` records a disabled `enable_sort` in `disabled_nodes` and adds nothing to the cost [costsize.c#cost_sort](../../../../raw/postgres-19/src/backend/optimizer/path/costsize.c#L2200-L2226) — the representation Robert Haas introduced in `e22253467942fdb100087787c3e1e3a8620c54b2`, "Treat number of disabled nodes in a path as a separate cost metric" (2024-08-21), which is an ancestor of this pin. The wording predates `REPACK`: the identical paragraph is in `doc/src/sgml/ref/cluster.sgml` at `ac58465e061^`, and `ac58465e061` carried it into the new reference page. The only scan-method override that still bites is `enable_indexscan = off`, which `plan_cluster_use_sort()` short-circuits on explicitly [planner.c#enable_indexscan-shortcircuit](../../../../raw/postgres-19/src/backend/optimizer/plan/planner.c#L7093-L7095). Not verified against a running v19 server, and no search was made for an upstream report of the stale wording.
- **No measurements.** The [I/O Impact and Throttling](#io-impact-and-throttling) section is source-and-history only at the asker's chosen scope. The relative cost of the paths it names — hint-bit dirtying of the old heap, TOAST rewrite volume, the buffer-bypassing write path, `CONCURRENTLY`'s decode/spill/replay overhead — is not quantified here.
- This page is drafted from a close source read but is **not yet agent-verified claim-by-claim**; `verified_by_agent` remains `not yet`.

## Related Pages

- [v19/index](../../index.md) — PostgreSQL 19 landing page.
- [How pg_plan_advice Works in PostgreSQL 19, and All Its Commits (unverified)](../query-planning/pg-plan-advice.md) — the other deep v19 feature walkthrough.
- [versions](../../../versions.md) — source pin manifest.
