# PostgreSQL 19

## Source Pin

- Branch/tag: `REL_19_STABLE`
- Commit: `dae3463fa969931458f1488f9b7af11e3741cd54`
- Status: `active`
- Source path: `raw/postgres-19/`
- Added: 2026-05-30
- Repinned: 2026-09-24

## Coverage

Behavioral claims cite the matching pinned checkout under `raw/postgres-19/`. PostgreSQL 19 has a `REL_19_STABLE` branch (upstream `master` has moved on to `20devel`), and the pin tracks its `dae3463f` tip, stamped `19beta4` (`REL_19_BETA4-17-gdae3463fa96`; the `REL_19_BETA4` tag is `b73d13c32c8`, 2026-09-21, and no RC or GA tag exists yet). Filed coverage includes a comprehensive walkthrough of the new `pg_plan_advice` contrib module with 34 direct module commits, a comprehensive walkthrough of the new in-core `REPACK` / `REPACK (CONCURRENTLY)` command with 79 feature-scope commits, a commit-backed walkthrough of parallel autovacuum, autovacuum table scoring (including the post-beta1 MXID-score division-by-zero fix `1f2297b5487`), and read-only scans setting all-visible VM bits, and a commit-by-commit history of online data checksum enabling and disabling, which was committed in `f19c0eccae9`, shipped in 19beta1 to 19beta3 and reverted in `c05d5ce1236` (85 commits, filed 2026-09-24). The 2026-09-24 repin reviewed all 159 commits after `135b867a` (crossing the `REL_19_BETA4` stamp `b73d13c32c8`): REPACK went from 58 to 79 feature-scope commits — `REPACK` now fails on any invalid index (`e14a2354d9f`, `0cabab44f12`), and `REPACK (CONCURRENTLY)` rejects non-heap table AMs (`fe7b6072342`), `user_catalog_table` tables (`04bb1500253`), materialized views (`40d811540a5`) and a dropped replica-identity index (`434bde9ce6b`), no longer needs `CONNECT` for the owner (`9bf3a414560`), copies the leader's timeouts into its decoding worker (`746a507a391`), and has fixed worker startup/shutdown (`47b5b23a780`) and progress reporting (`e418e8eb92a`, `d1f742d5d12`, `ccbea44294b`); `pg_plan_advice` went from 28 to 34 module commits and 20 to 21 core commits — partition names now need a schema (`3201ea8f171`), empty `JOIN_ORDER` sublists are rejected (`88cbbcb253a`), `JOIN_ORDER` feedback matching was fixed (`0f433a6b357`), and GEQO fitness now ranks `disabled_nodes` before cost (`d22961bca2c`), so join advice under GEQO is only probabilistic (`9ed2684b89a`); on-access VM setting now skips only a WAL-logged relation with logged hint bits whose dirty heap buffer needs a full-page image (`13243ae44cf`, which also adds the first regression test), the VM page is pinned before the heap cleanup lock (`b3609cd2db8`), frozen inserts no longer set `pd_prune_xid` (`41a8548ad41`), and the autovacuum freeze-score `pow()` scaling needs a component above 1.0 (`1bda0b23bce`). The range also reverted online data-checksum transitions (`c05d5ce1236`), SQL/PGQ (`2b9e1aff4d3`), `UPDATE/DELETE FOR PORTION OF` (`a9d2f728240`), `pg_get_*_ddl()` (`db169985c10`) and the `CREATE SCHEMA` extensions, and removed RI fast-path batching (`25649d6e791`); the navigation guide and the shared glossary's v19 notes were updated to match, and the glossary gained a GEQO entry. `verified_by_agent` is `not yet` on every v19 page. The 2026-09-01 repin reviewed all 104 commits after `67342a148`: `9bb8e16bd53` restricted REPACK's tolerate-a-dropped-relation ACL path to its unlocked discovery callers, `f23de46e15b` removed the undocumented `ONLY`/`*` spellings from the `REPACK` grammar, `dc8628e55d4` aligned the `ctid`, rewrite-command, progress-view and `ACCESS EXCLUSIVE` documentation with REPACK, `4af0528a0e4` made a rebalanced autovacuum cost limit reach the parallel vacuum workers instead of waiting for a reload, `7ddb9c41a13` gave `pg_stat_autovacuum_scores` autovacuum's TOAST-to-main reloption fallback, and `400c810ddbd` rejected the empty `FOREIGN_JOIN(())` sublist. Shifted anchors were refreshed, including the `gram.y` move caused by the `ALTER TABLE ... MERGE/SPLIT PARTITION(S)` revert `3e8bcc8644f`. A same-day claim-by-claim review of the `pg_plan_advice` page at that pin re-verified all 56 listed commits and every citation, and corrected six things: `PGS_CONSIDER_PARTITIONWISE` is never cleared by the module, `joinrel_setup_hook` reads only the trove's Gather/partitionwise slice, `EXPLAIN (PLAN_ADVICE) EXECUTE` shows nothing because the plan cache plans with no `ExplainState` (not because `PREPARE` planned earlier — it does not plan at all), the star-schema example was missing its `NO_GATHER` line, the `3ab3f33281f` entry mixed up generated with supplied `DO_NOT_SCAN` advice, and `ea203d371de` is Richard Guo's commit; the module's `postgres_fdw` TAP test was also added to the coverage. The earlier 2026-08-17 repin reviewed 152 commits after `99e47536`, crossing the `REL_19_BETA3` stamp: `5d47df21e89` added the `output_plugin_libraries` GUC and moved the pgrepack direct-use rejection into `StartupDecodingContext()` (CVE-2026-6471), `6a80179f6b0` closed the logical-decoding-deactivation versus slot-creation races, `d7db169fa13` rejected `CLUSTER (ANALYZE)`, `fd56954c9fe` documented REPACK's `NO_LOGICAL` table-AM flags, `567286b762b` made role and database-ownership changes invalidate role-dependent cached plans (CVE-2026-14666), `3ab3f33281f` renamed EXISTS-to-ANY subplans to `exists_to_any_*` and so changed `pg_plan_advice` target names, `b3331578b58` restored the autovacuum launcher's descending database sort, and `4ee0ccfd55f` refreshed the parallel-autovacuum worker-flag comment. Shifted source anchors were refreshed. The checkout had also drifted behind its declared pin before this repin; `scripts/wiki_lint` now verifies pin against checkout HEAD.

- [PostgreSQL 19 Codebase Navigation Guide (unverified)](codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v19 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [Wiki Glossary (unverified)](../glossary.md) - The one glossary shared by every version. PostgreSQL jargon, acronyms and source-code terms, each entry citing the pinned checkout it was checked on (PostgreSQL 12, 14, 17, 18 and 19; 260 terms).

## Questions

### Query Planning

- [How pg_plan_advice Works in PostgreSQL 19, and All Its Commits (unverified)](questions/query-planning/pg-plan-advice.md) - Comprehensive explanation of the new `pg_plan_advice` contrib module: the core `pgs_mask` strategy-mask and five planner hooks it relies on, per-index disabling for index-specific advice, the advice language (tags, relation identifiers, including underscore-separated occurrence numbers), plan-to-advice generation, advice enforcement (clear-only mask writes; `PGS_CONSIDER_PARTITIONWISE` never cleared, partitionwise driven through `PGS_JOIN_ANY` or Append/Merge Append instead), feedback/EXPLAIN output, GUCs, prepared-statement and plan-cache interaction (including why `EXPLAIN (PLAN_ADVICE) EXECUTE` displays nothing: the plan cache plans with no `ExplainState`, even though `PREPARE` itself defers planning to the first execution), the three test layers including the module's `postgres_fdw` TAP test, and the full scoped source history: 21 core planner foundation/enabling/fix commits, 34 direct module commits, and test/doc/build support commits. Re-verified claim by claim on 2026-09-01, with six corrections. Refreshed for the 2026-09-24 repin: partition names need a schema, empty `JOIN_ORDER` sublists are rejected, `JOIN_ORDER` feedback matching was fixed, and join advice under GEQO is only probabilistic; agent verification reset to `not yet`.

### Storage and Vacuum

- [How the REPACK Command Works in PostgreSQL 19, and Its 79 Feature-Scope Commits (unverified)](questions/storage-and-vacuum/repack-command.md) - Comprehensive walkthrough of the new in-core `REPACK` / `REPACK (CONCURRENTLY)` command (which absorbs `VACUUM FULL` and `CLUSTER`): the blocking rewrite path, lock-free multi-table candidate discovery followed by locked per-table open/recheck, partitioned-target discovery hardening, the concurrent online rewrite via logical decoding (decoding `bgworker`, `pgrepack` output plugin, temporary replication slot, race-safe on-demand decoding activation, change spill/replay, lock upgrade and swap), the `max_repack_replication_slots` GUC, `pg_stat_progress_repack`, generic `MAINTAIN` / `pg_maintain` documentation, tests, and 79 feature-scope commits. The 2026-09-02 follow-up rewrote the I/O section: there is no throttle at all (three options, none about I/O; no `vacuum_delay_point()` anywhere on the rewrite path; no caller but `ExecRepack()` and `VACUUM (FULL)`), the blocking rewrite **bypasses shared buffers** through `bulk_write.c` while only the `CONCURRENTLY` copy uses a `BAS_BULKWRITE` ring, both modes dirty the old heap (hint bits versus on-access pruning) and rewrite TOAST, and the section now carries a 13-row settings table with apply scopes plus an Open Question on the reference page's stale `enable_sort` advice. The 2026-09-24 repin added 21 commits: `REPACK` now fails on invalid indexes, and `CONCURRENTLY` rejects non-heap table AMs, `user_catalog_table` tables, materialized views and a dropped replica-identity index, with worker lifecycle, timeout, `CONNECT` and progress-reporting fixes.
- [PostgreSQL 19 Autovacuum and VACUUM: Parallel Workers, Table Scoring, and Setting Pages All-Visible During Reads (unverified)](questions/storage-and-vacuum/autovacuum-parallel-scoring-visibility.md) - Comprehensive, commit-backed explanation of three independent v19 features: parallel autovacuum (the `autovacuum_max_parallel_workers` GUC and `autovacuum_parallel_workers` reloption, the autovacuum-vs-maintenance worker cap in `parallel_vacuum_compute_workers`, `PVSharedCostParams` cost-delay propagation, and the post-beta3 `4af0528a0e4` fix that pushes a rebalanced cost limit to the workers after every nap), autovacuum table scoring (`AutoVacuumScores`, the five `*_score_weight` GUCs and all-zero escape hatch, descending sort, and `pg_stat_autovacuum_scores` with its post-beta3 `7ddb9c41a13` TOAST-reloption fallback), and read-only query scans setting all-visible in the visibility map via on-access pruning (`ScanRelIsReadOnly` -> `SO_HINT_REL_READ_ONLY` -> `heap_page_prune_opt` -> `HEAP_PAGE_PRUNE_SET_VM`), with the VACUUM VM-skip benefit, the read-query no-extra-dirtying safety valve, the post-beta1 `e9eaeb04248` FSM fix, the `b01c31eef9c` VM-clear WAL/incremental-backup correction plus its `9171f77db23` TAP test, and the `3180ce3d7a8` wrong-buffer error-path hardening; includes per-feature source-commit history. The 2026-09-24 repin rewrote the safety valve for `13243ae44cf` (only a dirty heap buffer that needs a full-page image on a WAL-logged, hint-logging relation skips setting the VM), added its first regression test, and folded in `b3609cd2db8`, `41a8548ad41`, `1bda0b23bce` (freeze-score scaling) and the `XLOG_PAGE_MAGIC` bump.
- [History of Online Data Checksum Enabling and Disabling in PostgreSQL 19, and All Its Commits (unverified)](questions/storage-and-vacuum/online-data-checksums-history.md) - Commit-by-commit history of online data checksum enabling and disabling, 85 commits in all, plus 27 checked and excluded. It covers the 2018 attempt (`1fde38beaa0`), reverted before 11beta1, and the 2019 ProcSignal barrier infrastructure. It covers the 2026 feature (`f19c0eccae9`) with its 51 follow-up fixes, tests and docs, and the release-note, tree-wide and translation commits. It ends with the 2026-09-16 revert on `REL_19_STABLE` (`c05d5ce1236`) and its two cleanups, so 19beta4 and the pin do not have the feature. The page also explains how the reverted feature worked: four states, ProcSignal barriers, the `XLOG2_CHECKSUMS` record and the launcher/worker page rewrite. It lists what the pinned tree still contains: the two-value state enum, the "buffer will be zeroed" note, `data_checksum_version_init` and a leftover injection point. It records the shallow-clone gap that the asker chose not to close with a fetch.
