# PostgreSQL 17.10

## Source Pin

- Branch: `REL_17_STABLE`
- Commit: `54eeefaedbee0385529f3edf321bb99e49232aaa`
- Status: `active`
- Source path: `raw/postgres-17/`
- Added: 2026-05-13

## Coverage

Behavioral claims cite the matching pinned checkout under `raw/postgres-17/`.

- [PostgreSQL 17 Contrib Extensions (unverified)](questions/contrib-extensions.md) - Complete inventory of PostgreSQL 17 contrib extensions backed by `*.control` files, with explanations for data types, indexes, diagnostics, FDWs, transforms, and SPI trigger examples.
- [Proposing a Sampling pgstatindex Variant for PostgreSQL 17 (unverified)](questions/pgstatindex-sample-variant-proposal.md) - Proposal for a `pgstatindex_approx` that random-samples physical B-tree pages instead of scanning all of them, with exact-vs-estimated fields, pros/cons, extension wiring, and a `pageinspect` SQL prototype.
- [GUC Default-Value Changes Since PostgreSQL 12 (unverified)](questions/guc-default-changes-since-v12.md) - The seven settings present in both v12 and v17 whose built-in default changed (`ssl_min_protocol_version`, `password_encryption`, `vacuum_cost_page_miss`, `checkpoint_completion_target`, `shared_buffers` boot value, `log_checkpoints`, `log_autovacuum_min_duration`), with old/new value, introducing major version, apply scope, exclusions for sample-only/type/added/removed/range-only changes, and test-coverage notes.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 17 (unverified)](questions/create-index-concurrently.md) - Comprehensive walkthrough of the v17 CIC implementation (four internal transactions, two heap scans, three waits; the `indislive`/`indisready`/`indisvalid` progression); a dedicated steps-and-locks section (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, `WaitForOlderSnapshots`); and a "what changed from PostgreSQL 12" section centered on the PG14 `PROC_IN_SAFE_IC` optimization (safe builds ignored in the snapshot wait, set via `set_indexsafe_procflags`), the reverted VACUUM-ignores-CIC attempt, and the `PGXACT`->`PGPROC` `statusFlags` move — all anchored to the v17 checkout's own commit history.
- [How Bi-Directional Logical Replication Works in PostgreSQL 17, and All Related Commits by Minor Version (unverified)](questions/bidirectional-logical-replication.md) - Explains the v17 bi-directional setup: two nodes publish and subscribe to each other with `origin = none`; apply workers tag replayed transactions with a per-subscription replication origin (`pg_<suboid>`) so every WAL record carries the origin ID, and the publisher's `pgoutput` `filter_by_origin_cb` skips origin-tagged changes at decode time (per change and per transaction via `DecodeTXNNeedSkip`), stopping loops after one hop; covers the `pg_subscription.suborigin` catalog plumbing, the `START_REPLICATION ... origin` option (publishers >= 16 only), reserved origin names, the `copy_data = true` + `origin = none` initial-sync WARNING from `check_publications_origin`, the in-tree two-node setup pattern, and the v17 limitations (manual conflict resolution, no DDL/sequence replication); includes the full commit history by minor version — foundations (9.5 origins, 9.6 message filtering, 10 logical replication), the 16-cycle core commits (`366283961a`, `8756930190`, `0324651573`), the 17.0 `pgoutput` filter-state fix (`54ccfd6586`), the 17.5 partition-aware WARNING fix (`0ae1245e04`), and the 17.10 CVE-2026-6638 SQL-injection fix (`f0f59b658e`), plus incidental and adjacent origin-infrastructure commits — anchored to the checkout's own commit history with `Stamp` bracketing.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 17? (unverified)](questions/attach-partition-index-drops.md) - Answers that ATTACH PARTITION never drops an index: `AttachPartitionEnsureIndexes` is strictly match-or-create (re-parent a compatible existing partition index via `IndexSetParentIndex`, else create one via `DefineIndex`), incompatible/extra indexes are kept, and the three look-alike cases (an absorbed index can no longer be dropped on its own, the manually-dropped redundant CHECK constraint, and a later cascading DROP on the parent) are not ATTACH drops; includes a version-by-version "changes since PostgreSQL 12" section — v13 attmap refactor and error-code fix, v14 error-wording and the dedicated `DROP INDEX CONCURRENTLY`-on-partitioned-index error, v15 `NULLS NOT DISTINCT` matching and the partitioned-index DROP deadlock fix, v16 invalid-index match skip (`fc55c7ff`, back-patched to v11+) and the `DefineIndex` `total_parts` parameter, v17.0 PK-vs-UNIQUE constraint-type match (`cee8db3f`), `CompareIndexInfo` expression-vs-column fix, `validatePartitionedIndex` syscache-copy fix, and `relhassubclass` locking, plus the 17.x stable update letting a re-attach validate a still-invalid parent index (`becf6d2696`) — all anchored to the v17 checkout's source and commit history, with first-release attribution bracketed by the "Stamp HEAD as NNdevel" commits.
