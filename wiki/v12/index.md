# PostgreSQL 12.2

## Source Pin

- Branch: `REL_12_STABLE`
- Commit: `45b88269a353ad93744772791feb6d01bc7e1e42`
- Status: `legacy`
- Source path: `raw/postgres-12/`
- Added: 2026-05-02

## Coverage
Behavioral claims cite the matching pinned checkout under `raw/postgres-12/`.

- [PostgreSQL 12 Codebase Navigation Guide (unverified)](codebase-navigation-guide.md) - Reviewed root-level map of the pinned v12 tree: build ownership; backend and standalone entry; simple and extended Parse/Bind/Execute/Sync flow; raw parse, analysis, RIR rewrite, planning, portal strategies, and full executor lifecycle; utility/event-trigger routing and error recovery; plan-cache, portal, planner, executor, relcache, GUC, and AM structures; custom AM registration and packaged/core extension boundaries; generated catalog/parser/fmgr/LWLock artifacts; and regression, isolation, TAP, module, and doc surfaces.

## Questions

### Query Planning

- [Foreign-Key Join Optimization for Two-Table Joins (unverified)](questions/query-planning/fk-join-optimization-two-tables.md)
- [Query Planner Statistics Sources in PostgreSQL 12 (unverified)](questions/query-planning/query-planner-statistics-sources.md) - Fully reviewed at the 12.2 pin: separates cumulative monitoring from direct planner inputs; traces `pg_class`, physical size, `pg_statistic`, built extended data, ordinary/partial index sizing, inherited and expression-index statistics including partial-index correlation, defaults/security/errors, indirect auto-analyze and planner endpoint counter increments, other estimate/cost metadata, AM/FDW/hook/contrib and generated-cache boundaries, and direct tests plus explicit test absence.
- [Planner Penalties for Bloated Indexes in PostgreSQL 12 (unverified)](questions/query-planning/bloated-indexes-query-planner.md) - Explains how v12 penalizes B-tree bloat indirectly through physical index pages, B-tree height, and page-cost modeling; scopes the penalties across access methods (page-count penalty shared via `genericcostestimate()`; measured tree-height charge B-tree-only, GiST/SP-GiST descent charges use a page-count-estimated height, hash has none, GIN/BRIN use separate models); expands on `RelationGetNumberOfBlocks()` and `index->pages`; proposes an `avg_leaf_density` / `leaf_fragmentation` REINDEX triage heuristic. A filed follow-up answers when the planner discards a GIN index and uses a B-tree instead, separating three gates: clause matching against the GIN operator family in `match_opclause_to_indexcol()` (with the `get_index_clause_from_support()` escape hatch and the core `pg_amop.dat` GIN memberships that carry no `<`/`<=`/`>=`/`>`); plan shape from GIN's AM flags (`amgettuple = NULL` so bitmap-only, no `amcanorder`, `amcanreturn = NULL`, `amsearchnulls`/`amsearcharray`/`amcanparallel` false), which rules out plain index scans, `ORDER BY` pathkeys, index-only scans, `IS NULL`, and native `IN (...)`; and cost, where `gincostestimate()` charges `random_page_cost` for every pending, entry, and data page with no descent shortcut while `genericcostestimate()` charges the B-tree a pro-rata page share. Covers `choose_bitmap_and()` / `add_path()` pruning, the 4X stale-metapage-statistics fallback, the keyless full-index path a partial GIN index can still produce via `amoptionalkey` and `GIN_SEARCH_MODE_EVERYTHING`, the four `CREATE INDEX` AM rejections (unique, INCLUDE, ASC/DESC, exclusion), where GIN still wins, eight settings with exact apply scopes, and the explicit absence of any upstream GIN-versus-B-tree plan comparison or `gincostestimate()` coverage. Exact-pin measurements on 12.2 price the same `n = 42` predicate at `12.22` through GIN and `4.65` through a B-tree on one table with identical statistics — even though the GIN index is 279 pages against 826 — and show a 1177-page `fastupdate` pending list moving GIN's index cost from `12.45` to `4720.55` so the planner drops it from the `BitmapAnd` entirely, with `393 * random_page_cost` reproduced exactly against `pgstatginindex` and `gin_clean_pending_list()`.
- [Comprehensive plan_cache_mode Analysis in PostgreSQL 12 (unverified)](questions/query-planning/plan-cache-mode.md) - Fully reviewed at the 12.2 pin: the GUC definition and session/transaction apply scope, the eight ordered decision rules in `choose_custom_plan`, what the setting cannot override (one-shot plans, parameterless statements, PL/pgSQL simple expressions, the simple protocol, SQL `DECLARE CURSOR`, and unnamed protocol statements whose counters reset on every `Parse`), the `auto` cost comparison and its custom-only planning charge, the correction step, parameter folding and its selectivity effect, key `CachedPlanSource`/`CachedPlan` fields, all `GetCachedPlan` callers, cost-history retention across invalidation and `DISCARD PLANS`, the generic-plan lock footprint, v12 observability limits and error paths, build/extension boundaries, test coverage and gaps, and the five ancestor commits. Includes fourteen exact-pin measurements with plan costs, lock counts, protocol comparisons, and planning-time numbers.
- [Table Partitioning Optimizations and Configuration During Query Planning and Execution in PostgreSQL 12 (unverified)](questions/query-planning/partitioning-planning-execution-optimizations.md) - Catalogs the five partitioning-related planner/executor optimizations and their configuration in v12: partition pruning (plan-time before child expansion, and run-time initial + per-scan on `Append`/`MergeAppend`) with per-strategy HASH/LIST/RANGE differences; constraint exclusion (plan-time only, mainly legacy inheritance); partitionwise join and partitionwise aggregate (both off by default, requiring matching partition schemes/bounds); executor tuple routing, cross-partition UPDATE row movement, and per-partition COPY batching; plus the four session-scoped GUCs and the v12 limitation that execution-time pruning does not apply to `ModifyTable`. Includes a follow-up analysis of locking and random-I/O sensitivity per optimization: only plan-time pruning removes read locks (partitions are pruned before `expand_partitioned_rtentry` opens/locks them), while constraint exclusion runs after `find_all_inheritors` already locked every child, run-time pruning keeps all plan-time-surviving partitions locked via `AcquireExecutorLocks`, and write-path tuple routing takes `RowExclusiveLock` lazily per touched partition; and the scan-eliminating optimizations (pruning, constraint exclusion) gain the most on slow random storage because avoided per-partition index heap fetches are priced at `random_page_cost` (4× `seq_page_cost`), whereas partitionwise join/aggregate mainly cut CPU/memory/cache misses (v12 models hash-join spill as sequential and keeps hash aggregation in memory).

### Indexing

- [Physical Index Statistics, Tuple Counts, and Bytes per Tuple in PostgreSQL 12 (unverified)](questions/indexing/physical-index-statistics-tuple-counts-and-bytes.md) - Fully reviewed at the 12.2 pin. Maps shared `pg_class` and `pg_index` metadata plus persistent metapage, page, and tuple fields for B-tree, hash, GiST, GIN, SP-GiST, BRIN, and contrib Bloom. It separates stored counters, catalog estimates, and page-scan measurements; explains why `pg_relation_size() / reltuples` is allocated bytes per the current `reltuples` unit rather than a universal tuple size; and states which row, entry, summary, and byte calculations are exact, conditional, or unavailable without access-method-specific traversal. The review corrected the `pgstattuple` payload rule (B-tree and hash pass an already-`MAXALIGN`ed `IndexTupleSize()`, so `tuple_len` includes each item's alignment padding, and the bigint averages need a `numeric` cast), the `btm_last_cleanup_num_heap_tuples` writer (bulk delete as well as cleanup), the `ANALYZE` skip guard, `PageIndexTupleDeleteNoCompact()` space accounting, `get_raw_page()`'s superuser requirement, and the `pgstattuple` regression-coverage claim; it also quantifies the BRIN `revmapNumPages` off-by-one and adds the `pd_prune_xid` comment-versus-GIN discrepancy.
- [How NULL Values Are Handled in PostgreSQL 12 Indexes (unverified)](questions/indexing/null-values-in-indexes.md) - Explains NULL handling by index access method: B-tree storage, ordering, `IS NULL` search, and unique-index semantics; hash NULL-entry skipping; GiST tuple NULL markers; SP-GiST's separate NULL tree; GIN NULL categories and lack of generic `NullTest` matching; BRIN `hasnulls`/`allnulls` range summaries; and contrib Bloom's no-NULL-search behavior.
- [Pros and Cons of Partial Indexes in PostgreSQL 12 (unverified)](questions/indexing/partial-indexes-pros-cons.md) - Explains partial-index benefits, tradeoffs, ordinary expression indexes as the baseline, partial indexes on expressions, planner predicate implication and expression-key matching, maintenance and operational restrictions, `ANALYZE` row-count and expression-statistics behavior, and how the planner costs partial indexes.
- [How pgstatindex Calculates B-Tree Index Statistics in PostgreSQL 12 (unverified)](questions/indexing/how-pgstatindex-calculates-information.md) - Reviewed exact v12 path: fresh extension 1.4-to-1.5 install/update wiring, wrappers and privileges, true-root metapage fields, one-time main-fork length capture, full physical block scan, half-dead `empty_pages`, captured-size invariant, physical leaf occupancy including retained `LP_DEAD` storage, backward-right-link fragmentation, two-decimal output formatting, concurrency and B-tree-validation limits, generated `BTREE_AM_OID` header dependency, and regression gaps.
- [Measuring B-Tree Index Bloat With Core SQL Only in PostgreSQL 12 (unverified)](questions/indexing/btree-index-bloat-core-sql-only.md) - Proposes measuring bloat as `actual size − modelled fresh size`, where the fresh size is computed from v12's own deterministic build rule (`_bt_pagestate`/`_bt_buildadd`: a leaf page closes once free space drops below `BLCKSZ * (100 - fillfactor) / 100`, giving `tuples_per_leaf = floor((BLCKSZ - 48 - floor(BLCKSZ * (100 - ff) / 100)) / (MAXALIGN(hoff + data) + 4))`). Ranks four core-only methods with measured accuracy: a 26.7 ms catalog-only sweep that matched an actual `CREATE INDEX CONCURRENTLY` rebuild to the block on 10 of 14 fixtures; a `pg_column_size` key-width refinement that cut the one 4.6% failure to 0.32%; an `EXPLAIN (ANALYZE, BUFFERS)` index-only-scan census that reproduced `pgstatindex`'s `leaf_pages` exactly on all 12 eligible fixtures and its `avg_leaf_density` to within 0.14 points; and the exact rebuild probe. Establishes the contrib boundary (no core page/FSM reader exists, and all four relevant contrib control files default to `superuser = true`), documents `VACUUM VERBOSE`'s exact page census and the no-op-VACUUM gap, and reports measured failure modes: stale `reltuples` hiding 89.9% bloat until `n_live_tup` is used, an all-duplicate index that a rebuild grows from 1291 to 1376 blocks, a random-insert index permanently at 27%, a 2330-deleted-page index whose leaf density still reads 89.83, and heap fetches destroying the census. A filed follow-up compares the core-SQL results with `pgstatindex` over a 54-index matrix (9 bloat types x 3 scales x partial/non-partial): the model is exact against a real rebuild in 39 of 54 cells and within 5 blocks in 47, the index-only-scan census reproduces `leaf_pages` exactly in all 36 cells that pass the `Heap Fetches: 0` precondition and `avg_leaf_density` to within 0.15 points, and `blocks - leaf_est` recovers the non-leaf total exactly, including 2330 deleted pages. Scored as a rebuild-size predictor, `ceil(leaf_pages * avg_leaf_density / fillfactor) + internal_pages + 1` loses to the core-SQL model in 16 of 18 cells and is 994.4% wrong on an `LP_DEAD` fixture where `pgstatindex` reports a healthy 90.06 density on an index that a rebuild shrinks by 90.9%, because physical occupancy counts dead entries as used. The one large model failure is a partial index on a table deleted without VACUUM or ANALYZE (510 blocks, 92.6 points), since partial indexes cannot use `n_live_tup`; one plain `ANALYZE` drops that error to at most one block. A source-only correction then renames the statements' reporting identifiers off the word "bloat" — `wasted` to `wasted_space`, `bloat_pct` to `wasted_space_pct`, the tag to `wiki_btree_wasted_space_sweep`, and the Method C probe index to `wiki_wasted_space_probe` — and establishes why: v12's own definition of a bloated index is per-page ("many empty or nearly-empty pages" in `ref/reindex.sgml`, with `maintenance.sgml`'s partly-emptied-page mechanism), state this file-size-minus-model arithmetic cannot see, while "wasted space" is `ref/copy.sgml`'s phrase for what a maintenance command recovers; no SQL-visible interface in the tree says `bloat` and this checkout has no glossary at all. No number moves, because `AS` only labels and the two label changes cannot reach the expression `ORDER BY` key or the query ID (v12 jumbles the `Query` tree in contrib `pg_stat_statements`, ignores `resname`, and never sees the comment, which the lexer discards) — but the probe rename is an object name in five places and, because v12 keys a utility statement by a hash of its text, does give the `CREATE`/`DROP INDEX CONCURRENTLY` new `pg_stat_statements` entries.
- [Impact of B-Tree Leaf Density (60% vs 90%) on Index Scan Queries in PostgreSQL 12 (unverified)](questions/indexing/leaf-density-60-vs-90-query-impact.md) - Reviewed analysis of planner page-count scaling, executor leaf-page walking, how splits produce 60% density, per-query-shape impact from probes to full index scans, settings, build boundaries, and the test gap, with exact-pin measurements of leaf pages, warm buffers, plan costs, and a density-driven `tree_level` change.
- [B-Tree Leaf Density vs Fragmentation Impact on Index Scan I/O in PostgreSQL 12 (unverified)](questions/indexing/leaf-density-vs-fragmentation-index-scan-io.md) - Fully reviewed comparison of density-driven leaf-page growth against fragmentation-driven physical-order penalties: the exact `pgstatindex` definitions, the one-buffer-per-right-link scan path, the planner page-count model and its metapage/internal/deleted-page slack, per-level density and fragmentation estimate tables, and the v12 absence of index-page prefetch (`PrefetchBuffer` reaches only heap forks; `effective_io_concurrency` covers bitmap heap scans only). Adds exact-pin measurements: 90.06 versus 59.90 density raised warm index-only scan buffers 50.5 % (2738 to 4121, matching the predicted 1.5035x), while removing 49.95 % fragmentation at matched density moved them 0.5 %; `pageinspect` confirmed 1845 of 3694 backward right links, a mean jump of 1848 blocks, and zero links to the next physical block, so `leaf_fragmentation` understates disorder. Also documents what both metrics hide: `LP_DEAD` entries keep density at 90.00 over 90 % dead entries (547 leaf pages before and after `VACUUM`, density falling to 9.26), half-dead pages stay in the leaf chain yet are excluded from both columns, deleted pages are unlinked but still priced, and both columns return `NaN` on an empty index; plus session-scoped GUC apply scopes and a validated `pgstatindex` survey query.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](questions/indexing/create-index-concurrently.md) - Reviewed comprehensive walkthrough of the v12 CIC parser/utility/generated-catalog path and `DefineIndex`: the four internal transactions, two heap scans, and three deliberate transaction-set waits; the `indislive`/`indisready`/`indisvalid` state-flag progression set non-transactionally by `index_set_state_flags`; a `maintenance_work_mem` follow-up covering the AM-dependent first-build and common validation-TID-sort uses, B-tree's intended primary-sort budget, 32 MB planned-participant thresholds, requested-versus-launched shares, required tape runs, per-AM behavior across B-tree/hash/GiST/GIN/SP-GiST/BRIN/Bloom including conditional GIN cleanup and BRIN's empty enumeration, scoped sort/temp observability, and the workload-specific plateau; a broader performance-GUC matrix covering B-tree worker request and pool limits, AM-specific settings, v12 heap-scan and storage boundaries, WAL/checkpoint/commit costs, waits/timeouts, exact apply scopes, absent later-version controls, and commonly confused non-controls; plus a dedicated section mapping every step to the table locks (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, and `WaitForOlderSnapshots`), with the `LockConflicts` rationale for why DML proceeds while another CIC/`VACUUM`/`ANALYZE`/DDL is blocked. Distinguishes the four deliberate core transaction barriers from additional waits in DDL event triggers, index predicates/expressions, AM callbacks, B-tree unique validation, and parallel-worker coordination; at the four core barriers it catalogs conflicting-lock holders at the initial `ShareUpdateExclusiveLock` acquisition (mapped to v12 commands via the `mvcc.sgml` lock table, with the autovacuum SIGINT-cancel/anti-wraparound exception and prepared-transaction lock retention), open writer transactions at the two `WaitForLockers(ShareLock)` waits (idle-in-transaction included; lock waiters, later writers, and prepared transactions skipped), and same-database old-snapshot holders at `WaitForOlderSnapshots` (long-running queries, REPEATABLE READ/SERIALIZABLE transactions, cursor-holding idle sessions, manual `ANALYZE`, other CICs; autovacuum/lazy-VACUUM, other databases, xmin-0 backends, and prepared transactions excluded), with the `pg_stat_progress_create_index` wait phases, a resolved inter-builder trace (same-table CICs serialize by `ShareUpdateExclusiveLock`; different-table CIC/RIC can meet at Wait 3 through database-wide old-snapshot filtering; the builder clears its own `xmin` before waiting), a resolved assessment that skipping prepared transactions in the `WaitForLockers(ShareLock)` writer waits is not sufficient for index correctness (a prepared write that commits after the build is left unindexed), a resolved first-build-scan tuple-visibility trace into the heap AM (`index_concurrently_build` sets `ii_Concurrent`, so `heapam_index_build_range_scan` scans with a fresh MVCC snapshot via `HeapTupleSatisfiesMVCC` instead of the non-concurrent `SnapshotAny`/`HeapTupleSatisfiesVacuum` path, omitting `RECENTLY_DEAD` tuples and never setting `ii_BrokenHotChain`), and worked examples for a running `pg_dump` (blocks only the snapshot wait, for the dump's whole duration, timeouts disabled) and an hour-long open / idle-in-transaction session (blocks per what it did: target-table writes hit the writer waits, pinned snapshots hit the snapshot wait, reads-only `READ COMMITTED` sessions clear their xmin via `SnapshotResetXmin` and block nothing). Adds a comprehensive failure-scenarios section: the last persistent state transition fixes the outcome — no index before commit 1, invalid/not-ready before `SET_READY`, invalid/ready before `SET_VALID`, or a valid index despite a reported command error when `ddl_command_end` fails after `SET_VALID` — with the planner/executor/HOT/uniqueness cost of each leftover, the fact that ordinary ERROR/cancel failures release (do not leak) the session lock, the resolved crash/immediate-shutdown recovery outcome (below), regression evidence (a failed unique build left `INVALID` and retained through `VACUUM FULL`), and `DROP INDEX` / `REINDEX INDEX CONCURRENTLY` recovery, and a resolved walsender/replication-slot Wait 3 assessment (a `hot_standby_feedback` standby and any replication slot hold the cluster's dead-row removal horizon through `GetOldestXmin`/`RecentGlobalXmin`, not a per-backend xmin, so they never enter `WaitForOlderSnapshots`; a physical walsender is additionally excluded by database and by the valid-VXID gate, and only a logical walsender mid-`REPEATABLE READ` snapshot export is eligible — as an ordinary in-database transaction), and a resolved crash / immediate-shutdown analysis (the `index_set_state_flags` flips are xidless `heap_inplace_update` writes that `RecordTransactionCommit` commits asynchronously and `heap_xlog_inplace` redoes physically on recovery, so replay lands on one of the four flag outcomes and cannot lose completed build data while retaining durable SET_VALID for the reviewed in-tree AMs — B-tree via outside-buffer `smgrimmedsync`, hash/GiST/SP-GiST/GIN/BRIN via shared-buffer WAL, and contrib Bloom via `GenericXLog`; the validate scan obeys WAL-before-data, unlogged heap/index forks reset together, the separate prepared-transaction gap is not repaired by recovery, and third-party AM durability remains outside the core-source proof); the `REINDEX INDEX CONCURRENTLY` mechanics now live on a separate page.
- [How REINDEX INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](questions/indexing/reindex-index-concurrently.md) - Comprehensive walkthrough of the v12 `REINDEX INDEX CONCURRENTLY` implementation in `ReindexRelationConcurrently`: the six phases (create the `_ccnew` copy, build, validate, swap, set-dead, drop) and five wait points — the same three CIC waits (two `ShareLock` conflict-set waits plus `WaitForOlderSnapshots`) plus two `AccessExclusiveLock` reader-capable waits before set-dead and drop; the shared build/validate core; the atomic `index_concurrently_swap` (new valid + old invalid, `_ccnew`→original / original→`_ccold`, with constraints/triggers/comment/dependencies and per-index cumulative stats moved); session- and transaction-level `ShareUpdateExclusiveLock` on the old index, new index, and heap; dispatch/restrictions (no transaction block, temp fallback, direct system-catalog error vs schema/database skip, partitioned-table skip vs partitioned-index error, exclusion error-vs-skip, invalid skip-vs-allow, toast rebuilt); two-index state flags; per-phase failure outcomes with planner-vs-executor invalid-index overhead, regression recovery evidence, and the dedicated `reindex-concurrently` isolation spec; a dedicated section confirming a failure never leaves the original `index_name` invalid for a healthy index (the phase-4 swap atomically renames and re-validates in one transaction, so the invalid leftover is always the differently-named `_ccnew` or `_ccold`, never the bare `index_name`, and a bloated-but-valid index never becomes no usable index), the sole exception being an index already invalid before RIC ran; a source-backed GUC-performance follow-up covering AM-dependent build/validation memory, B-tree-only worker controls, v12 bulk-read/synchronized-scan boundaries, temporary and fixed output storage, B-tree immediate sync plus WAL/checkpoint/commit costs, all five timeout-sensitive waits, exact apply scopes, absent later-version controls, and commonly confused non-controls; a dedicated section on running RIC against an already-invalid index (rebuilt/repaired when named directly, skipped with a warning when reached via `REINDEX TABLE`/`SCHEMA`/`DATABASE`); multi-index batching; and the progress-phase discrepancy where phase 6 reports `WAIT_4` so the view's `waiting for readers before dropping` text is never emitted.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 12? (unverified)](questions/indexing/attach-partition-index-drops.md) - Answers that `ALTER TABLE ... ATTACH PARTITION` never drops an index from the attached table: `AttachPartitionEnsureIndexes` is strictly match-or-create (re-parent a compatible existing partition index via `IndexSetParentIndex`, or create a clone via `DefineIndex`), with incompatible/extra indexes left in place; confirms no `performDeletion`/`RemoveInheritance` runs in the attach path (`MergeConstraintsIntoExisting` is CHECK-only), notes v12's looser matching (no `indisvalid` skip, no constraint-type check) still never drops, distinguishes the three look-alikes (absorbed index, manual redundant-CHECK drop, cascading parent `DROP INDEX`), shows `ALTER INDEX ... ATTACH PARTITION` likewise only re-parents or errors, and is regression-proven (four incompatible indexes kept plus one added).
- [All Outcomes That Leave an Invalid Index in PostgreSQL 12, Including a Failed CREATE INDEX CONCURRENTLY (unverified)](questions/indexing/invalid-index-outcomes.md) - Enumerates the complete, closed set of operations that can persist `pg_index.indisvalid = false` in v12 — a failed/cancelled/crashed `CREATE INDEX CONCURRENTLY`, a failed `REINDEX CONCURRENTLY` (invalid `_ccnew`/`_ccold`), a failed/interrupted `DROP INDEX CONCURRENTLY` (the clear-valid step is retryable), an incomplete partitioned parent index (`CREATE INDEX ON ONLY` or an attached invalid child, resolved by `validatePartitionedIndex` on `ATTACH PARTITION`), and `pg_upgrade` from <= 9.6 marking new-cluster hash indexes invalid — grounded in the full list of `indisvalid=false` writers; with the persistent `(indislive, indisready, indisvalid)` state table, the contrast that single-transaction `CREATE INDEX`/`REINDEX`/`DROP INDEX` never leave invalid leftovers, what an invalid index costs (planner skip, executor still maintains when ready, VACUUM still processes) and which commands reject it (`CLUSTER`, `REFRESH MATERIALIZED VIEW CONCURRENTLY`, `ADD CONSTRAINT ... USING INDEX`, `REPLICA IDENTITY`, bulk `REINDEX CONCURRENTLY`), and how to clear each; cross-links the dedicated CIC and RIC pages.
- [Calibrating a COMMENT-Stored Bytes-per-Table-Tuple REINDEX Threshold for Every Non-B-Tree Index in PostgreSQL 12 (unverified)](questions/indexing/comment-stored-bytes-per-table-tuple-non-btree.md) - Stores `pg_relation_size(index) / table pg_class.reltuples` in each non-btree index comment at build time and calibrates the percentage increase that should trigger a rebuild. Measured on an isolated 12.2 server over 132 cells (22 workloads x 6 index shapes) with `REINDEX` ground truth: GiST is the only access method that calibrates cleanly (any threshold in 20-30% scores 14/0/0/8, and a fresh zero-bloat build drifts only -0.29% to +3.84% across 25k-1.6M rows); hash (9-11%) and SP-GiST (29-32%) score 100% only at a fixed row count because a fresh build drifts -20.31% to +49.42% and up to +33.08% respectively; GIN needs 50-75%; and BRIN must never fire, since `REINDEX` reclaimed exactly 0.00% in all 22 BRIN cells while the reading ranged -87.50% to +9900.00%. Documents why deletes make every access method read `1/(1-f)-1` with no index information at all, the GIN pending-list trap (a flush moved the reading from +261.73% to a *worse* +388.78% while delivering the entire 3,779 -> 1,336 block win the rebuild did not), hash's build-time bucket estimate (10.3% baseline spread on identical data), its splitpoint staircase and 87 unwritten pages that `pg_relation_size` counts but the filesystem never allocated, SP-GiST's `RecentGlobalXmin` placeholder gate, BRIN's `pages_per_range` 6.3x baseline spread and precision-not-size degradation, plus the stale-`reltuples` +84.71%/-7.64% inversion, the partial-index false negative (-5.02% reading against 44.59% reclaimable), and full `COMMENT ON INDEX` mechanics on the pin (literal-only grammar, `ShareUpdateExclusiveLock`, owner-only writes, world-readable, preserved by plain and CONCURRENTLY rebuilds, copied by `LIKE INCLUDING ALL`).

### Storage and Vacuum

- [How Much I/O a VACUUM FULL Performs on a Multi-GB, Near-Empty Heap in PostgreSQL 12 (unverified)](questions/storage-and-vacuum/vacuum-full-io-on-near-empty-heap.md) - **Now measured on a 12.2 server built from the pin**, on the asker's fixture: 200,000,000 rows loaded into 884,956 blocks (7,249,559,552 bytes, 226 tuples/page), 99.9% deleted with the physically last rows kept so `VACUUM` could not truncate, then `VACUUM` and `VACUUM FREEZE` leaving every one of the 884,956 pages all-visible and all-frozen at unchanged size. `VACUUM FULL` then read **all 884,956 blocks** with `heap_blks_hit` 0, and the block layer delivered **7,249,559,552 bytes** — the file size to the byte — in 2.15 s, while a plain `VACUUM` on the identical heap read **0 blocks** in 17 ms. Four runs agree byte-for-byte on `heap_blks_read`, `rchar` (7,250,034,354), `syscr` (885,019) and WAL (84,720). A warm page cache changed only `read_bytes` (0) and the clock, not the 884,956 reads. The extreme case — the same 6.75 GB heap with `vacuum_truncate = off` and **zero rows** — still read all 884,956 blocks to produce a **0-byte** new heap. Also measured: 885 `XLOG/FPI` records for 885 new pages under `pg_waldump` (7,377,944 bytes at `replica` against 84,720 at `minimal`); `heap_blks_scanned` reporting 0 of 884,956 for 49 of 51 samples and for all 94 samples on the rowless heap; the index rebuild reading only the new 885-block heap with `idx_blks_read` 0; `VACUUM FULL` at 2.05 s against a cost-delayed `VACUUM (DISABLE_PAGE_SKIPPING)` at 57.27 s over the same bytes; the old relfilenode left at 0 bytes with its higher segments already unlinked and removed at `CHECKPOINT`; and 226 of 226 line pointers retained per emptied page (`pd_lower` 928), which is why `pgstattuple` reported 88.53% free rather than ~100%. Source-only sections still cover: reads scale with the physical file (`RelationGetNumberOfBlocks`, one `ReadBufferExtended` per block, no visibility-map skip and no prefetch in `cluster.c`/`heapam_handler.c`/`rewriteheap.c`), while writes, WAL, fsync and index rebuilds scale with the surviving rows, and freeing the old multi-GB file is filesystem metadata work (first segment truncated with a deferred unlink, later segments unlinked at once, dirty buffers dropped unwritten). Includes the `raw_heap_insert` packing rule with a `floor(8168 / (MAXALIGN(t_len) + 4))` capacity derivation and a 4 GB worked example, the 32-buffer `BAS_BULKREAD` ring and its `StrategyRejectBuffer` escape, hint bits as the only old-heap write source, why no `vacuum_delay_point` throttles the rewrite, and the doc-versus-source conflict over "extra disk space approximately equal to the size of the table". The review corrected three substantive claims: `pg_stat_progress_cluster.heap_blks_scanned` is updated per returned tuple as `rs_cblock + 1` and never squared up, so it under-reports badly on exactly this shape (use `pg_statio_all_tables.heap_blks_read`); vacuumed-empty pages keep their line pointer arrays because `PageRepairFragmentation` never removes unused line pointers, so `PageGetMaxOffsetNumber` is not 0 and the scan walks the array skipping on `ItemIdIsNormal`; and the published monitoring statements needed a `BEGIN`/`COMMIT` wrapper, since top-level `SET LOCAL` only warns and leaves the query untimed.
- [How Reliable the Catalog-Statistics Table and TOAST Bloat Query Is in PostgreSQL 12 (unverified)](questions/storage-and-vacuum/catalog-statistics-bloat-query-reliability.md) - Reviews and scores the widely circulated `pg_stats`/`pg_class` bloat-estimate statement (the one with the `no_stats`, `null_headers`, `data_headers`, `estimates_with_toast` CTE chain) on an exact-pin 12.2 server, against exact `pgstattuple` and against a real `VACUUM FULL`. Verdict: a usable screen and a bad measurement. Over 21 scored fixtures the estimate lands within 1 point of the rewrite on 13 and averages 9.47 points of absolute error — slightly better than `pgstattuple`'s own `dead + free` reading (10.79) — but it reads **0 where a rewrite returns 50.00%** on a deleted-but-unanalyzed table, **−25.05** where `MAX(null_frac)` charges a whole extra aligned header per row, **−11.98** where TOAST chunks pack 6 to a page instead of 4, and **−9.00** on an inheritance parent whose `datawidth` doubles because `pg_stats` exposes both the inherited and non-inherited rows; and it invents **+21.00** points from column alignment padding, **+33.00** from page-packing remainders it does not floor, and **+31.00** from ignoring `fillfactor`. Documents the silent omissions (materialized views absent from `information_schema.columns`, partitioned parents absent from `pg_stat_user_tables`, `STATISTICS 0` and never-analyzed tables reduced to a `NULL` `pct_bloat` that the published `WHERE` clause can never pass), the case where the query beats the exact function (a dropped 500-byte column: 92 against a 92.42% rewrite where `pgstattuple` reads 6.01), the never-vacuumed TOAST relation whose `relpages = 0` makes a 31 MB table report `table_mb = 0.203`, the three incompatible size bases in one report, and the operational surface (one `AccessExclusiveLock` on any `pg_table_size`-branch table kills the whole statement at `lock_timeout`; a role with `SELECT` on 1 of 26 tables gets a clean-looking one-row report; 36 ms at 27 tables against 224 ms at 427, dominated by `information_schema.columns`, not by `pg_table_size`).
- [Measuring Heap and TOAST Bloat With pgstattuple_approx in PostgreSQL 12 (unverified)](questions/storage-and-vacuum/pgstattuple-approx-heap-and-toast-bloat.md) - One runnable statement that reports the main heap and its TOAST heap as separate rows, with physical size, live/dead tuples, free space, scanned percentage, the table's effective `fillfactor`, the `100 - fillfactor` reserve in bytes, and estimated bloat as dead space plus free space above that reserve. The TOAST row cannot come from `pgstattuple_approx` on this pin — it rejects `RELKIND_TOASTVALUE` — so the statement measures it with the exact `pgstattuple`, hard-codes fillfactor 100 because `heap_reloptions()` forces that value for TOAST relations, and keeps the `reltoastrelid <> 0` filter below the lateral call so a table with no TOAST relation cannot reach `could not open relation with OID 0`. Tested against `VACUUM FULL` on an isolated 12.2 server over ten fixtures: heap rows with real bloat read 1.8 to 4.0 points low, the requested formula is algebraically identical to the rewrite-matching ratio form at `fillfactor = 100` (12 of 12 rows agreed to the digit) but understates by **15.54 points** on a bloated `fillfactor = 70` table, and TOAST rows read 0.2 to 9.5 points high. The headline TOAST finding is that free space there is mostly chunk geometry: a never-deleted TOAST relation reported **19.58% free** with `VACUUM FULL` reclaiming 0.00%, reconciled to the byte from `TOAST_MAX_CHUNK_SIZE` = 1,996 (chunks 1,996/1,996/1,996/412, 1,604 free bytes per page), while a payload that is an exact multiple of the chunk size read **0.24%**. Also measures the cost split (45 buffer accesses for the heap branch against 10,006 — two per page — for a 5,000-page TOAST scan; 51x time and 690x physical reads between the two functions on the same 27 MB heap), the `pg_stat_scan_tables` privilege path, and the VACUUM warning in one table: a trailing delete let `VACUUM` truncate 16 MB to 8 MB while an interior delete of the same row count changed nothing until `VACUUM FULL`.

### Replication and WAL

- [How wal_sender_timeout Is Used and What It Impacts in PostgreSQL 12 (unverified)](questions/replication-and-wal/wal-sender-timeout.md) - Source-backed sender-silence timeout: v12 per-connection `PGC_USERSET` and live reload; process-local reply tracking; accepted messages; normal half-time keepalive plus logical-wait/shutdown exceptions; cooperative full-deadline termination; physical, client-tool, logical apply, table-sync catch-up, and `pg_recvlogical` paths; non-streaming exclusions; separate receiver controls; monitoring and synchronous-wait effects. The slot follow-up maps every `pg_replication_slots` field after timeout, contrasts persistent and temporary slots across timeout, EOF/error, orderly `CopyDone`, and crash recovery, and traces `restart_lsn`-based WAL retention through checkpoint/restartpoint removal, unreserved slots, on-disk state, `wal_keep_segments`, and archive boundaries; includes build/module boundaries, direct-test gaps, and adjacent slot tests. Fully re-reviewed at the 12.2 pin on 2026-07-29: all 364 citations re-resolved and label-checked, the `pg_stat_replication` superuser/`pg_read_all_stats` detail gate and the `immediately_reserve` default recorded, and one exact-pin server reproduced the reload-driven persistent-slot timeout with its client-visible closed connection, the temporary-slot drop, per-connection precedence over a lower configuration-file value, and the statistics privilege split.

### Observability

- [EXPLAIN ANALYZE BUFFERS Output in PostgreSQL 12 (unverified)](questions/observability/explain-analyze-buffers-output.md)
- [How pg_stat_statements Works and Which Settings Affect It in PostgreSQL 12 (unverified)](questions/observability/pg-stat-statements.md)

### Server Administration

- [PostgreSQL 12 Database Health Checklist (unverified)](questions/server-administration/database-health-checklist.md) - Deep source-backed checklist for monitoring access and snapshot semantics; client capacity, real blockers and prepared transactions; independent vacuum/analyze and XID/MultiXact risks; checkpoint/background-writer, WAL/archive/replication/slot, checksum, query, and relation signals; expanded critical log families; exact restart/reload/session scopes; and test/build boundaries. Fully re-reviewed at the 12.2 pin on 2026-07-29: all 345 citations re-resolved and label-checked (`ForwardSyncRequest` renamed from the nonexistent `ForwardFsyncRequest`, `analyze_rel` corrected from `do_analyze_rel`, swapped `auth.c` labels fixed, `hot_standby_feedback` moved to `config.sgml`, the progress-vacuum doc range widened to cover every column used, five recovery-conflict reasons cited at the collector, and per-GUC evidence added for nine replication settings). An exact-pin primary plus streaming standby reproduced the `pg_monitor` progress-field gate, a recovery conflict counted as `confl_snapshot`, the PID-zero prepared-transaction blocker with its retained locks, lock-wait/deadlock logging, temp-file accounting, and the `pg_settings`/`pg_stat_statements` redaction boundaries.
- [psql Environment Variables and Timeout Settings in PostgreSQL 12 (unverified)](questions/server-administration/psql-environment-variables-and-timeouts.md) - Lists psql-specific frontend variables, libpq connection/session environment variables inherited by `psql`, and the session impact of `statement_timeout` and `lock_timeout` via `SET`, `SET LOCAL`, and `PGOPTIONS`.
- [Extracting Declarative Range Partition Bounds With SQL and No Regex in PostgreSQL 12 (unverified)](questions/server-administration/extract-range-partition-bounds-without-regex.md) - PostgreSQL 12 stores a range bound only as the `pg_node_tree` in `pg_class.relpartbound`, whose raw text dumps each datum as bytes, so the one usable SQL form is the `pg_get_expr(relpartbound, oid)` deparse that `psql \d` prints and `pg_dump` pastes into `ATTACH PARTITION`. Establishes the grammar from `get_rule_expr`/`get_range_partbound_string`/`get_const_expr` — fixed `FOR VALUES FROM (`/`) TO (`/`, ` structure, bare `MINVALUE`/`MAXVALUE`, `DEFAULT` for the default partition, no `::type` and no `COLLATE` because `showtype = -1`, and `simple_quote_literal` quoting whose backslash handling follows `standard_conforming_strings` — then gives a regex-free recipe that masks literal contents after `string_to_array(bound_def, '''')` so `strpos`/`substr` can find the real separators, plus a gated `split_part` variant for provably quote-free bounds. Also covers which relations qualify (`relispartition` + `partstrat = 'r'`, `relkind` r/p/f, multi-level, cross-schema, DEFAULT rows, detached and index partitions excluded), casting the text back through the key type, why `boolean` and `char(3)` differ from their output text, and rejected alternatives (`relpartbound::text`, `pg_get_partition_constraintdef`, `pg_partition_tree`). Exact-pin measurements on an isolated 12.2 server: a from-scratch reconstruction of the deparse rules matched `pg_get_expr` on 41 of 41 partitions, the recipe scored 95 of 95 control elements against creation-time ground truth while the obvious split failed 13 elements and 9 of 41 partitions, `pg_get_expr(…, 0)` and `pretty = true` were identical on 45 of 45, the value round trip through the key type was exact in 78 of 81, and GUC sweeps, lock and privilege probes, a `REPEATABLE READ` drop race, 1000-partition timings, and the untoastable-`pg_class` size ceiling are all reported.
