# Wiki Index

This is the global catalog for the PostgreSQL engine wiki.

## Entry Points

- [versions](versions.md) - PostgreSQL version index and source pin manifest.
- [overview](overview.md) - Cross-version architecture overview.
- [log](log.md) - Chronological activity log.


## Version-Specific Pages

### PostgreSQL 19

- [v19/index](v19/index.md) - Active version landing page. Source checkout pinned to the post-`REL_19_BETA1` `master` commit `cdae794af31b3e9cfc323fc654292d86fa746f77` (v19 has no `REL_19_STABLE` branch yet).
- [PostgreSQL 19 Codebase Navigation Guide (unverified)](v19/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v19 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [How pg_plan_advice Works in PostgreSQL 19, and All Its Commits (unverified)](v19/questions/pg-plan-advice.md) - Comprehensive explanation of the new `pg_plan_advice` contrib module: the core `pgs_mask` strategy-mask and five planner hooks, per-index disabling for index-specific advice, the advice mini-language (tags, relation identifiers), plan-to-advice generation, advice enforcement, feedback/EXPLAIN output, the five `PGC_USERSET` GUCs, prepared-statement and plan-cache interaction (advice frozen at plan time; `always_store_advice_details` and `choose_custom_plan` re-plan policy plus relcache/syscache invalidation), the round-trip `test_plan_advice` harness, and the full scoped source history: 20 core planner foundation/enabling/fix commits, 22 direct module commits, and test/doc/build support commits.
- [How the REPACK Command Works in PostgreSQL 19, and Its 42 Feature-Scope Commits (unverified)](v19/questions/repack-command.md) - Comprehensive walkthrough of the new in-core `REPACK` / `REPACK (CONCURRENTLY)` command that absorbs `VACUUM FULL` and `CLUSTER`: the blocking new-heap rewrite/swap path, the concurrent online rewrite via logical decoding (decoding background worker, `pgrepack` output plugin, temporary replication slot, change spill/replay through the identity index, lock upgrade and relfilenode swap), CONCURRENTLY restrictions, the `PGC_POSTMASTER` `max_repack_replication_slots` GUC, `pg_stat_progress_repack`, I/O impact (no cost-based delay applies; automatic BULKREAD/BULKWRITE ring buffers), the `test_decoding`/injection-point tests, and 42 feature-scope commits.
- [PostgreSQL 19 Autovacuum and VACUUM: Parallel Workers, Table Scoring, and Setting Pages All-Visible During Reads (unverified)](v19/questions/autovacuum-parallel-scoring-visibility.md) - Comprehensive, commit-backed walkthrough of three independent v19 features: parallel autovacuum (the `PGC_SIGHUP` `autovacuum_max_parallel_workers` GUC defaulting to 0, the per-table `autovacuum_parallel_workers` reloption, the autovacuum-vs-`max_parallel_maintenance_workers` cap in `parallel_vacuum_compute_workers`, and the new `PVSharedCostParams` cost-delay propagation; index phases only), autovacuum table scoring (`AutoVacuumScores` filled by `relation_needs_vacanalyze`, the five `*_score_weight` GUCs with an all-zero escape hatch, the post-beta1 MXID-score division-by-zero guard `1f2297b5487`, the descending `do_autovacuum` sort, the `pg_stat_autovacuum_scores` view, and how it differs from the launcher's database-level `adl_score`), and read-only query scans setting pages all-visible in the visibility map via on-access pruning (`ScanRelIsReadOnly` -> `SO_HINT_REL_READ_ONLY` -> `heap_page_prune_opt` -> `HEAP_PAGE_PRUNE_SET_VM`, `PruneState`, the read-query no-extra-dirtying/FPI safety valve, and the VACUUM VM-skip that reduces future work); includes per-feature source-commit history.



### PostgreSQL 18

- [v18/index](v18/index.md) - Primary version landing page. Source checkout pinned to `REL_18_STABLE` commit `6cb307251c5c6261286c1566496920976640108e`.
- [PostgreSQL 18 Codebase Navigation Guide (unverified)](v18/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v18 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 18 (unverified)](v18/questions/create-index-concurrently.md) - Comprehensive walkthrough of the v18 `CREATE INDEX CONCURRENTLY` implementation: parser/utility dispatch, restrictions, four internal transactions, two heap scans, three waits, table and session `ShareUpdateExclusiveLock`, `indislive`/`indisready`/`indisvalid` state progression, invalid-index failure outcomes, tests, and changes from PostgreSQL 17 (`pg_index` TOAST snapshot handling, parallel GIN build, virtual-generated-column rejection, and temporal `WITHOUT OVERLAPS` shared index-definition plumbing).
- [How Bi-Directional Logical Replication Works in PostgreSQL 18, and New Logical Replication Features Since PostgreSQL 17 (unverified)](v18/questions/bidirectional-logical-replication.md) - Explains how two v18 nodes replicate to each other without loops: mutual `origin = none` subscriptions, apply-worker origin tagging into WAL, `pgoutput`/decode-time origin filtering, the `copy_data` initial-sync WARNING, setup pattern, and limitations — including the new v18 conflict detection layer (`update_origin_differs`/`delete_origin_differs` logged with the other node's origin and counted in `pg_stat_subscription_stats`, regression-tested in the bidirectional TAP test); plus a section covering every logical replication feature new since v17, verified in source: conflict logging/statistics, generated-column replication (`publish_generated_columns` and column lists), the `streaming = parallel` default, alterable `two_phase`, the `max_active_replication_origins` (restart) and `idle_replication_slot_timeout` (reload) GUCs, `pg_createsubscriber` `--all`/`--clean`/`--enable-two-phase`, `pg_recvlogical` `--enable-failover`/`--drop-slot` changes, and the `pg_logicalinspect` contrib module.
- [Computing and Storing avg_leaf_density During (Auto)VACUUM of a B-Tree Index (unverified)](v18/questions/avg-leaf-density-during-vacuum.md) - Reviewed design and evidence-backed cons for computing `pgstatindex`-style `avg_leaf_density` during B-tree VACUUM, including metapage/statistics storage, scan-skip caveats, and empty-page deletion accuracy gaps.
- [How Custom Cumulative Statistics Work in PostgreSQL 18 (unverified)](v18/questions/custom-cumulative-statistics.md) - Explains custom cumulative statistics registration, variable and fixed custom stat storage, flushing, snapshots, reset/drop behavior, and clean-shutdown persistence in PostgreSQL 18.
- [EXPLAIN ANALYZE BUFFERS Output in PostgreSQL 18 (unverified)](v18/questions/explain-analyze-buffers-output.md) - Detailed field-by-field explanation of `EXPLAIN (ANALYZE, BUFFERS)` shared, local, temp, planning, serialization, parallel-worker, and I/O timing output in PostgreSQL 18.
- [Extension Hooks for VACUUM and Autovacuum in PostgreSQL 18 (unverified)](v18/questions/extension-hooks-vacuum-autovacuum.md) - Catalogs every extension point on the VACUUM/ANALYZE and autovacuum paths: in-process hook variables, table and index AM and FDW callbacks, and adjacent surfaces (background workers, custom cumulative statistics), with manual-vs-autovacuum coverage.
- [GUC Default-Value Changes Since PostgreSQL 12 (unverified)](v18/questions/guc-default-changes-since-v12.md) - Summarizes the GUC settings present in both PostgreSQL 12 and 18 whose defaults changed: seven changes from v13-v15, the v18 `effective_io_concurrency` default increase to 16, and the v18 `log_connections` type/default-spelling change that preserves disabled-by-default behavior.
- [Why pgstatindex Cannot Use pgstattuple_approx-Style Approximation in PostgreSQL 18 (unverified)](v18/questions/pgstatindex-approx-sampling.md) - Explains why `pgstattuple_approx`'s heap visibility-map/FSM shortcut does not transfer to B-tree `pgstatindex`, scopes a separate approximate diagnostic, and catalogs current `pgstatindex` regression coverage.
- [Proposing a Sampling pgstatindex Variant for PostgreSQL 18 (unverified)](v18/questions/pgstatindex-sample-variant-proposal.md) - Designs a `pgstatindex_approx` that random-samples physical index blocks instead of reading all of them; covers the exact-vs-estimated field split, the ratio-vs-count estimator, extension wiring, a `pageinspect` SQL prototype, and pros/cons.
- [How pg_stat_statements Works and Which Settings Affect It in PostgreSQL 18 (unverified)](v18/questions/pg-stat-statements.md) - Explains the shared-preload extension's query-ID, normalization, hook, shared-hash, query-text file, readout, reset/save, permissions, and configuration behavior in PostgreSQL 18.
- [Limiting Query Text Size in pg_stat_statements in PostgreSQL 18 (unverified)](v18/questions/pg-stat-statements-query-text-size.md) - There is no v18 GUC to cap individual query-text length; `track_activity_query_size` does not apply, texts store full-length in the external file, and the only levers are `pg_stat_statements.max`, the wholesale-discard garbage collector, and read-time `showtext`/`left()`.
- [Row-Level Security (RLS) in PostgreSQL 18: Implementation, Performance, Settings, and Fixes Since PostgreSQL 12 (unverified)](v18/questions/row-level-security-rls.md) - Explains RLS as rewrite-time security quals and write checks, policy storage and DDL, bypass decisions, planner/executor effects, partitions/views/COPY/logical replication boundaries, scalability and performance mechanisms, all RLS-related settings and catalog/tool surfaces, and RLS-related fixes since PostgreSQL 12 whose commit provenance is verified against the now-unshallowed checkout (hashes, subjects, and ancestry from the v13 development stamp to the pin; completeness probed by history search), now with per-branch minor-release first-appearance labels traced from each fix's distinct cherry-picks and release tags (including the finding that two listed hashes are REL_18_STABLE back-patches whose master commits are on the unreleased v19 line).
- [How track_activity_query_size Is Used in PostgreSQL 18 (unverified)](v18/questions/track-activity-query-size.md) - Explains how the postmaster-context GUC sizes backend activity-string shared memory, caps `pg_stat_activity.query`, interacts with `track_activities`, feeds deadlock/crash activity reporting, and does not truncate `pg_stat_statements.query`.
- [Usage of NUM_BUFFER_PARTITIONS in PostgreSQL 18 (unverified)](v18/questions/num-buffer-partitions.md) - Explains the shared buffer mapping hash partition count, `BufferMapping` LWLocks, normal lookup/allocation and invalidation paths, relation extension, wait events, test-coverage gap, and the full source commit history (16->128) in PostgreSQL 18.



### PostgreSQL 17.10

- [v17/index](v17/index.md) - Active version landing page. Source checkout pinned to `REL_17_STABLE` commit `54eeefaedbee0385529f3edf321bb99e49232aaa`.
- [PostgreSQL 17 Codebase Navigation Guide (unverified)](v17/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v17 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [Pros and Cons of Partial Indexes in PostgreSQL 17 (unverified)](v17/questions/partial-indexes-pros-cons.md) - Explains partial-index benefits, tradeoffs, ordinary expression-index behavior, partial indexes on expressions, planner predicate implication and expression-key matching, operational restrictions, `ANALYZE` row-count and expression-statistics behavior, planner costing, regression coverage, and what changed from PostgreSQL 12 (documentation warning, concurrent-build safe-wait exclusion, MERGE target rechecks, expression statistics, `NULLS NOT DISTINCT`, partial-unique proof restriction, and partial-hash planning fix).
- [PostgreSQL 17 Contrib Extensions (unverified)](v17/questions/contrib-extensions.md) - Lists all 53 control-file-backed contrib extensions in PostgreSQL 17, with cited explanations covering install scope, data types, index/search helpers, diagnostics, FDWs, procedural-language transforms, and SPI trigger examples.
- [Proposing a Sampling pgstatindex Variant for PostgreSQL 17 (unverified)](v17/questions/pgstatindex-sample-variant-proposal.md) - Designs a `pgstatindex_approx` that random-samples physical B-tree blocks; covers exact-vs-estimated fields, pros/cons, extension wiring, and a `pageinspect` SQL prototype using PostgreSQL 17 source behavior.
- [GUC Default-Value Changes Since PostgreSQL 12 (unverified)](v17/questions/guc-default-changes-since-v12.md) - Summarizes the seven GUCs present in both v12 and v17 whose built-in default changed (with old/new value, introducing major version, and restart/reload/session apply scope), verified against v17 `guc_tables.c`/`config.sgml`/`postgresql.conf.sample` and pinned to introducing versions via the v17 checkout's own commit history; separates out sample-only, type, added/removed-setting, and range-only changes that are not default-value changes, with explicit test-coverage notes.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 17 (unverified)](v17/questions/create-index-concurrently.md) - Reviewed walkthrough of the v17 `CREATE INDEX CONCURRENTLY` implementation in `DefineIndex` (four internal transactions, two heap scans, three transaction waits; the `indislive`/`indisready`/`indisvalid` progression using v17's transactional `CatalogTupleUpdate` path in `index_set_state_flags`; preconditions/restrictions and the invalid-index failure path); a dedicated steps-and-locks section (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, and `WaitForOlderSnapshots`, with the `LockConflicts`/`WaitForLockersMultiple` rationale); and a "what changed from PostgreSQL 12" section: the PG14 `PROC_IN_SAFE_IC` optimization (safe non-partial/non-expression builds advertise via `set_indexsafe_procflags` and are ignored only in the snapshot wait `WaitForOlderSnapshots`, not the lock-based `WaitForLockers`), transactional `index_set_state_flags` (`83158f74d3a`), the added-then-reverted VACUUM-ignores-CIC attempt (so VACUUM still holds CIC's horizon in v17), the `PGXACT`->`PGPROC` `statusFlags` move, and the owner/restricted-search-path build hardening — anchored to the v17 checkout's own commit history (`c98763bf`, `f9900df5`, `e28bb885`, `83158f74d3a`, `5788e258`), with regression and `multiple-cic`/`prepared-transactions-cic` isolation coverage.
- [How REINDEX INDEX CONCURRENTLY Is Implemented in PostgreSQL 17 (unverified)](v17/questions/reindex-index-concurrently.md) - Comprehensive walkthrough of the v17 `REINDEX INDEX CONCURRENTLY` implementation in `ReindexRelationConcurrently`: the six phases (create the `_ccnew` copy via `index_concurrently_create_copy`, build, validate, swap, set-dead, drop) run across many short transactions, each phase looping all indexes; the five waits — two `WaitForLockersMultiple(ShareLock)` writer waits, `WaitForOlderSnapshots` (which now ignores safe concurrent builds via `PROC_IN_SAFE_IC`), and the two extra `WaitForLockersMultiple(AccessExclusiveLock)` reader waits before set-dead and drop that CIC lacks; the atomic phase-4 `index_concurrently_swap` (new valid + old invalid flipped together with the transactional `CatalogTupleUpdate`, `_ccnew`->original / original->`_ccold` rename, constraints/triggers/comment/dependencies and `pgstat_copy_relation_stats`/`CopyStatistics` moved); `ShareUpdateExclusiveLock` on heap, old index, and new index at both transaction and session level; dispatch via `ExecReindex`/`ReindexParams`, the restriction set (transaction block, recovery, temp fallback, system catalogs, exclusion, invalid-allowed-when-named), the `indislive`/`indisready`/`indisvalid` progression on both indexes, and a per-phase failure table establishing that a healthy `index_name` is never left invalid (only the differently-named `_ccnew` pre-swap or `_ccold` post-swap is, with planner/relcache/executor handling of invalid-but-ready leftovers), and a dedicated "can a failure leave an invalid index with the original index name?" section that answers the bloat-to-no-index worry directly — a RIC failure never converts a bloated-but-valid `index_name` into no usable index because the phase-4 swap renames the rebuilt copy to `index_name` and flips validity in one transaction, so `index_name` is invalid only when it was already invalid before the run (the `concur_reindex_ind5` repair regression test). Includes a detailed "what changed from PostgreSQL 12" section anchored to the checkout's own commit history: partitioned REINDEX (incl. CONCURRENTLY) now supported via `ReindexPartitions`->`ReindexMultipleInternal` leaf recursion (`a6642b3ae06`, v14; v12 errored "not yet implemented for partitioned indexes"), `REINDEX (TABLESPACE ...)` (`c5b286047cd`, v14), `index_set_state_flags` made transactional (`83158f74d3a`, v14, replacing v12's `heap_inplace_update`), `PROC_IN_SAFE_IC` extended to RIC (`f9900df5`, v14), the progress-view `WAIT_5` "waiting for readers before dropping" fix (`e66bcfb4c66`, v14; v12 wrongly reused `WAIT_4`), the v15 shared-memory statistics rework (`8ea7963fc74`), and the v17 `MAINTAIN` privilege (`ecb0fd33720`); with regression (`create_index.sql`, including partitioned and repair cases) and `reindex-concurrently` isolation coverage.
- [How Bi-Directional Logical Replication Works in PostgreSQL 17, and All Related Commits by Minor Version (unverified)](v17/questions/bidirectional-logical-replication.md) - Explains how two v17 nodes replicate to each other without loops: subscriptions created with `origin = none` make the publisher's `pgoutput` origin filter skip WAL changes that carry a replication origin ID, which is exactly the tag every apply worker stamps on the transactions it replays; covers the subscription `origin` parameter and `pg_subscription.suborigin`, WAL origin tagging (`XLOG_INCLUDE_ORIGIN` -> `XLogRecordAssemble`), decode-time filtering (`pgoutput_origin_filter`, `DecodeTXNNeedSkip`), the initial-sync `copy_data`/`origin = none` WARNING, setup pattern, and limitations (manual conflicts, no DDL/sequence replication); plus the complete related commit history grouped by minor version: 16-cycle feature commits, the 17.0 filter-state fix, the 17.5 partition-aware WARNING fix, and the 17.10 CVE-2026-6638 SQL-injection fix, with foundations, incidental, and adjacent commits listed separately.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 17? (unverified)](v17/questions/attach-partition-index-drops.md) - Answers that `ALTER TABLE ... ATTACH PARTITION` never drops an index from the table being attached: `AttachPartitionEnsureIndexes` is strictly match-or-create — for each partitioned parent index it re-parents a compatible existing partition index via `IndexSetParentIndex` (kept, not rebuilt) or creates a cloned one via `DefineIndex`, while incompatible/extra/invalid child indexes are left in place (regression-proven by the `indexing` test showing four incompatible indexes retained plus one added). Distinguishes the three look-alikes that are not ATTACH drops (a re-parented index becomes `relispartition` and can no longer be dropped on its own; the documented manual drop of the now-redundant CHECK constraint; a later cascading `DROP INDEX` on the parent). Includes a version-by-version "changes since PostgreSQL 12" section: the no-drop invariant is unchanged in every release, with per-version entries for v13 (attmap refactor, error-code fix), v14 (error wording, dedicated `DROP INDEX CONCURRENTLY`-on-partitioned-index error), v15 (`NULLS NOT DISTINCT` joins the matching rules, partitioned-index DROP deadlock fix), v16 (invalid-index match skip `fc55c7ff`, back-patched to v11+ so it post-dates the pinned 12.2; `DefineIndex` `total_parts`), v17.0 (same-constraint-type match `cee8db3f` stopping a PRIMARY KEY matching a plain UNIQUE, `CompareIndexInfo` expression-vs-column fix, `validatePartitionedIndex` syscache-copy fix, `relhassubclass` locking), and the 17.x stable update `becf6d2696` that lets re-attaching an already-attached index validate a still-invalid parent — anchored to the v17 checkout's source and commit history, with first-release attribution bracketed by the "Stamp HEAD as NNdevel" commits.



### PostgreSQL 14.23

- [v14/index](v14/index.md) - Active version landing page. Source checkout pinned to the `REL_14_STABLE` tip commit `5c00f4e2e3bcee6931ae93429d53f7c2a4f46156` (`REL_14_23-3-g5c00f4e2e3b`, three commits past the 14.23 release stamp; source version 14.23).
- [PostgreSQL 14 Codebase Navigation Guide (unverified)](v14/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v14 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [Row-Level Security (RLS) in PostgreSQL 14: Implementation, Scalability and Performance, and Settings (unverified)](v14/questions/row-level-security-rls.md) - Explains RLS as a query-rewrite feature: `pg_policy` storage and the `relrowsecurity`/`relforcerowsecurity` flags, relcache build into `rd_rsdesc`, the `check_enable_rls` bypass decision (RLS_NONE / RLS_NONE_ENV / RLS_ENABLED, superuser/`BYPASSRLS`/owner/`FORCE`), `get_row_security_policies` producing security-barrier `USING` quals and `WITH CHECK` options, permissive-OR/restrictive-AND combination with an always-false default-deny, command/role matching, the planner `security_level`/leakproof qual-ordering mechanism, executor `ExecWithCheckOptions` enforcement, plan-cache `dependsOnRLS` re-planning on role/`row_security` change, partition/inheritance handling (only parent policies apply), and the COPY/CTAS/RI/error-message-leak boundaries; the source-evident scalability and performance issues (per-row qual evaluation, leakproof ordering that can block index use, subqueries in policies, qual-tree growth with many policies, plan-cache churn, aggregate statements consuming RLS-filtered subplans with mitigations around indexable policies, partition pruning/partitionwise aggregation, stable plan-cache environments, and trusted summary/reporting paths, FK bulk-check downgrade and related per-row `FOR KEY SHARE`/MultiXact risk, partition fan-out); a query-vs-policy planning subsection answering that you need not restate RLS policy conditions in the query (policy `USING` quals are transferred into `baserestrictinfo` at the lowest security level and are securely promotable, so the planner already uses them for index scans and partition pruning, while query-text copies land at the maximum security level and help only if leakproof); a subsection on wrapping a function call in a scalar sub-SELECT (e.g. `(SELECT auth.uid())` for a `STABLE` JWT helper) so the planner turns it into an InitPlan evaluated once per query and cached in a `PARAM_EXEC` parameter, instead of re-invoking a per-row function filter — with the caveats that only uncorrelated sub-SELECTs become InitPlans, that an index-qualifiable comparison already evaluates a `STABLE` function once per scan, and that it is a once-per-execution cache, not a general memoization; a dedicated RLS-and-the-plan-cache section on how query planning caches RLS work (the plan cache keyed on role + `row_security` via `dependsOnRLS`/`rewriteRoleId`/`dependsOnRole`, and the role-independent relcache `rd_rsdesc` policy cache), the scenarios where caching does not help (role or `row_security` change, the never-applicable simple-validity fast path, un-cached simple-protocol and one-shot SPI queries, first-five custom plans, `plan_cache_mode` forced custom/generic choices, SQL `DECLARE CURSOR` planning outside the saved plan cache, and policy/table DDL invalidation), and the no-cache re-analyze/rewrite/re-plan cost; and all related settings (the `row_security` session GUC and pg_dump default, the `plan_cache_mode` session GUC, the `ALTER TABLE` ENABLE/DISABLE/FORCE/NO FORCE flags, the `BYPASSRLS` role attribute, the `CREATE`/`ALTER POLICY` options with their PERMISSIVE/ALL/PUBLIC defaults, and the two extension hooks).
- [Performance Implications of Functions and Procedures in a WHERE Clause in PostgreSQL 14, and How to Minimize the Overhead (unverified)](v14/questions/functions-procedures-in-where-clause.md) - Explains that a procedure (`prokind='p'`) cannot appear in a WHERE clause (the parser rejects it via `ParseFuncOrColumn`; procedures run only through the `CALL` utility statement), and that row-dependent or volatile functions in a WHERE clause are per-row costs driven by the `ExecScan` qual loop (`ExecQual` -> `EEOP_FUNCEXPR` fmgr dispatch once per row reaching the expression after earlier qual checks), while a no-`Var`, non-volatile qual can be pulled into a one-time gating `Result` filter; covers volatility (`VOLATILE` re-evaluates every row where needed, `STABLE` is optimized to a single evaluation only as an index-scan comparison value/runtime key or no-`Var` one-time qual, `IMMUTABLE` is constant-folded when given constant arguments), the cost model (`procost * cpu_operator_cost`, with omitted `CREATE FUNCTION COST` defaulting to 1 for C/internal functions and 100 otherwise), fixed default selectivity for function quals (0.3333333 for a bare boolean function, 0.5 for other boolean expressions), expression indexes/generated columns and their immutability requirements, leakproof/security-barrier qual ordering, and parallel-safety effects; and the mitigations (label volatility correctly, add an expression index or stored generated column, use an uncorrelated scalar sub-SELECT when once-per-execution semantics are intended, including RLS policy expressions such as `(SELECT auth.uid()) = user_id` after policy sublinks flow through planner preprocessing, inline simple SQL functions, set accurate `COST`/`ROWS`, attach a planner `SUPPORT` function, mark `LEAKPROOF`/`PARALLEL SAFE` when truthful, and arrange cheaper selective indexable quals to filter rows first).
- [How MultiXact Works in PostgreSQL 14, and How Foreign Keys and Other Operations Degrade Performance When the Local Cache Spills to Secondary Storage (unverified)](v14/questions/multixact-foreign-keys-cache-spill.md) - Explains MultiXact as the shared-row-lock mechanism: a `MultiXactId` in a tuple's `xmax` standing for a set of `(TransactionId, MultiXactStatus)` members, the two SLRUs (offsets/members) backed by `pg_multixact/`, the shared `MultiXactStateData` and per-backend oldest-member/oldest-visible arrays, the create path (`MultiXactIdCreate`/`MultiXactIdExpand` -> `MultiXactIdCreateFromMembers` -> `GetNewMultiXactId`/`RecordNewMultiXact`, writing offsets then members under exclusive SLRU locks), and the read path (`GetMultiXactIdMembers`); the backend-local 256-entry, per-transaction LRU cache (`mXactCacheEnt`, `MAX_CACHE_ENTRIES`, the `MXactContext` under `TopTransactionContext`, `mXactCachePut` tail eviction, and no cross-backend sharing) and how a lookup that the cache cannot answer falls back through the tiny 8-offset/16-member SLRU pool to the on-disk `pg_multixact/` segment files (`SlruPhysicalReadPage` -> `pg_pread` -> the `SLRURead` wait); how foreign keys create MultiXacts (RI check `SELECT ... FOR KEY SHARE OF x` -> `heapam_tuple_lock` -> `heap_lock_tuple` -> `compute_new_xmax_infomask`, with the hot-parent-row escalation and the `fk-contention` isolation test) plus the other creators (compatible explicit row locks, UPDATE/DELETE of already-locked rows, and VACUUM MultiXact freezing); the three stacked degradation paths (local-cache spill to secondary storage; SLRU control-lock/per-buffer/I/O contention on MultiXact SLRUs; and monotonic member-space growth driving aggressive anti-wraparound vacuum via `MultiXactMemberFreezeThreshold` and, at the limit, the `multixact "members" limit exceeded` error); observability through `pg_stat_slru`, `pg_stat_activity` wait events, `mxid_age()`, and `pg_get_multixact_members()`; and the vacuum GUCs plus per-table storage parameters with apply scopes (`vacuum_multixact_freeze_min_age` and `vacuum_multixact_freeze_table_age` session-scoped, `autovacuum_multixact_freeze_max_age` restart-only, per-table `autovacuum_multixact_freeze_*` reloptions via `ALTER TABLE`), noting v14 has no GUC to resize the SLRU buffers or the local cache.



### PostgreSQL 12.2

- [v12/index](v12/index.md) - Legacy version landing page. Source checkout pinned to `REL_12_STABLE` commit `45b88269a353ad93744772791feb6d01bc7e1e42`.
- [PostgreSQL 12 Codebase Navigation Guide (unverified)](v12/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v12 source tree: top-level and backend makefile layout, simple and extended SQL protocol flow through `tcop`/parser/analyzer/rewrite/planner/executor, utility-command dispatch, generated catalog/parser/GUC/header artifacts, key structs, contrib and core extension-command boundaries, and test/doc surfaces.
- [How NULL Values Are Handled in PostgreSQL 12 Indexes (unverified)](v12/questions/null-values-in-indexes.md) - Explains NULL handling by index access method: B-tree storage, ordering, `IS NULL` search, and unique-index semantics; hash NULL-entry skipping; GiST tuple NULL markers; SP-GiST's separate NULL tree; GIN NULL categories and lack of generic `NullTest` matching; BRIN `hasnulls`/`allnulls` range summaries; and contrib Bloom's no-NULL-search behavior.
- [Pros and Cons of Partial Indexes in PostgreSQL 12 (unverified)](v12/questions/partial-indexes-pros-cons.md) - Explains partial-index benefits, tradeoffs, ordinary expression indexes as the baseline, partial indexes on expressions, planner predicate implication and expression-key matching, maintenance and operational restrictions, `ANALYZE` row-count and expression-statistics behavior, and how the planner costs partial indexes.
- [Foreign-Key Join Optimization for Two-Table Joins (unverified)](v12/questions/fk-join-optimization-two-tables.md) - How the v12 planner uses foreign-key constraints when joining two tables.
- [Query Planner Statistics Sources in PostgreSQL 12 (unverified)](v12/questions/query-planner-statistics-sources.md) - Explains that the v12 planner does not use `pg_stat_all_tables`, `pg_stats`, or `pg_stats_ext` directly; it uses `pg_class` relation-size stats, `pg_statistic` column stats, extended statistics catalogs, index/FK/constraint metadata, and explicit planner statistics hooks, with regression coverage notes.
- [EXPLAIN ANALYZE BUFFERS Output in PostgreSQL 12 (unverified)](v12/questions/explain-analyze-buffers-output.md) - Detailed field-by-field explanation of `EXPLAIN (ANALYZE, BUFFERS)` shared, local, temp, and I/O timing output in PostgreSQL 12.
- [How pg_stat_statements Works and Which Settings Affect It in PostgreSQL 12 (unverified)](v12/questions/pg-stat-statements.md) - Explains the v12 extension's in-extension query jumbling, hooks, shared-hash and external query-text storage, readout, permissions, persistence, and its four GUCs (`max`, `track`, `track_utility`, `save`) plus `shared_preload_libraries` and `track_io_timing`.
- [PostgreSQL 12 Database Health Checklist (unverified)](v12/questions/database-health-checklist.md) - Production-oriented checklist for activity, connections, locks, database and table counters, vacuum and wraparound, checkpoints, archiving, replication, checksums, query statistics, relation diagnostics, and database-log findings, including required extensions and exact restart/reload/session configuration scopes.
- [psql Environment Variables and Timeout Settings in PostgreSQL 12 (unverified)](v12/questions/psql-environment-variables-and-timeouts.md) - Lists psql-specific frontend variables, libpq connection/session environment variables inherited by `psql`, and the session impact of `statement_timeout` and `lock_timeout` via `SET`, `SET LOCAL`, and `PGOPTIONS`.
- [How wal_sender_timeout Is Used and What It Impacts in PostgreSQL 12 (unverified)](v12/questions/wal-sender-timeout.md) - Explains the sender-side replication timeout: GUC scope/default/disable value, reply tracking in `WalSndLoop`, half-time keepalives and full-time disconnect, physical and logical streaming paths, publisher-side impact on logical subscriptions and `pg_recvlogical`, differences from subscriber-side receiver settings, and effects on monitoring, synchronous replication, and slot WAL retention.
- [How pgstatindex Calculates B-Tree Index Statistics in PostgreSQL 12 (unverified)](v12/questions/how-pgstatindex-calculates-information.md) - Explains the exact v12 `pgstatindex` calculation path: SQL surface, access checks, metapage fields, full physical page scan, page classification, `avg_leaf_density`, `leaf_fragmentation`, edge cases, and regression coverage gaps.
- [Planner Penalties for Bloated Indexes in PostgreSQL 12 (unverified)](v12/questions/bloated-indexes-query-planner.md) - Explains how v12 penalizes B-tree bloat indirectly through physical index pages, B-tree height, cache/page-cost modeling, and executor leaf-page walking; scopes the mechanisms across access methods via the `amcostestimate` dispatch (page-count penalty shared by B-tree/hash/GiST/SP-GiST through `genericcostestimate()`; measured tree-height penalty B-tree-only, with GiST/SP-GiST charging the same descent formula on a page-count-estimated height and hash charging no descent cost; GIN/BRIN use separate models); expands how `RelationGetNumberOfBlocks()` obtains `index->pages` through storage-manager file-size probes rather than index-page scans; adds a proposed `avg_leaf_density` / `leaf_fragmentation` triage heuristic for REINDEX candidates.
- [Impact of B-Tree Leaf Density (60% vs 90%) on Index Scan Queries in PostgreSQL 12 (unverified)](v12/questions/leaf-density-60-vs-90-query-impact.md) - Planner page-count scaling via `index->pages`, executor leaf walking through `_bt_steppage` / `_bt_readnextpage` / `_bt_getbuf`, buffer effects, test coverage gaps, and point-vs-range query impact for 60% vs 90% `avg_leaf_density`.
- [B-Tree Leaf Density vs Fragmentation Impact on Index Scan I/O in PostgreSQL 12 (unverified)](v12/questions/leaf-density-vs-fragmentation-index-scan-io.md) - Comprehensive comparison of density-driven leaf-page count growth versus fragmentation-driven physical-order penalties, with density/fragmentation level estimates, combined I/O multipliers, planner visibility, executor leaf walking, and cache/storage sensitivity.
- [Proposing a Sampling pgstatindex Variant for PostgreSQL 12 (unverified)](v12/questions/pgstatindex-sample-variant-proposal.md) - Designs a `pgstatindex_approx` that random-samples physical B-tree blocks in PostgreSQL 12; covers exact-vs-estimated fields, pros/cons, extension wiring, and a contrib `pageinspect` SQL prototype with a v12 `bt_metap` unsigned-`oldest_xact` overflow workaround.
- [Finding and Prioritizing Bloated B-Tree Indexes for REINDEX in PostgreSQL 12 (unverified)](v12/questions/index-bloat-reindex-heuristic.md) - Proposes a three-stage REINDEX triage heuristic: a no-page-I/O shortlist from `pg_class`, `pg_stat_*`/`pg_statio_*`, and `pg_relation_size()` (with `idx_scan` counting semantics and `pg_class` staleness caveats), exact confirmation via gated `pgstatindex` runs, and a wasted-bytes-times-usage priority score with v12 `REINDEX` / `REINDEX CONCURRENTLY` execution and monitoring notes; adds a post-REINDEX measurement method covering size and `pgstatindex` shape deltas, per-scan counter rates (counters survive both REINDEX forms in v12, including the `index_concurrently_swap` stats copy), and `EXPLAIN (ANALYZE, BUFFERS)` / `pg_stat_statements` 1.7 before/after windows with `track_io_timing` scope notes; also explains why `leaf_fragmentation` is excluded from the priority score (planner adjacency-blindness via tablespace `random_page_cost`, no byte value, metric coarseness, narrow conditional runtime impact, and free repair via density-triggered sorted rebuilds).
- [Comprehensive plan_cache_mode Analysis in PostgreSQL 12 (unverified)](v12/questions/plan_cache_mode_analysis.md) - Detailed analysis of the `plan_cache_mode` GUC, including the cost-based heuristic for generic vs custom plans, decision hierarchy, and planning cost estimation in PostgreSQL 12.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](v12/questions/create-index-concurrently.md) - Comprehensive walkthrough of the v12 `CREATE INDEX CONCURRENTLY` implementation in `DefineIndex`: the four internal transactions, two heap scans, and three transaction waits; the `indislive`/`indisready`/`indisvalid` state-flag progression set non-transactionally by `index_set_state_flags`; the preconditions/restrictions (no transaction block, temp fallback, partitioned/system-catalog/exclusion bans); the failure path that leaves an invalid index; and a dedicated section mapping every step to the table locks (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, and `WaitForOlderSnapshots`), with the `LockConflicts` rationale for why DML proceeds while another CIC/`VACUUM`/`ANALYZE`/DDL is blocked, plus regression and `multiple-cic` isolation test coverage, now with a resolved inter-builder trace showing same-table CIC serialization by `ShareUpdateExclusiveLock`, cross-table CIC/RIC old-snapshot waits, and the no-`xmin` final wait boundary, and a line-by-line first-build-scan tuple-visibility trace into the heap AM (`ii_Concurrent` selects a fresh MVCC snapshot over `SnapshotAny`, so the first scan omits `RECENTLY_DEAD` tuples and never sets `ii_BrokenHotChain`), and a resolved assessment that skipping prepared transactions in the two `WaitForLockers(ShareLock)` writer waits is not sufficient for index correctness (a prepared write that commits after the build is left unindexed, because the MVCC scans skip in-progress tuples, all three waits skip the prepared xact's invalid-VXID dummy proc, and `COMMIT PREPARED` does no index maintenance). Also enumerates every operation that can block CIC at each of its four blocking points: conflicting-lock holders at the initial `ShareUpdateExclusiveLock` acquisition (mapped to v12 commands via the `mvcc.sgml` lock table, with the autovacuum SIGINT-cancel/anti-wraparound exception and prepared-transaction lock retention), open writer transactions at the two `WaitForLockers(ShareLock)` waits (idle-in-transaction included; lock waiters, later writers, and prepared transactions skipped), and same-database old-snapshot holders at `WaitForOlderSnapshots` (long-running queries, REPEATABLE READ/SERIALIZABLE transactions, cursor-holding idle sessions, manual `ANALYZE`, other CICs; autovacuum/lazy-VACUUM, other databases, xmin-0 backends, and prepared transactions excluded), with the `pg_stat_progress_create_index` wait phases, and worked examples for a running `pg_dump` (blocks only the snapshot wait, for the dump's whole duration, timeouts disabled) and an hour-long open / idle-in-transaction session (blocks per what it did: target-table writes hit the writer waits, pinned snapshots hit the snapshot wait, reads-only `READ COMMITTED` sessions clear their xmin via `SnapshotResetXmin` and block nothing). Adds a comprehensive failure-scenarios section: which internal transaction was running fixes the ordinary ERROR/cancel outcome — no index (failure before the first commit), an invalid not-ready index (build-scan failure such as a pre-existing duplicate caught by the unique build sort), or an invalid ready index that still takes writes and enforces uniqueness (validation/second-scan failure) — with the planner/executor/HOT/uniqueness cost of each leftover, the fact that ordinary ERROR/cancel failures release (do not leak) the session lock, the resolved crash/immediate-shutdown recovery outcome (below), regression evidence (a failed unique build left `INVALID` and retained through `VACUUM FULL`), and `DROP INDEX` / `REINDEX INDEX CONCURRENTLY` recovery, and a resolved walsender/replication-slot Wait 3 assessment (a `hot_standby_feedback` standby and any replication slot hold the dead-row removal horizon through `GetOldestXmin`/`RecentGlobalXmin`, not a per-backend xmin, so they never enter `WaitForOlderSnapshots`; a physical walsender is additionally excluded by database and by the valid-VXID gate, and only a logical walsender mid-`REPEATABLE READ` snapshot export is eligible — as an ordinary in-database transaction), and a resolved crash / immediate-shutdown analysis (the `index_set_state_flags` flips are xidless `heap_inplace_update` writes that `RecordTransactionCommit` commits asynchronously and `heap_xlog_inplace` redoes physically, so a crash lands on one of the four documented leftovers and never on a valid-but-incomplete index, because every core access method makes its first build durable before SET_VALID — B-tree via the outside-buffers `smgrimmedsync`, hash/GiST/SP-GiST/GIN/BRIN via shared-buffer build WAL — the validate scan's WAL precedes SET_VALID under the WAL-before-data rule, and unlogged tables reset heap and indexes together on crash); the `REINDEX INDEX CONCURRENTLY` mechanics now have their own page (below).
- [How REINDEX INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](v12/questions/reindex-index-concurrently.md) - Comprehensive walkthrough of the v12 `REINDEX INDEX CONCURRENTLY` implementation in `ReindexRelationConcurrently`: the six phases (create the `_ccnew` copy via `index_concurrently_create_copy`, build, validate, swap, set-dead, drop) and five wait points — the same three CIC waits (two `WaitForLockers(ShareLock)` waits over the `ShareLock` conflict set and `WaitForOlderSnapshots`) plus two extra `WaitForLockers(AccessExclusiveLock)` reader-capable waits before marking the old index dead and before dropping it; the shared build/validate core; the atomic `index_concurrently_swap` (new marked valid + old invalid, `_ccnew`→original and original→`_ccold`, with constraints/triggers/comment/dependencies and per-index cumulative stats moved); `ShareUpdateExclusiveLock` held at transaction and session level on the old index, new index, and heap; dispatch/restrictions (no transaction block, recovery-forbidden, temp fallback, direct system-catalog error vs schema/database skip, partitioned-table skip vs partitioned-index error, exclusion error-vs-skip, invalid-index skip-vs-allow, toast rebuilt); two-index state flags; per-phase failure outcomes with planner-vs-executor invalid-index overhead, regression recovery evidence, and the dedicated `reindex-concurrently` isolation spec; a dedicated section confirming that a failure never leaves the original `index_name` invalid for a healthy index (the phase-4 swap atomically renames and re-validates in one transaction, so the invalid leftover is always the differently-named `_ccnew` or `_ccold`, and you never go from a bloated-but-valid index to no usable index), the sole exception being an index already invalid before RIC ran; a dedicated section on running RIC against an already-invalid index (rebuilt/repaired when named directly, skipped with a warning when reached via `REINDEX TABLE`/`SCHEMA`/`DATABASE`); multi-index batching; and the source-vs-view discrepancy where phase 6 reports `WAIT_4`, so `waiting for readers before dropping` is never emitted.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 12? (unverified)](v12/questions/attach-partition-index-drops.md) - Answers that `ALTER TABLE ... ATTACH PARTITION` never drops an index from the table being attached: `AttachPartitionEnsureIndexes` is strictly match-or-create — for each partitioned parent index it re-parents a compatible existing partition index via `IndexSetParentIndex` (kept, not rebuilt) or creates a cloned one via `DefineIndex`, while incompatible/extra indexes on the partition are left in place (regression-proven by `indexing.out`: four incompatible indexes retained plus one added). Confirms there is no `performDeletion`/`RemoveInheritance` in the attach path (those live in DETACH/DROP CONSTRAINT/trigger/sequence paths) and that the pre-index `CreateInheritance` step only merges CHECK constraints (`MergeConstraintsIntoExisting`). Notes that v12's matching is looser than later minors (no `indisvalid` skip, constraint check is existence-only, not type) but still only re-parents or creates — never drops. Distinguishes the three look-alikes (an absorbed/re-parented index losing independent droppability, the documented manual drop of the now-redundant CHECK constraint, and a later cascading parent `DROP INDEX`), shows `ALTER INDEX ... ATTACH PARTITION` (`ATExecAttachPartitionIdx`) likewise only re-parents or errors (`refuseDupeIndexAttach`, "index definitions do not match"), and gives the docs' pre-build workflow (plain `CREATE INDEX`, since v12 bans partitioned CIC) to avoid a costly build.
- [All Outcomes That Leave an Invalid Index in PostgreSQL 12, Including a Failed CREATE INDEX CONCURRENTLY (unverified)](v12/questions/invalid-index-outcomes.md) - Enumerates the complete, closed set of operations that persist `pg_index.indisvalid = false` in v12, derived from the full list of `indisvalid=false` writers in the pinned checkout: a failed/cancelled/crashed `CREATE INDEX CONCURRENTLY` (invalid not-ready vs invalid ready), a failed `REINDEX CONCURRENTLY` (invalid `_ccnew` before the swap or `_ccold` after; a healthy original name is never left invalid), a failed/interrupted `DROP INDEX CONCURRENTLY` (marked invalid then dead across commits; the clear-valid step is retryable by design), an incomplete partitioned parent index (`CREATE INDEX ON ONLY` with partitions sets `INDEX_CREATE_INVALID`, or attaching an invalid child marks the parent invalid; `validatePartitionedIndex` flips it valid once every partition has a valid match), and `pg_upgrade` from <= 9.6 which runs `UPDATE pg_index SET indisvalid=false` for hash indexes in the new cluster and writes `reindex_hash.sql`. Includes the persistent `(indislive, indisready, indisvalid)` state table (and why `(f,t)` is impossible), the contrast that single-transaction `CREATE INDEX`/`REINDEX`/`DROP INDEX` roll back cleanly with no leftover, what an invalid index costs (planner skips it, executor still maintains it when `indisready`, VACUUM still processes it) and which commands reject it (`CLUSTER`, `REFRESH MATERIALIZED VIEW CONCURRENTLY`, `ADD CONSTRAINT ... USING INDEX`, `REPLICA IDENTITY`, bulk `REINDEX CONCURRENTLY`), and how to clear each outcome; cross-links the dedicated CIC and RIC pages.



## Maintenance Tooling

- `scripts/recent_log` - recent wiki activity.
- `scripts/wiki_lint` - wiki health checks.

## Maintenance Notes

- Update this page whenever a wiki page is created or substantially changed.
- Keep version-specific entries tagged with their PostgreSQL major version.
- Prefer links to version landing pages, such as `vNN/index`, once versions exist.
