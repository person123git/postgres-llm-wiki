# Wiki Index

This is the global catalog for the PostgreSQL engine wiki.

## Entry Points

- [versions](versions.md) - PostgreSQL version index and source pin manifest.
- [overview](overview.md) - Cross-version architecture overview.
- [log](log.md) - Chronological activity log.

## Version-Specific Pages

### PostgreSQL 19

- [v19/index](v19/index.md) - Active version landing page. Source checkout pinned to the `REL_19_STABLE` commit `67342a148632801d44c2fb9a7bf4231b6827c5d2` (upstream `master` has moved on to `20devel`; the branch is stamped `19beta3`, 26 commits past the `REL_19_BETA3` tag).
- [PostgreSQL 19 Codebase Navigation Guide (unverified)](v19/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v19 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.

#### Query Planning

- [How pg_plan_advice Works in PostgreSQL 19, and All Its Commits (unverified)](v19/questions/query-planning/pg-plan-advice.md) - Comprehensive explanation of the new `pg_plan_advice` contrib module: the core `pgs_mask` strategy-mask and five planner hooks, per-index disabling for index-specific advice, the advice mini-language (tags, relation identifiers, including underscore-separated occurrence numbers), plan-to-advice generation, advice enforcement, feedback/EXPLAIN output, the five `PGC_USERSET` GUCs, prepared-statement and plan-cache interaction (advice frozen at plan time; `always_store_advice_details` and `choose_custom_plan` re-plan policy plus relcache/syscache invalidation), the round-trip `test_plan_advice` harness, and the full scoped source history: 20 core planner foundation/enabling/fix commits, 27 direct module commits, and test/doc/build support commits.

#### Storage and Vacuum

- [How the REPACK Command Works in PostgreSQL 19, and Its 55 Feature-Scope Commits (unverified)](v19/questions/storage-and-vacuum/repack-command.md) - Comprehensive walkthrough of the new in-core `REPACK` / `REPACK (CONCURRENTLY)` command that absorbs `VACUUM FULL` and `CLUSTER`: the blocking new-heap rewrite/swap path, lock-free multi-table candidate discovery followed by locked per-table open/recheck, partitioned-target discovery hardening, the concurrent online rewrite via logical decoding (decoding background worker, `pgrepack` output plugin, temporary replication slot, race-safe on-demand decoding activation, change spill/replay through the identity index, lock upgrade and relfilenode swap), CONCURRENTLY restrictions, the `PGC_POSTMASTER` `max_repack_replication_slots` GUC, `pg_stat_progress_repack`, I/O impact (no cost-based delay applies; automatic BULKREAD/BULKWRITE ring buffers), generic `MAINTAIN` / `pg_maintain` documentation, the `test_decoding`/injection-point tests, and 55 feature-scope commits.
- [PostgreSQL 19 Autovacuum and VACUUM: Parallel Workers, Table Scoring, and Setting Pages All-Visible During Reads (unverified)](v19/questions/storage-and-vacuum/autovacuum-parallel-scoring-visibility.md) - Comprehensive, commit-backed walkthrough of three independent v19 features: parallel autovacuum (the `PGC_SIGHUP` `autovacuum_max_parallel_workers` GUC defaulting to 0, the per-table `autovacuum_parallel_workers` reloption, the autovacuum-vs-`max_parallel_maintenance_workers` cap in `parallel_vacuum_compute_workers`, and the new `PVSharedCostParams` cost-delay propagation; index phases only), autovacuum table scoring (`AutoVacuumScores` filled by `relation_needs_vacanalyze`, the five `*_score_weight` GUCs with an all-zero escape hatch, the post-beta1 MXID-score division-by-zero guard `1f2297b5487`, the descending `do_autovacuum` sort, the `pg_stat_autovacuum_scores` view, and how it differs from the launcher's database-level `adl_score`), and read-only query scans setting pages all-visible in the visibility map via on-access pruning (`ScanRelIsReadOnly` -> `SO_HINT_REL_READ_ONLY` -> `heap_page_prune_opt` -> `HEAP_PAGE_PRUNE_SET_VM`, `PruneState`, the read-query no-extra-dirtying/FPI safety valve, the VACUUM VM-skip that reduces future work, the post-beta1 `e9eaeb04248` FSM-refresh fix, the `b01c31eef9c` VM-clear WAL/incremental-backup correction with its `9171f77db23` TAP test, and the `3180ce3d7a8` wrong-buffer error-path hardening); includes per-feature source-commit history.

### PostgreSQL 18

- [v18/index](v18/index.md) - Primary version landing page. Source checkout pinned to `REL_18_STABLE` commit `baa7b142aace6821ce085906f314a75bcc4d95c8` (`REL_18_6-10-gbaa7b142aac`, source version 18.6).
- [PostgreSQL 18 Codebase Navigation Guide (unverified)](v18/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v18 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.

#### Indexing

- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 18 (unverified)](v18/questions/indexing/create-index-concurrently.md) - Comprehensive walkthrough of the v18 `CREATE INDEX CONCURRENTLY` implementation: parser/utility dispatch, restrictions, four internal transactions, two heap scans, three waits, table and session `ShareUpdateExclusiveLock`, `indislive`/`indisready`/`indisvalid` state progression, invalid-index failure outcomes, tests, and changes from PostgreSQL 17 (`pg_index` TOAST snapshot handling, parallel GIN build, virtual-generated-column rejection, and temporal `WITHOUT OVERLAPS` shared index-definition plumbing).
- [Computing and Storing avg_leaf_density During (Auto)VACUUM of a B-Tree Index (unverified)](v18/questions/indexing/avg-leaf-density-during-vacuum.md) - Reviewed design and evidence-backed cons for computing `pgstatindex`-style `avg_leaf_density` during B-tree VACUUM, including metapage/statistics storage, scan-skip caveats, and empty-page deletion accuracy gaps.

#### Storage and Vacuum

- [Extension Hooks for VACUUM and Autovacuum in PostgreSQL 18 (unverified)](v18/questions/storage-and-vacuum/extension-hooks-vacuum-autovacuum.md) - Catalogs every extension point on the VACUUM/ANALYZE and autovacuum paths: in-process hook variables, table and index AM and FDW callbacks, and adjacent surfaces (background workers, custom cumulative statistics), with manual-vs-autovacuum coverage.
- [Usage of NUM_BUFFER_PARTITIONS in PostgreSQL 18 (unverified)](v18/questions/storage-and-vacuum/num-buffer-partitions.md) - Explains the shared buffer mapping hash partition count, `BufferMapping` LWLocks, normal lookup/allocation and invalidation paths, relation extension, wait events, test-coverage gap, and the full source commit history (16->128) in PostgreSQL 18.

#### Replication and WAL

- [How Bi-Directional Logical Replication Works in PostgreSQL 18, and New Logical Replication Features Since PostgreSQL 17 (unverified)](v18/questions/replication-and-wal/bidirectional-logical-replication.md) - Explains how two v18 nodes replicate to each other without loops: mutual `origin = none` subscriptions, apply-worker origin tagging into WAL, `pgoutput`/decode-time origin filtering, the `copy_data` initial-sync WARNING, setup pattern, and limitations — including the new v18 conflict detection layer (`update_origin_differs`/`delete_origin_differs` logged with the other node's origin and counted in `pg_stat_subscription_stats`, regression-tested in the bidirectional TAP test); plus a section covering every logical replication feature new since v17, verified in source: conflict logging/statistics, generated-column replication (`publish_generated_columns` and column lists), the `streaming = parallel` default, alterable `two_phase`, the `max_active_replication_origins` (restart) and `idle_replication_slot_timeout` (reload) GUCs, `pg_createsubscriber` `--all`/`--clean`/`--enable-two-phase`, `pg_recvlogical` `--enable-failover`/`--drop-slot` changes, and the `pg_logicalinspect` contrib module.

#### Observability

- [How Custom Cumulative Statistics Work in PostgreSQL 18 (unverified)](v18/questions/observability/custom-cumulative-statistics.md) - Explains custom cumulative statistics registration, variable and fixed custom stat storage, flushing, snapshots, reset/drop behavior, and clean-shutdown persistence in PostgreSQL 18.
- [EXPLAIN ANALYZE BUFFERS Output in PostgreSQL 18 (unverified)](v18/questions/observability/explain-analyze-buffers-output.md) - Detailed field-by-field explanation of `EXPLAIN (ANALYZE, BUFFERS)` shared, local, temp, planning, serialization, parallel-worker, and I/O timing output in PostgreSQL 18.
- [How pg_stat_statements Works and Which Settings Affect It in PostgreSQL 18 (unverified)](v18/questions/observability/pg-stat-statements.md) - Explains the shared-preload extension's query-ID, normalization, hook, shared-hash, query-text file, readout, reset/save, permissions, and configuration behavior in PostgreSQL 18.
- [Limiting Query Text Size in pg_stat_statements in PostgreSQL 18 (unverified)](v18/questions/observability/pg-stat-statements-query-text-size.md) - There is no v18 GUC to cap individual query-text length; `track_activity_query_size` does not apply, texts store full-length in the external file, and the only levers are `pg_stat_statements.max`, the wholesale-discard garbage collector, and read-time `showtext`/`left()`.
- [How track_activity_query_size Is Used in PostgreSQL 18 (unverified)](v18/questions/observability/track-activity-query-size.md) - Explains how the postmaster-context GUC sizes backend activity-string shared memory, caps `pg_stat_activity.query`, interacts with `track_activities`, feeds deadlock/crash activity reporting, and does not truncate `pg_stat_statements.query`.

#### Server Administration

- [Changes to Declarative Partition Bound Syntax and pg_class.relpartbound Since Partitioning Was Introduced, as of PostgreSQL 18 (unverified)](v18/questions/server-administration/declarative-partition-bound-syntax-and-relpartbound.md) - Answers whether the declarative partition bound syntax or `pg_class.relpartbound` changed since PostgreSQL 10: yes to both, but every substantive change landed early. Syntax changed three times (`MINVALUE`/`MAXVALUE` replacing `UNBOUNDED` before 10.0 shipped, `DEFAULT` plus hash `WITH (MODULUS m, REMAINDER r)` in v11, the literal enumeration collapsing to a general `partition_bound_expr` in v12) and the grammar production has been frozen since v12 apart from pgindent, a typo, and the v18 commit `2d8bff603c9` that added error cursors to two hash-bound errors. The `relpartbound` column definition never changed, while the stored `pg_node_tree` gained `is_default`/`modulus`/`remainder` in v11 and had every `:location` written as `-1` from v17 (`d20d8fbd3e4`); the v16 generated-node-support switch left the output byte-compatible, and `outDatum`/`_outConst` are identical to `REL_10_0`. The deparsed `pg_get_expr(relpartbound, oid)` text never changed, so dumps stay comparable across versions, and no typed bound accessor was ever added — the three SQL forms remain the opaque node tree, the deparsed `FOR VALUES ...` string, and the derived `pg_get_partition_constraintdef()` boolean. Also covers the write-once/clear-once lifecycle including the two-transaction concurrent detach, the three engine read paths and their errors, why `MODULUS`/`REMAINDER`/`MINVALUE`/`MAXVALUE` are not bound-grammar tokens, the reverted MERGE/SPLIT PARTITION, and measurements taken on the previous 18.3-line pin `6cb307251c5`: the literal stored node text for all four bound shapes, `IN ((2+1))` stored as `3`, order-independent hash options, `"minvalue"` accepted but `"MINVALUE"` rejected, `FROM (-5)` deparsing as `FROM ('-5')`, pretty-printing a no-op on 14/14 bounds, and a no-TOAST size cliff between an 8,000-character bound (7,627 stored bytes) and an 8,400-character one (`row is too big: size 8216, maximum size 8160`).
- [GUC Default-Value Changes Since PostgreSQL 12 (unverified)](v18/questions/server-administration/guc-default-changes-since-v12.md) - Summarizes the GUC settings present in both PostgreSQL 12 and 18 whose defaults changed: seven changes from v13-v15, the v18 `effective_io_concurrency` default increase to 16, and the v18 `log_connections` type/default-spelling change that preserves disabled-by-default behavior.
- [Row-Level Security (RLS) in PostgreSQL 18: Implementation, Performance, Settings, and Fixes Since PostgreSQL 14 (unverified)](v18/questions/server-administration/row-level-security-rls.md) - Fully reviewed rewrite/relcache/planner/executor walkthrough with command-layered policies, WCO order, recursion and RI boundaries, selectivity/cost, locks, partitions/views/COPY/replication, settings, and all five PostgreSQL 14-derived performance follow-ups. The scalar-subquery review covers lazy per-occurrence/per-outer-plan-execution InitPlans, residual filters, index runtime keys, lossy rechecks and rescans, selectivity tradeoffs, and volatility/folding hazards. Tests on the previous 18.3-line pin reproduce those boundaries; 18.6's `0b12f56bfac` (CVE-2026-14666) then closed the same-role staleness window by registering `PlanCacheRoleCallback` on `pg_auth_members`/`pg_authid`/`pg_database`, so membership-edge `inherit_option`, `BYPASSRLS`, superuser, and current-database-owner changes no longer need `DISCARD PLANS`; the release-based history contains 24 scoped changes not already present in PostgreSQL 14.0.

### PostgreSQL 17.11

- [v17/index](v17/index.md) - Active version landing page. Source checkout pinned to `REL_17_STABLE` commit `786db8dcf168bd9df8f55047337525ac19118b1c` (`REL_17_11-7-g786db8dcf16`, source version 17.11).
- [PostgreSQL 17 Codebase Navigation Guide (unverified)](v17/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v17 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.

#### Query Planning

- [Planner Penalties for Bloated Indexes in PostgreSQL 17 (unverified)](v17/questions/query-planning/bloated-indexes-query-planner.md) - Yes, but every mechanism is indirect: v17 penalizes a bloated index only through `IndexOptInfo.pages` (a live `RelationGetNumberOfBlocks()` answer for non-partial indexes, `estimate_rel_size()` for partial ones), the fast-root `tree_height` charge of `DEFAULT_PAGE_CPU_MULTIPLIER` (50) `* cpu_operator_cost` per level, the `index_pages` share passed into `index_pages_fetched()` and `compute_parallel_worker()`, and the v17-only `num_sa_scans = Min(num_sa_scans, ceil(index->pages * 0.3333333))` ScalarArrayOp descent clamp. Covers what the planner cannot see (`avg_leaf_density`, `leaf_fragmentation`, index FSM free space, entries VACUUM removed), eight bloat shapes with per-query-shape sensitivity, and a since-v12 section in which `index_pages_fetched()`/`cost_index()` are byte-identical to `REL_12_0` while v13 deduplication (`0d861bbb702`), v14 bottom-up deletion (`d168b666823`), same-VACUUM page recycling (`9dd963ae253`), the 2% index-vacuum bypass (`5100010ee4d`), the wraparound failsafe (`1e55e7d1755`), `reltuples = -1` (`3d351d916b2`), v16 macro extraction (`eb5c4e953bb`), and the v17 clamp (`5bf748b86bc`) are each attributed to their first release tag. Exact-pin measurements on an isolated 17.10 server (previous pin): a closed-form cost prediction from (pages, tuples, fastlevel) that matches `EXPLAIN` to the cent, a point lookup priced at exactly `4.44` on both a 2,745-block/90.06%-density index and its 26,411-block/9.62% twin, a `1428.00` cost gap that is exactly `357 * random_page_cost` between 49.73% and 0% `leaf_fragmentation` (zero residual), two 2,745-block indexes with identical `12730.42` cost but 9.27% versus a healthy-looking 89.18% `avg_leaf_density` (the second hiding 2,465 deleted pages), a forged `pg_class.relpages = 1` that leaves cost unchanged while forging a partial index's `reltuples` does move it, a seq-scan plan flip at 25% selectivity, an index dropped from a `BitmapAnd` with its qual demoted to a `Filter`, 4 versus 6 parallel workers, a SAOP descent plateau at 3 versus 19, 852 versus 2,749 blocks with deduplication off, and 583 versus 1,174 blocks of version-churn growth with and without a held `REPEATABLE READ` snapshot. A filed follow-up answers when a GIN index is discarded and a B-tree used instead: three gates — clause matching (`op_in_opfamily()` in `match_opclause_to_indexcol()`, with `get_index_clause_from_support()` as the only escape hatch and the core `pg_amop.dat` GIN families carrying no `<`/`<=`/`>=`/`>`), plan shape from GIN's AM flags (`amgettuple = NULL`, `amcanorder`/`amcanreturn`/`amsearchnulls`/`amsearcharray`/`amcanparallel` all off), and cost (`gincostestimate()` charging `random_page_cost` plus `50 * cpu_operator_cost` for every pending, entry and data page, then `add_path()`/`choose_bitmap_and()` dropping the loser) — plus the 4X stale-metapage fallback, the keyless full-index path a partial GIN index can still yield, the four `CREATE INDEX`/`CLUSTER` rejections, where GIN still wins, and a v17 detail worth noting: a bare boolean `Var` does match a `btree_gin` bool opclass because `IsBooleanOpfamily()` falls back to a catcache lookup. Exact-pin measurements priced the same `n = 42` predicate at `12.97` through GIN and `4.52` through a B-tree on one table with literally identical statistics (both closed forms predicted to the cent) even though GIN was 279 blocks against the B-tree's 280; reproduced B-tree wins on `BETWEEN` (`67.15` vs `44.31`), `n < 20` (`28.63` vs `8.89`) and `IN (1,2,3)` (`30.10` vs `13.57`); proved three no-GIN-path cases (`ORDER BY … LIMIT`, index-only scan, `IS NULL`) with `disable_cost`-priced sequential scans; showed 982 pending pages moving a GIN scan from `13.80` to `4187.83` and out of a `BitmapAnd` with its qual demoted to a `Filter`, with `gin_clean_pending_list()` returning exactly `982`; recovered the charged page count exactly in five states via a two-`random_page_cost` probe (including `985.00` = 982 pending + 3, and `1001.00` on a 10-block keyless partial index); and measured a multicolumn `gin (tsv, cat)` beating the `BitmapAnd` at `21.55` versus `183.18`.
- [Table Partitioning Optimizations and Configuration During Query Planning and Execution in PostgreSQL 17 (unverified)](v17/questions/query-planning/partitioning-planning-execution-optimizations.md) - Explains, per partitioning type, the query planning/execution optimizations in PostgreSQL 17: legacy inheritance gets only plan-time constraint exclusion (and manual trigger routing), while declarative partitioning (RANGE/LIST/HASH) gets plan-time and run-time partition pruning (per-strategy bound matching — HASH needs all keys + equality/`IS NULL`, LIST includes `<>`, RANGE uses key prefixes + DEFAULT), partitionwise join (now with merged/non-identical bounds via `partition_bounds_merge`), partitionwise aggregate (FULL/PARTIAL through `group_by_has_partkey`), and executor tuple routing with cached last-found lookups, cross-partition UPDATE row movement, and per-partition COPY batching; documents the four `PGC_USERSET` GUCs (`constraint_exclusion` default `partition`, `enable_partition_pruning` on, `enable_partitionwise_join`/`enable_partitionwise_aggregate` off) with session/transaction scope; and adds an "optimizations since PostgreSQL 12" section attributing, via the checkout's own Stamp-bracketed commit history, v13 advanced partitionwise join (`c8434d64ce0`), v14 run-time pruning for `UPDATE`/`DELETE` (`86dc90056df`) and `DETACH PARTITION CONCURRENTLY` (`71f4c8c6f74`), v15 `live_parts` bitmap / run-time-prune refactor / `MERGE` (`475dbd0b718`, `297daa9d435`, `7103ebb7aae`), v16 cached tuple routing (`3592e0ff98b`), and v17 `boolcol IS [NOT] UNKNOWN` pruning (`07c36c1333e`) and concurrent-detach robustness (`11f1218ce81`). Adds a follow-up analysis of locking reduction and random-I/O sensitivity per optimization: only plan-time pruning removes read locks (it prunes in `expand_partitioned_rtentry` before `try_table_open` locks the surviving `live_parts`, whereas inheritance's `find_all_inheritors` locks every child before constraint exclusion runs, and `AcquireExecutorLocks` still locks every plan-time-surviving partition regardless of run-time pruning; write-path routing takes `RowExclusiveLock` lazily per touched leaf); the scan-eliminating optimizations gain most on high-latency random storage because avoided per-partition index/heap fetches are priced at `random_page_cost` (4× `seq_page_cost`); and — unlike v12 — v17 partitionwise aggregate also has a random-I/O tie-in because v17 hash aggregation spills to disk with spill writes priced at `random_page_cost`, while partitionwise-join batch spills stay sequential (`seq_page_cost`).

#### Indexing

- [B-Tree Bloat and Wasted Space From pgstatindex Alone, on PostgreSQL 12 and 17 (unverified)](v17/questions/indexing/btree-bloat-with-pgstatindex.md) - One statement whose only measurement function is `pgstatindex`, running unchanged on 12 and 17, reporting wasted space beside estimated reclaimable space — both now measured against a rebuild at the index's own fillfactor, whose target density comes from `BLCKSZ * (100 - fillfactor) / 100` in `_bt_pagestate` rather than from the fillfactor itself. Documents why each candidate filter exists — every one guards a shape `pgstatindex` raises on, and one raised call aborts the whole report — including the single behavioural difference between the majors: 17.11 refuses an invalid index (`13503eb5905`, earliest containing tag `REL_17_0`) while 12.2 returns a row for it. Measured on isolated 12.2 and 17.11 servers over 24 index shapes with `REINDEX INDEX` as ground truth: 91 of 94 and 89 of 93 indexes within 1.0 point, identical `+1.7` worst over-estimate, 24 of the report's rows byte-identical across the two servers, and three named under-estimates (deleted-not-vacuumed `−90.0`, a rebuild that would deduplicate `−69.4` on v13+, 400-byte keys `−5.1`). Also: the `NaN` trap for indexes with no leaf pages and why `NaN > 20` is true, the `pg_stat_scan_tables` grant plus why passing an OID avoids needing schema `USAGE`, the 256 kB `BAS_BULKREAD` ring and ~830 MB read per run, `lock_timeout` cancelling at 2000.9 ms behind an uncommitted `DROP INDEX`, and the concurrent-drop race that kills the statement outright. A follow-up drops the `alert_pct` parameter and the `status` column it drove — the report now measures and estimates without judging — and proves on both restarted servers that nothing else moved: the amended output equals the filed output with that one column cut, byte for byte, `EXCEPT` in both directions over the 28 internal columns returns 0 rows across 214 and 220 indexes, and plan shape and execution time are unchanged. A second follow-up rebases every wasted-space calculation on the index's own fillfactor and renames the pair `wasted_vs_fillfactor` / `wasted_ff_pct`: the leaf term is measured against the build-code target density and clamped at zero, so a fresh index reports 0.0 at any fillfactor where the old baseline read 89.6% at `fillfactor = 10`. Proved on both restarted servers that only those two fields moved (12 untouched fields byte-identical; `EXCEPT` 0 rows over the 27 shared internal columns across 214 and 220 indexes; identical plan shape and identical total buffers, 108,021 and 108,327), that a rebuilt index reports no waste (exactly 0 for 81 of 97 and 76 of 96, and ≤ 0.4% for every index the report prints), and that the new column is `(leaf_capacity - target_free) / block_size` of `est_reclaimable` for in-page waste — 0.8951 predicted, 0.8868-0.8921 measured, 1.0001 where the waste is whole dead pages. Four new non-default-fillfactor fixtures show the consequence: `i_ff10_del90` is 89.9% reclaimable and reports 8.3% wasted, because at fillfactor 10 most of the file is free space by instruction.
- [Checking Whether an Index Needs a Rebuild to Enable Deduplication After pg_upgrade From PostgreSQL 12 to 17 (unverified)](v17/questions/indexing/btree-deduplication-after-pg-upgrade.md) - Every B-tree index `pg_upgrade` carries over from a v12 cluster is unable to deduplicate, because deduplication safety is recorded once at build time in the metapage field `btm_allequalimage`, which v12 never wrote and `pg_upgrade` was never taught to set; `btm_version` is 4 before and after, so version is not the discriminator. Gives a core-SQL-only check (byte 64 of block 0 via `pg_read_binary_file` + `get_byte`, no `pageinspect`) plus a catalog mirror of `_bt_allequalimage` so pointless rebuilds are skipped, the exact `GRANT EXECUTE` it needs, a platform self-test, priority ordering, and snapshot/twin fallbacks. Measured on three 12.2 -> 17.10 `pg_upgrade --copy` runs: 21/21 carried-over indexes read `pd_lower = 64` and the flag false, probe agreed with `bt_metap()` in 75/75 row comparisons (57 before the rebuilds, 18 after), all 21 index files MD5-identical across the upgrade, gate matched the engine's `DEBUG1` verdict on 19 shapes, rebuilds reclaimed 67.4%-69.1% on seven duplicate-heavy indexes and 0.0% on five not-equal-image ones, a carried-over index was byte-identical in size to a fresh `deduplicate_items = off` twin, and the savings curve ran 0.0%/10.0%/48.1%/65.4%/69.5%/67.4% at 1/2/5/20/100/1000 rows per key. Also documents the `deduplicate_items` trap, `VACUUM FULL`/`CLUSTER` versus plain `VACUUM`, the zeroed `relpages`/`reltuples` left by `pg_upgrade`, and `pg_largeobject_loid_pn_index` as the single carried-over catalog index.
- [Pros and Cons of Partial Indexes in PostgreSQL 17 (unverified)](v17/questions/indexing/partial-indexes-pros-cons.md) - Explains partial-index benefits, tradeoffs, ordinary expression-index behavior, partial indexes on expressions, planner predicate implication and expression-key matching, operational restrictions, `ANALYZE` row-count and expression-statistics behavior, planner costing, regression coverage, and what changed from PostgreSQL 12 (documentation warning, concurrent-build safe-wait exclusion, MERGE target rechecks, expression statistics, `NULLS NOT DISTINCT`, partial-unique proof restriction, and partial-hash planning fix).
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 17 (unverified)](v17/questions/indexing/create-index-concurrently.md) - Reviewed walkthrough of the v17 `CREATE INDEX CONCURRENTLY` implementation in `DefineIndex` (four internal transactions, two heap scans, three transaction waits; the `indislive`/`indisready`/`indisvalid` progression using v17's transactional `CatalogTupleUpdate` path in `index_set_state_flags`; preconditions/restrictions and the invalid-index failure path); a dedicated steps-and-locks section (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, and `WaitForOlderSnapshots`, with the `LockConflicts`/`WaitForLockersMultiple` rationale); and a "what changed from PostgreSQL 12" section: the PG14 `PROC_IN_SAFE_IC` optimization (safe non-partial/non-expression builds advertise via `set_indexsafe_procflags` and are ignored only in the snapshot wait `WaitForOlderSnapshots`, not the lock-based `WaitForLockers`), transactional `index_set_state_flags` (`83158f74d3a`), the added-then-reverted VACUUM-ignores-CIC attempt (so VACUUM still holds CIC's horizon in v17), the `PGXACT`->`PGPROC` `statusFlags` move, and the owner/restricted-search-path build hardening — anchored to the v17 checkout's own commit history (`c98763bf`, `f9900df5`, `e28bb885`, `83158f74d3a`, `5788e258`), with regression and `multiple-cic`/`prepared-transactions-cic` isolation coverage; now extended with a `maintenance_work_mem` follow-up covering the separate AM-build and serial-validation budgets, B-tree/BRIN parallel thresholds and mandatory worker tape runs, per-AM plateau boundaries, conditional GIN pending-list cleanup, observability, and the workload-specific plateau; plus a broader GUC-performance follow-up covering build/parallel controls and worker pools, GiST/GIN boundaries, v17 read-stream I/O, index/temp storage, WAL/checkpoints/commit latency, waits/timeouts, apply scopes, and commonly confused non-controls.
- [How REINDEX INDEX CONCURRENTLY Is Implemented in PostgreSQL 17 (unverified)](v17/questions/indexing/reindex-index-concurrently.md) - Comprehensive walkthrough of the v17 `REINDEX INDEX CONCURRENTLY` implementation in `ReindexRelationConcurrently`: the six phases (create the `_ccnew` copy via `index_concurrently_create_copy`, build, validate, swap, set-dead, drop) run across many short transactions, each phase looping all indexes; the five waits — two `WaitForLockersMultiple(ShareLock)` writer waits, `WaitForOlderSnapshots` (which now ignores safe concurrent builds via `PROC_IN_SAFE_IC`), and the two extra `WaitForLockersMultiple(AccessExclusiveLock)` reader waits before set-dead and drop that CIC lacks; the atomic phase-4 `index_concurrently_swap` (new valid + old invalid flipped together with the transactional `CatalogTupleUpdate`, `_ccnew`->original / original->`_ccold` rename, constraints/triggers/comment/dependencies and `pgstat_copy_relation_stats`/`CopyStatistics` moved); `ShareUpdateExclusiveLock` on heap, old index, and new index at both transaction and session level; dispatch via `ExecReindex`/`ReindexParams`, the restriction set (transaction block, recovery, temp fallback, system catalogs, exclusion, invalid-allowed-when-named), the `indislive`/`indisready`/`indisvalid` progression on both indexes, and a per-phase failure table establishing that a healthy `index_name` is never left invalid (only the differently-named `_ccnew` pre-swap or `_ccold` post-swap is, with planner/relcache/executor handling of invalid-but-ready leftovers), and a dedicated "can a failure leave an invalid index with the original index name?" section that answers the bloat-to-no-index worry directly — a RIC failure never converts a bloated-but-valid `index_name` into no usable index because the phase-4 swap renames the rebuilt copy to `index_name` and flips validity in one transaction, so `index_name` is invalid only when it was already invalid before the run (the `concur_reindex_ind5` repair regression test). Adds a source-backed GUC-performance follow-up covering build/validation memory, B-tree/BRIN worker request and pool caps, AM-specific GiST/GIN behavior, v17 read streams, temporary and output storage, WAL/checkpoints and the nine internal one-index commits, all five timeout-sensitive waits, exact apply scopes, and commonly confused non-controls. Includes a detailed "what changed from PostgreSQL 12" section anchored to the checkout's own commit history: partitioned REINDEX (incl. CONCURRENTLY) now supported via `ReindexPartitions`->`ReindexMultipleInternal` leaf recursion (`a6642b3ae06`, v14; v12 errored "not yet implemented for partitioned indexes"), `REINDEX (TABLESPACE ...)` (`c5b286047cd`, v14), `index_set_state_flags` made transactional (`83158f74d3a`, v14, replacing v12's `heap_inplace_update`), `PROC_IN_SAFE_IC` extended to RIC (`f9900df5`, v14), the progress-view `WAIT_5` "waiting for readers before dropping" fix (`e66bcfb4c66`, v14; v12 wrongly reused `WAIT_4`), the v15 shared-memory statistics rework (`8ea7963fc74`), and the v17 `MAINTAIN` privilege (`ecb0fd33720`); with regression (`create_index.sql`, including partitioned and repair cases) and `reindex-concurrently` isolation coverage.
- [Testing the PostgreSQL 12 Core-SQL B-Tree Bloat Method on PostgreSQL 17 (unverified)](v17/questions/indexing/btree-index-bloat-core-sql-only.md) - The v12 page's four contrib-free methods executed verbatim on an isolated 17.11 server built from the pin and scored against `CREATE INDEX CONCURRENTLY` rebuilds plus `pgstatindex` ground truth, over the 15 named fixtures, a 9-bloat-type x 3-scale x partial/non-partial 54-cell matrix, and a six-point duplication-ratio sweep. Accuracy transfers unchanged on distinct-key indexes: the catalog model is exact on 11 of 15 fixtures and all 24 non-partial matrix cells outside the duplicate-key type, Method B's `leaf_pages` census is exact on 12 of 12 fixtures and 36 of 36 eligible cells, `pg_column_size` still fixes variable key width (+105 blocks to −1), Method C stays exact by construction, and the stale-partial-index failure still clears with one `ANALYZE` (worst error 499 blocks to 1). What breaks is v13 B-tree deduplication, which the page-fill model cannot see: +223.3% on an all-duplicate 1,000,000-row index, +92.5% at 5 rows per key on an index with 0% reclaimable, +20.9% on a 25%-NULL index (NULL runs deduplicate too), and +217 points on Method B's density formula (313.58% against 96.15%). Files a source-derived dedup-aware sweep — 6 bytes per extra TID, gated on `n_distinct > 0` because `ANALYZE` switches to the unreliable negative fraction above 10% distinct, plus non-unique, `deduplicate_items`, and the `pg_amproc` equal-image support function — that cuts the worst `dup` error to −24 blocks (2.9%) without changing an already-exact cell, and a `reltuples = -1` guard for the v14 unknown sentinel that otherwise reports 100.0% bloat on a healthy 21 MB index (`TRUNCATE` reproduces the state; the collector's `n_live_tup` read 2,000,000 for 1,000,000 rows, so the guard reports unmeasured instead of guessing). Also documents the v17 `VACUUM VERBOSE` rewrite (four page classes, no index row count) and its new 2%-of-pages index-vacuum bypass, which prints no index line at all. A filed follow-up asks whether that corrected sweep works for indexes on a v12 server as well as a v17 one, and answers yes for a reason that is in the SQL: each v17 term is gated on a catalog fact a 12 server cannot produce — an `amprocnum = 4` equal-image row (`_bt_allequalimage` is what the engine consults, and support numbers are bounded by the AM's `amsupport`, so 12.2 answers `ERROR: invalid function number 4, must be between 1 and 3`), the `deduplicate_items` reloption (12.2: `ERROR: unrecognized parameter`), and the `-1` `reltuples` sentinel (12.2 reads 0), the three constructs first shipping in `REL_13_0`, `REL_13_0` and `REL_14_0`. Measured by running the identical statement on isolated 12.2 and 17.11 servers over the same DDL and data: 0 of 34 indexes credited on 12.2 with `expected_blocks` identical to v12 Method A in every cell and no miss worse than 7 blocks, against 16 of 36 credited on 17.11; a 10-distinct-key index with 90% of its rows deleted and vacuumed is reported at 90.0% on 12.2 (rebuild 278 blocks) and 90.1% on 17.11 (rebuild 87), against true bloat of 89.9% and 89.8%, where the uncorrected v12 model would have said 67.5% on 17.11. The follow-up also exposes one v17 false positive of the sweep as filed — an `INCLUDE` index whose included column is low-cardinality read 69.7% bloat on a 0%-reclaimable 2749-block index, because `_bt_allequalimage` refuses INCLUDE indexes outright while the catalog probe inspects only key columns; adding `indnatts = indnkeyatts` to the gate makes that cell exact and moves no other — and one v12-only hazard the `-1` branch cannot catch: after `TRUNCATE` and reload, 12.2's stale-zero `reltuples` makes a healthy 825-block index read 99.9% bloat where 17.11 reports `unmeasured`, so a verified zero-plus-size rule is filed for 12 and 13 servers. A further follow-up proposes one statement for servers 12 through 17 and measures it on isolated 12.2, 14.23 and 17.11 servers: exact posting-tuple arithmetic (`MAXALIGN(base + tids * 6) + 4` under the 812-byte 1/10-page cap, with `_bt_buildadd`'s high-key truncation credit on leaf capacity), key groups from a NULL-run plus most-common-value plus remainder mixture, a negative `n_distinct` credited only for whole-table indexes, a nondeterministic-collation conjunct in the gate, both `reltuples` eras, and a second floor percentage to alert on. Same text everywhere: 68/68/72 indexes swept over 133,677/94,910/103,056 blocks in 17.1 to 20.5 ms; 0/60/62 credited; `expected_blocks` identical to v12 Method A in all 34 scored cells on 12.2. Within 5% of a rebuild on 26 of 33 cells on 14.23 and 27 of 35 on 17.11 against Method A's 14 and 15, and alerting on the floor plus the `caveats` column scores 4 true positives, 0 false positives and 1 false negative on every server — where the point estimate alone is fooled by a nondeterministic ICU index (88.2% without the new conjunct, 0.1% with it), a hot-value skew index whose `n_distinct` came back 82,250 against a true 750,001 (0.2% after `SET (n_distinct = -0.75)`), and a table doubled since its last `ANALYZE`. A final source-only follow-up corrects two reporting defects both statements shipped with, neither of which touches `expected_blocks` or any percentage measured on the page: `has_freed_pages` was `fsm_bytes > 0`, which proves only that something recorded a free page in the index's free space map since its relfilenode was created — an index FSM stores just free-or-used, the first recorded page extends the fork to a fixed 24576 bytes, `GetFreeIndexPage` marks pages used without shortening it, no nbtree path calls `FreeSpaceMapPrepareTruncateRel`, and only `RelationSetNewRelfilenumber` (`REINDEX`, `TRUNCATE`) resets it, while deleted-but-not-yet-recyclable pages, half-dead pages and partly-emptied leaves never enter the map at all — so it is renamed `fsm_written_since_build`, and current page classes are shown to come only from `VACUUM VERBOSE`'s `newly deleted / currently deleted / reusable` line in core or from contrib (`pgstatindex.deleted_pages`/`empty_pages`, `pg_freespace` with `avail > 0`, `pageinspect`'s per-page `type`); and `wasted` was clamped at zero beside an unclamped `bloat_pct`, so a 3211-block index whose model predicts 3353 printed `0 bytes` next to −4.4% and the v17 sweep hid 10,805,248 bytes of over-prediction next to −92.5%, fixed by making both columns signed (`pg_size_pretty` renders negatives symmetrically, and the regression suite asserts it) and by dropping the same clamp from the `ORDER BY` key so a `LIMIT 20` no longer ties every over-predicted row at zero. A fifth source-only follow-up renames the reporting columns off the word "bloat" — both statements now emit `wasted_space`, `wasted_space_pct` and `wasted_space_pct_floor` under `wiki_btree_wasted_space_sweep_*` tags — and shows the rename is `AS`-label-deep: `TargetEntry.resname` is declared `query_jumble_ignore` and `gen_node_support.pl` emits no jumble code for such a field, so `queryid` cannot move; both `ORDER BY` keys are expressions rather than labels; and the 22-byte names are far under the `NAMEDATALEN` truncation point. The justification is source-level too: the glossary defines bloat as per-page space (free space plus outdated row versions) that neither statement can see, "wasted space" is the docs' own phrase for space a maintenance command recovers, and no core view, `pg_proc.dat` entry or contrib SQL script names a column `bloat`. What a consumer must change is named — reading an old name now raises `ERRCODE_UNDEFINED_COLUMN`, `pg_size_pretty` returns `text` so ranking belongs on the byte expression (hence a suggested `wasted_space_bytes`), and an existing `pg_stat_statements` entry keeps showing the old tag because the text is written only when the entry is created. A sixth follow-up works a twelve-issue external review of the portable statement on a **17.11** server built from the current pin — the first measurements on the page taken on the pinned commit — and files the ledger as four issues already fixed (negative `n_distinct`, the nondeterministic-collation and INCLUDE conjuncts, both reporting defects), one mischaracterized, five applied and two rejected. The mischaracterization is the page's own "page packing" attribution: `i_q1000`'s 49-block gap is 48 blocks of posting-tuple miscounting (the model let one posting tuple span two key groups, though `_bt_load` flushes the pending list at every key change) plus one block of internal-fanout error (pivots above a deduplicated leaf level measure 8-24 bytes with a heap TID, 212 per page against the modelled 284), and leaf packing is modelled correctly. Applied: each key group now rounds up to whole posting tuples, which removes every phantom reading above 264 rows per key (`i_q1000` +5.5% to −0.6%) but is not a strict improvement — it prices the last, partial tuple at full size, so `r` = 133 goes to −100.0% — and leaves `i_cd` at +8.0%, whose 37 blocks are an off-by-one leaf capacity (12 modelled posting tuples where the build fits 11 data items plus the high key); `pg_stats_ext.n_distinct` replaces the per-column product when an `ndistinct` object covers the whole key, which turns a genuinely 49.8%-reclaimable correlated two-column index from `−38.3%` (floor `−53.3%`, silent on both) into `+49.9%`, one block from the rebuild, with measured superset, wrong-column-set, reversed-column and expression-key behavior and `pg_stats_ext`'s owner-membership condition stated; a `statistics not visible to this role` caveat distinguishes a withheld `pg_stats` row from an absent one and joins the alert-suppression set, because the 32-byte default `avg_width` makes a healthy 100-byte-key index read 62.5% on **both** columns to an unprivileged role; a randomly inserted, never-deleted fixture reports 27.1% on both columns at 65.68% leaf density with the model exactly equal to the 2745-block rebuild, which is the one failure the floor rule cannot defend against; and the baseline is stated from `_bt_findsplitloc` (fillfactor only on rightmost splits, 50:50 elsewhere) with a measured drift table — a rebuild reclaimed 1020 blocks and 500,000 further random inserts gave 10.9% of it back. Rejected with source reasons: `least(reltuples, n_live_tup)` stays, because `reltuples` alone reports the 19%-drained `i_stale` as 0.0% (521 blocks lost, floor included); and `bt_metap().allequalimage` stays out, because the build recomputes the flag, a pg_upgrade'd v12 index reports false until a `REINDEX`, the function is superuser-only, and a `deduplicate_items = off` index reports true while holding no posting lists — the metapage answers "may this index deduplicate as it stands", not "would a fresh build deduplicate". Verified to run unchanged on 12.2, where every added construct exists, `inherited`/`exprs` are deliberately not referenced, and the same healthy random-insert index reads 26.6%. A seventh follow-up files the seventeen mandatory deduplication-gate tests and scores every proposed statement against them on a **17.11 ICU** build of the current pin: the corrected statement fails 3 of 17 and the earlier v17 sweep fails 6, because the gate only asks whether an equal-image support function exists in `pg_amproc` while `_bt_allequalimage` calls it. Eight custom operator classes carry the cases no built-in type can — `amvalidate` accepts SQL and PL/pgSQL support functions — and over 28 fixtures with nothing to reclaim the filed gate over-credits 5 (a healthy 1376-block index reads **69.3%** and two mixed-key indexes with an `ndistinct` object read **78.1%**), while a NULL-returning function aborts `CREATE INDEX` outright. Change 6 requires the registered proc to be `LANGUAGE internal` with `prosrc` in (`btequalimage`, `btvarstrequalimage`): 0 over-credits, one deliberate under-credit at −226.4% on a custom opclass that really does deduplicate, and no reading moves on a stock database, whose 29 B-tree support-function-4 rows all pass. `prosrc` beats `proname` on measured fixtures — a renamed built-in and a same-named SQL impostor at a fresh OID break the name test in both directions, while `fmgr`'s built-in fast path and its `LANGUAGE internal` prosrc lookup explain why. Rewriting a support function to return false leaves the metapage true, makes `bt_index_check` raise `metapage incorrectly indicates that deduplication is safe`, and a `REINDEX` then triples the index — the corrected reading predicted that to 0.4 points. On 12.2 seven of the seventeen tests are unconstructible (no `FUNCTION 4`, no `deduplicate_items`, no ICU) and all four variants agree with the engine on all 10 buildable fixtures. Ships a zero-fixture audit query (6 rows in the fixture database, 0 on a stock 17.11 and on 12.2) and the runnable fixture harness. A ninth follow-up folds change 6 into that statement — the section is retitled "with all six changes", change 6 keeps its rationale but loses its duplicate SQL, and no assembly step is left — and **re-runs every server-measured table on the page on 17.11**, so nothing on it is a 17.10 observation any more. Four servers: one ICU-and-debug 17.11 install carrying the 15 v12 fixtures with the 54-cell matrix, the twelve-issue-review family, the 28 mandatory-test fixtures and a fresh harness database, plus a second 17.11 cluster and isolated 14.23 and 12.2 servers for the 12-through-17 family; every scored text, including the pre-change-6 form, is generated mechanically from the page's own Markdown. Reproduced exactly: all 15 `pgstatindex` rows, Method C's rebuilds, Method B's 36-of-36 census, the 313.58%-against-96.15% density blow-up, 5 gate over-credits before change 6 and 0 after with 1 under-credit, 8 for the earlier sweep, the audit query's 6 / 0 / 0 rows and every 12.2 blocker. Moved only where `ANALYZE` sampled differently: Method A exact on 11 of 15 fixtures rather than 9, the matrix at 33 exact rather than 30, and `i_q1000` at 899 blocks rather than 901 because 8 most-common values were stored instead of 11. One earlier claim is corrected: change 6 does **not** under-credit an opclass whose support function is a `LANGUAGE internal` alias of `btequalimage` — both texts report 69.3% on a true 69.4% — so its single cost is confined to non-internal support functions, re-measured with a SQL one. Newly measured on the ICU build: the nondeterministic-collation conjunct (a healthy 3611-block index reads 0.1% against the earlier sweep's 87.6%), change 1's residual over-prediction on a two-column and a partial fixture (−24.8% and −33.1%), a never-rebuilt 1.5M-row random twin at 25.9% beside its rebuilt sibling's 11.5%, and interleaved timings that price the five earlier changes at about 30% while change 6 stays inside the noise. The recommended-statement section now names one block of SQL to run, ranks all six variants on accuracy, fix count and measured version coverage, names the residual errors, lists what it does not replace (Method C as arbiter, Method B's leaf census, Method A-prime's sampled width), and states what would displace the recommendation. A tenth follow-up folds 74 partial-index tests into the mandatory suite, taking it to 91 tests, and the recommended statement **fails the primary requirement**: 12 freshly built or freshly grown partial indexes with nothing to reclaim report `wasted_space_pct_floor` from 84.0% to 99.6% (worst: 1,000 build-time rows against 496,000 real ones), 8 genuinely 73-94%-reclaimable partial indexes read under 20%, and the filed alerting rule suppresses none of the 21. One structural fact explains it — `ANALYZE` gives a partial index column statistics only when it is an expression index, so a plain-column partial index is priced from whole-table `avg_width`, `null_frac` and `n_distinct` — and materialising each predicate subset as its own table makes the same statement exact on 7 of 7 probes, locating the defect entirely in the statistics input. The floor column is proved immune to the whole duplication family (`n_distinct`, MCV and extended-statistics mismatches move only the point estimate, worst 70.6% against 0.0% on the floor) and fully exposed to width, NULL-fraction and row-count mismatch; one `ANALYZE` repairs 7 of the 12 false positives and nothing in core SQL repairs the other 5. Two measured alert-routing changes take the 12 to 1 at zero cost in true detections, and three incidental findings are filed: index datums over 510 bytes are pglz-compressed in place (142 blocks against 1560 on the same key length), a same-session `DELETE; VACUUM` can leave `n_dead_tup` non-zero because ANALYZE/VACUUM write absolutely while pending deltas add on top, and an append-only partial index measures 880 blocks live against 881 rebuilt. A further follow-up applies both changes to the statement as a hard `WHERE` exclusion — a `suppress_partial` flag that withholds a partial index missing an index-column statistics row, one crediting duplicates from table statistics, or one whose table has changed past its **auto-analyze trigger** — and re-scores the whole suite on a fresh 17.11 server: **12 critical false positives become 1** (test 47, a wide `INCLUDE` column at 84.0%), the one plain false positive goes to 0, the 8 false negatives are unchanged, and all four true detections still report (75.0/74.3/89.1/94.2 against measured 74.9/74.3/89.1/94.2). The trigger is `autovacuum_analyze_threshold + autovacuum_analyze_scale_factor * reltuples` with per-table reloption overrides and a negative `reltuples` read as zero, exactly as `relation_needs_vacanalyze` computes it — measured at 100,050, 50,050, 10,050, 100, 1,000,000, 60 and 50 across seven fixtures — because the originally sketched `n_mod_since_analyze > 0` test, while scoring the 74 tests identically, withheld two genuinely 89.1%-reclaimable indexes that the trigger form reports. The exclusion carries `is_partial`, so 0 of 4 non-partial controls are touched, and the price of that scoping is stated: a non-partial expression index with no statistics row reads 64.9% on a freshly built 5201-block index and is still reported. Whole-database effect: 82 rows to 46, 8 readings above the 50% line to 2, 9 of the pre-change top-20 triage rows withheld; cost inside the noise at 33.4/38.0/32.8 ms against 37.5/33.6/35.2 ms interleaved. One new hazard is measured: without `pg_stat_force_next_flush()` before an `ANALYZE`, a same-session bulk load leaves `n_mod_since_analyze` at the full 200,000 rows (and `n_live_tup` at 400,000 for a 200,000-row table), which fires the new caveat spuriously in the safe direction. A twelfth follow-up adds change C for that last survivor and the suite reaches **0 critical false positives**: a partial index with a variable-width `INCLUDE` column returns no row, because a non-key column can never be an expression and `ANALYZE` writes index statistics only for expression attributes, so its width can only ever come from the table's `avg_width` — 13 bytes against a stored 207 on test 47, a 36-byte modelled tuple against a measured 217.7, on a 787-block index `pgstatindex` reports at 89.71% leaf density. Exactly one of the 74 tests moves; all 74 live-block counts and the pre-change reported/withheld state reproduce the previous run cell for cell. The term is scoped to `attlen < 0` non-key columns, so an `INCLUDE (int)` index keeps its correct 0.0% reading, and the two wider forms are priced rather than adopted (any `INCLUDE` column costs one more correct row; any variable-width column costs three more but is the only form that catches a wide *key* column, which still reads 84.1% with an empty `caveats` string). The price is stated: a genuinely 89.9%-reclaimable partial index with a `text` payload, correctly estimated at 89.5%, is now withheld too, and the term emits no caveat, so a reader cannot tell from the output that this silence will never lift. Change C is also the one exclusion term run on 12.2, where the same fixture builds the same 787 blocks, the pre-change text reports 84.1% and the amended text withholds it. A thirteenth follow-up turns the exclusion on the **non-partial** side and renames the flag `suppress_row`: a non-partial expression index with no statistics row of its own returns no row, because `ANALYZE` writes an index its own `pg_statistic` rows only for expression attributes and `index_drop` uses the same `pg_index.indexprs` test when it decides whether the index has statistics to remove, so the expression is priced at the statement's 32-byte default. Control `np97` read **64.9% on 5201 blocks a `REINDEX` reproduces exactly** — a 44-byte modelled tuple against a measured 120-byte item on leaves `pgstatindex` puts at 91.31% density — and a mixed `(k, upper(s))` key read 60.5%; both are now withheld, and none of the 74 partial-index verdicts moves, since the disjunct carries `NOT is_partial`. Unlike change C the silence lifts: one `ANALYZE` takes `np97` to a reported `−16.6%`, and the identical-DDL pair 106/107 prices the trade at 96.4% without a statistics row against 89.2% with one, on a measured 89.9% reclaim. The two wider forms are measured and not applied, which leaves one alertable hole — a plain index whose column carries `SET STATISTICS 0` still reads 64.9% on a healthy index — and change D is the second term run on 12.2, where the same expression fixture builds the same 5201 blocks and moves from a reported 64.9% to withheld. A fourteenth follow-up then **rebuilds that statement for readability and maintainability without moving a value it returns**: still sixteen CTEs, but `sized`/`fit`/`posting` collapse into one `page` CTE of three commented laterals while a new `env` and a new `gate` take the server constants and the whole deduplication decision out of `idx` (77 lines to 62), three statistics-state facts are named once in `modelled` so the caveat list and the `WHERE` clause stop spelling the same conditions twice, and a 35-line header maps each named change 1-6 and A-D to its stage. Identity is measured on two newly initialised clusters: `EXCEPT` in both directions between views generated from the two Markdown blocks returns **0 rows over 95 indexes on 17.11 and 92 on 12.2**, both exact texts give byte-identical `psql` output, and all 91 mandatory tests plus fixtures 92-112 score identically (30 gate fixtures reproducing 421/460/1376/1931 blocks and the single `i_ei_true` under-credit; 74 partial tests at 66 PASS / 0 critical false positives / 0 false positives / 8 false negatives, with 74 of 74 live-block counts matching for the fourth run). The cost is stated rather than hidden — one extra `CTE Scan on idx`, `+1.3 ms` planning and `+0.8 ms` execution at 123 indexes, `+3.2%` execution at 600 — and running the whole partial suite on 12.2 for the first time found the exclusions' portability limit: `dedup_applies` is false for all 106 indexes there, so two of the four partial terms cannot fire and tests 30 and 64 return as unsuppressed critical false positives at 87.6% and 93.5%. A fifteenth follow-up adds **mandatory test 113**, a fully drained job queue — 1,000,000 rows inserted as `state = 'pending'`, a partial index `WHERE state = 'pending'`, every row then updated to `'done'` — and the recommended statement **failed it in all three states** on an index a `REINDEX` takes from 2745 blocks to 1. The arithmetic was already right (`expected_blocks` 1); a guard written for the majors where `reltuples = 0` was ambiguous replaced the answer with `unmeasured`. **Change E** believes that zero when it is provably current — v14 or later and `n_mod_since_analyze = 0`, the condition commit `3d351d916b2` created by making the unknown state `-1` — and the two maintained states now read **100.0% against a measured 100.0%**. It is the first lettered change that adds rows rather than withholding them, and both directions are priced: change B's auto-analyze trigger in place of the strict zero reports **99.3% on a 1.1 MB index that is entirely live**, so the strict form is kept; change E's own cost is one critical false positive where an `ANALYZE` sample misses the whole predicate subset (87.5% on a live 8-block index, produced 8 times in 10 at `default_statistics_target = 1`); the drained-but-unanalysed state stays withheld because no core-SQL method can see it and `VACUUM` alone does not lift it; and on 12.2 the version gate makes the change a measured no-op even though the counter there reads 0. Over 119 indexes on a rebuilt suite, `EXCEPT` in both directions moves exactly 4 rows, all in that branch. A sixteenth follow-up **removes that guard entirely**, at the asker's request: `live_rows` is a two-arm `CASE`, every `reltuples = 0` is priced as an empty index on 12 through 17, and the now-unreachable `unmeasured: reltuples 0` status arm goes with it — a third statement text proves that deletion returns **0 rows from `EXCEPT` in both directions over 52 columns**. Both servers were rebuilt and the whole suite re-scored, and the two majors move in opposite directions. On **17.11 it buys nothing and costs one critical false positive**: 3 of 118 rows move, two are withheld by change B either way, and fixture 118 — a queue that refilled after being measured empty — reports **99.3% on a live 1.1 MB index** with `status = ok` and an empty `caveats`, the same reading that got the auto-analyze trigger rejected as change E's test. On **12.2 it is a real trade**: 7 of 103 rows move, the drained-queue family reports there for the first time (100.0/100.0/99.6 against measured 100.0/100.0/99.6), two critical false positives arrive with it, and the verdicts go 54 PASS / 5 UNMEASURED / 5 critical to 57 / 0 / 7. A new non-partial fixture prices the majors' difference: an index rebuilt while its table was empty and then reloaded reads `unmeasured` on 17.11, where `RelationSetNewRelfilenumber` leaves `-1`, and **100.0% waste on a dense 21 MB index** on 12.2, where it leaves 0.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 17? (unverified)](v17/questions/indexing/attach-partition-index-drops.md) - Fully reviewed distinction between core and custom behavior: the built-in table ATTACH path never drops an existing index, but an event trigger or `ProcessUtility_hook` can issue an independent `DROP INDEX`. Covers parser/utility dispatch, parent/child locks that serialize concurrent DROP, rollback, exact `CompareIndexInfo` rules (matching partial/expression indexes can be reused; exclusion indexes never match), `pg_inherits`/`pg_class`/`pg_depend` re-parenting, physical and multilevel creation, `ALTER INDEX ... ATTACH PARTITION`, generated catalog/parser and AM/contrib boundaries, test gaps, and the meaningful changes since v12. An exact-pin smoke test exposed an open multilevel validity edge: recursive creation can absorb an invalid leaf while the pre-existing top parent index remains valid.
- [Detecting Inflated Non-B-Tree Indexes From Catalogs and a COMMENT-Stored Baseline in PostgreSQL 17 (unverified)](v17/questions/indexing/non-btree-index-inflation-comment-baseline.md) - A `REINDEX`-candidate heuristic for HASH, GIN, GiST, SP-GiST and BRIN whose only persistent state is an `@idxmaint:` JSON payload appended to the index's own `COMMENT ON INDEX`, which survives both `REINDEX` forms because `index_concurrently_swap` explicitly moves the `pg_description` row. Rather than comparing current size to baseline size, it predicts the size of a fresh rebuild for the current logical population, per access method: indexed tuples for hash/GiST/SP-GiST, tuples x `avg_width` for GIN, and `ceil(table relpages / pages_per_range)` for BRIN. Validated on an isolated 17.11 server over a 13-cell matrix with `REINDEX INDEX` as ground truth: 7 flagged candidates reclaimed 37.7-85.7%, four controls were correctly left alone, and `1 - 1/size_inflation` predicted the actual reclaimed fraction to 0.0 points on 7 of 13 cells and within 2.5 on 12 of 13; all three full runs produced identical (B, post-churn, C, R) quadruples. Documents three catalog facts the design turns on - an index's `reltuples` means the AM's entry count after `CREATE INDEX`, the table's row estimate after `ANALYZE` (`tupleFract` is 1.0 for plain indexes) and the AM's `num_index_tuples` after `VACUUM`, a 2x gap for GIN and 10,000x for BRIN; that no per-table statistics reset timestamp exists in v17, so resets are caught only by counter monotonicity; and that a BRIN build-time `reltuples` is not a range count at all but the number of (participant, range) pairs a parallel build produced - measured 23 serial, 44-45 default and 102 with four workers on one 23-range index, because each participant counts its own partial summaries and the leader's union never recounts. Two measured results contradict the brief: `brin_desummarize_range()` + `brin_summarize_range()` on all 318 ranges grew a churned `minmax_multi` index 71% (114,688 to 196,608 bytes) where `REINDEX` returned it to 114,688, and flushing a GIN pending list grew the index from 16,654,336 to 21,905,408 bytes. Includes capture/parse/evaluate SQL, all 13 reproducible cells, measurement queries, experimental per-AM thresholds, and fourteen open questions - among them that the BRIN arm is unvalidated against any true positive. Reviewed 2026-08-25 on the same pin and the same restarted cluster: three run-3 figures in a run-4 table corrected, the `reltuples` progression and the statistics-reset output re-measured because their recorded evidence was missing (both confirmed), the BRIN over-count mechanism corrected against source, and one access-method guard added after a 3x-bloated B-tree scored `none` - proven inert on 17 of 19 baselined indexes. Re-run in full the same day: the matrix three more times (runs 5-7) plus every probe, scored by the published statement - all 13 quadruples, reclaimed bytes and recommendations byte-identical to run 4, so six runs across two days agree; 9 of 13 inflation figures are bit-identical and the 4 that drift move at most 2.6%, so `pf_shift` (8.976-9.152 against a true 9.00x) and c10's churn ratio (5.31-5.48) are now given as ranges. Every probe reproduced, including the `reltuples` probe reproducing its own aborted-transaction bug while its corrected form returned the filed 43/200000/22 exactly; the invalid-index arm was re-demonstrated on a hash index because the guard pre-empts it for B-tree; and a parallel BRIN `reltuples` proved non-reproducible (43-46 over eight default builds, 102 then 90 with four workers, 23 serial both days).
- [A COMMENT-Stored Baseline and Normalized Index Growth for Finding GIN Indexes That Need REINDEX CONCURRENTLY in PostgreSQL 17 (unverified)](v17/questions/indexing/gin-reindex-normalized-growth-comment-baseline.md) - A GIN-only `REINDEX CONCURRENTLY` candidate heuristic whose entire persistent state is a 133-byte `@ginbase:` JSON payload appended to the index's own `COMMENT ON INDEX` - nine flat fields, no table, no extension - and whose normalizer is the *table's* `reltuples`, because a GIN index's own `reltuples` reads 1000000 / 200000 / 100000 / 500000 on one unchanged index over `CREATE INDEX` / `ANALYZE` / `VACUUM` / `REINDEX`, a 10x swing set by whichever command wrote last. The premise holds: nothing under `src/backend/access/gin/` calls `RelationTruncate`, so `ginvacuumcleanup` returns recyclable pages to the free space map and never to the filesystem, making `pg_relation_size` a high-water mark that only `REINDEX` lowers. Scored on a 12-cell matrix run three times against a 17.11 server built from the pin, with `REINDEX INDEX` as ground truth: **right on 10 of 12 cells**, `1 - 1/normalized_index_growth` predicting the reclaimed fraction to within 0.05 points on 5 of the 9 evaluable cells (83.82/83.82, 54.63/54.63, 57.18/57.14, 11.91/11.95, 0.00/0.00), and runs 2 and 3 byte-identical. The three published statements were then extracted from the page and re-run verbatim in a fourth database on a differently named index, where the full capture -> churn -> evaluate -> rebuild -> re-capture lifecycle worked and the prediction came out at **55.66% against a measured 55.66%**. **Four results contradict the brief.** The `index size remains >= 75-80% of baseline` clause is *vacuous* on GIN - `index_size_ratio >= 1` always holds on an un-rebuilt index, so the shrinkage rule reduces to `heap_tuple_ratio <= 0.50` and thereby forces `normalized >= 2.0`, a strict subset of the growth rule that can never fire on its own. "Both grew 50%, so normalized = 1.0, so do nothing" does not happen: growing the table 50% by ordinary `INSERT` grew the index 120% (20,594,688 -> 45,391,872) for `normalized` 1.4694, and `REINDEX` then reclaimed 39.51% - a true positive the brief's 1.50 threshold demotes to `watch`. A fresh GIN build is **not linear in heap tuples**, so `heap_tuple_ratio` is a poor normalizer: rebuilt over one 10,000-key universe the same index measured 7,495,680 / 13,729,792 / 20,594,688 / 82,329,600 / 82,329,600 / 246,169,600 bytes at 250k / 500k / 1M / 2M / 4M / 8M rows, quadrupling between 1M and 2M when every posting list converts to a posting tree (`n_data_pages` 0 -> 10,000, `n_entry_pages` 2,513 -> 49) and then adding zero bytes to 4M - which is exactly why the +100%-same-keys cell over-predicted 67.65% against an actual 35.33% while the +100%-new-keys cell, whose lists stayed inline, predicted 11.91 against 11.95. And the heuristic has a systematic blind spot sized by `gin_pending_list_limit`, not by any ratio: a paired probe added 150,000 rows to two identical 600,000-row tables and the `fastupdate = on` index grew 515 pages (4,218,880 bytes) while the `fastupdate = off` index grew **nothing at all**; `VACUUM` flushed the pending list, the 515 pages went to the FSM and stayed in the file, and `REINDEX` returned both to 16,474,112 - so a six-point growth sweep reads `normalized` 1.1419 / 1.0049 / 0.8374 with 20.39 / 20.39 / 0.48% reclaimable. Two further measured cautions: the baseline itself is a function of `maintenance_work_mem`, since `ginBuildCallback` flushes its accumulator at that budget - the same 2M-row index rebuilt to 109,371,392 bytes at 64MB against 82,329,600 at 256MB and 1GB, 32.8% apart - and the first stale-statistics guard had to be **withdrawn** because `n_mod_since_analyze` read 600000 while the same `VACUUM (ANALYZE)` had correctly set `reltuples` to 400000: `pgstat_report_analyze` zeroes the counter but a backend's pending counts are applied additively afterwards, and non-forced flushes are rate-limited to `PGSTAT_MIN_INTERVAL` (1000 ms), reproduced independently at 1 of 4 back-to-back `DELETE` + `VACUUM (ANALYZE)` pairs with `n_live_tup` clamped to 0. The veto that replaced it, an `analyze_count` comparison, is what caught the one cell whose ratios were silently meaningless - 60% deleted with no `ANALYZE`, reading `normalized` 1.0000 over a real 46.66% reclaim. Ships the four SQL examples the brief asks for (record, read, ratios, verdict), an 11-rung verdict ladder whose gate order was corrected on evidence, 11 proven edge cases (`REINDEX CONCURRENTLY` moving the `pg_description` row 16900 -> 16904, `TRUNCATE` refusal, `pg_dump` carrying the payload, non-owner refusal at SQLSTATE 42501, `ShareUpdateExclusiveLock` on the index only), and fourteen open questions - including that partial and multi-column GIN indexes were never tested, where the table `reltuples` is not the indexed population at all. **Reviewed 2026-08-27 against a second 17.11 built from the same pin**, with corrections made in place: all 137 citations resolve, are in range and version-correct, and the three published statements still extract to exactly 41 / 18 / 76 lines and run verbatim. The fixture SQL was never published so the absolute byte sizes could not return, but every fixture-independent claim did - `churn_ratio` 2.0000, `heap_tuple_ratio` 1.0000, the `candidate` verdict, `btu` 1200000, the `insufficient churn` re-evaluation, the exact `reltuples` swing 1000000 / 200000 / 100000 / 500000, and the whole `fastupdate` pairing (310 pending pages, **515** FSM pages, **4,218,880 bytes** on `fastupdate = on` against **nothing at all** on `fastupdate = off`) - with the prediction identity landing at 60.90 against a measured 60.88. Three findings: the comment-size arithmetic was wrong (142 and 199 bytes, not 143 and 192, the latter contradicting the page's own E1 row of 196); `VACUUM` is only a *conditional* writer of the index's `reltuples`, since the `pg_class` write lives in `update_relstats_all_indexes` and is skipped whenever the heap scan skipped a page, measured as a second plain `VACUUM` leaving 500000 where `DISABLE_PAGE_SKIPPING` wrote 100000; and one `maintenance_work_mem` row is off by exactly one page against a metapage invariant that held with residual 0 on six fresh builds, now open question 14.
- [Measuring Wasted and Reclaimable Bytes in a GIN Index With Contrib Extensions on PostgreSQL 17 (unverified)](v17/questions/indexing/gin-index-wasted-space-contrib.md) - No single contrib function can measure GIN waste: `pgstattuple` refuses GIN outright (`index "..." (gin index) is not supported`, and an *invalid* GIN index is refused earlier still, as `is not valid`) and `pgstatginindex` reads only `version` / `pending_pages` / `pending_tuples` from block 0 - it reported `0 / 0` for an index whose file was 65% dead pages. The procedure is a `pageinspect` page census: read blocks 1..relpages-1 once each through an `OFFSET 0` lateral (1234 buffer hits against 2468 for the naive two-call form, and one read per block cold, with no `BAS_BULKREAD` ring), feed the same `bytea` to `gin_page_opaque_info` and `page_header`, classify by the flags array, and report whole-page waste, live-page slack (`pd_upper - pd_lower`, GIN's own `GinDataLeafPageGetFreeSpace`) split into entry-tree and posting-tree bytes, and pending-list bytes - never their sum. Three cross-checks: `pg_freespace` at **8160** per free page (not 8191, and not `freespace.c`'s own stale 8164 comment), `VACUUM VERBOSE`'s fourth index number, and the metapage's `n_entry_pages`/`n_data_pages`, with `census_total_pages = blocks` as the statement's self-check. **Reviewed end to end on 2026-08-26** against a 17.11 server rebuilt from the pin (the first sandbox had been deleted): every citation re-read, the seven fixtures published as SQL, and three of them - the empty, mass-deleted and half-emptied posting-tree cases - reproducing the first run's figures byte for byte, including `898 in total, 768 newly deleted, 768 currently deleted, 0 reusable` then `0 / 0 / 768 reusable` only after three transaction ids were consumed. `waste + slack` was an upper bound on the reclaimed bytes in 7 of 7 fixtures; `waste` alone was not a lower bound in 1 of 7 (64.64% dead against 58.05% reclaimed, the aged 268-block core rebuilding to 318). Entry-page slack is growth room, not waste - a fresh control reads 48.05% slack and reclaims nothing, and a rebuilt index absorbed 50,000 rows with zero growth in either tree - while posting-tree slack does track the truth (51.34% against 46.33%). New in the review: the payload-and-fill model misses by +19.94% when churn replaces the key population (dead entry tuples are payload a rebuild discards) against +0.07% when it does not; dead pages are demonstrably reused (491 of them absorbed a pending list for +26 blocks); a flush can leave the file size unchanged; and on a physical standby `gin_clean_pending_list` and `VACUUM` are both refused while `pgstatginindex` and the raw-page functions still answer. Also documents the four-state pending-list lifecycle, the superuser-versus-`pg_stat_scan_tables` split, invalid / partitioned / other-session-temp behaviour including `pg_freespace` answering where the other two refuse, the absent GIN verifier in v17 amcheck, and cancellations at 2001.272 ms (`lock_timeout`) and 1500.477 ms (`statement_timeout`). Thirteen open questions. A follow-up round closed **eight of the thirteen open questions**: one held `REPEATABLE READ` snapshot keeps 768 dead pages out of the FSM across two extra VACUUMs and six xids, and one VACUUM after its release recycles them all **with no new xids**; the census's self-check is blind to concurrent writers (25 of 25 passed while `pending_pages` disagreed with `meta_pending_pages` in 19 of 25 and the block count went stale by up to 664 blocks); autoanalyze alone flushes a 491-page pending list in under 10 s, growing the file 836 -> 958 blocks (the GUC is `PGC_SIGHUP`, so a reload suffices); `REINDEX CONCURRENTLY` yields the identical byte count as `REINDEX`; five further opclasses and shapes take the bounds to 12 of 12 upper and 11 of 12 lower, with a 0.13-point margin on `btree_gin` and fresh-build fill spanning 51.30% to 72.11%; the payload-and-fill failure mode reproduces at +18.12% on a multicolumn index whose churn replaced both key columns; killing the server mid-insert produces real all-zero pages (4 and 7); and a census evicts, holding all 6,665 pages of an index resident where a seq scan of a 4,092-block table leaves 98, then losing 2,942 to the next censuses. Eleven open questions remain. A third round attacked those eleven with measurements, leaving **ten (two closed, six narrowed, one new)**: the published fixtures reproduce byte for byte in two virgin databases (only `prune_xid` moves); the payload model's error is linear in the dead-key share (+12.53 points per 25% of keys replaced, five points on a straight line); an entry-tuple probe read from `pd_lower` counts the index's keys exactly and measures the dead-key population (50,028 against 1 on the two churn shapes), though correcting the model needs a dead-tuple size no contrib function exposes; a second index breaks the lower bound (21.66% dead against 19.25% reclaimed), and the bound is an identity - waste under-reports exactly when the aged in-use core is smaller than its rebuild; a flush cannot reuse the pages it frees, because `RecordPageWithFreeSpace` writes only the bottom FSM level while `fsm_search` starts at the root, and the pending list spends the free stock first (0 free -> +736 blocks, 736 free -> +0); a concurrent VACUUM defeats every cross-check (0 to 2,294 dead pages against a truth of 2,368, all checks passing), while re-reading the file size catches writers 13 of 14 where the metapage check catches 0 of 14; `REINDEX CONCURRENTLY` matches plain `REINDEX` byte for byte under load; the tally reaches 26 fixtures at 26 of 26 upper and 24 of 26 lower; and a census does **not** evict a hot working set - it spends its usage count, taking a 3,704-page hot set from `usagecount` 5 to 1 while destroying a once-read set outright. A third follow-up makes the same statements run on **PostgreSQL 12** and re-runs the whole programme on both majors, from two servers built out of tree from this repo's own pins. Three edits suffice - `get_raw_page(..., blkno::int)` (v12's `pageinspect` takes `int4`; `f18aa1b2039` widened it in `REL_14_0`), a `pagesize = 0` arm ahead of the flags tests, and the same guard on the probe's `'{}'` filter - and the FSM check and size bracket need none, both deriving 8160. The one non-cosmetic edit is decisive: on 12.2 `gin_page_opaque_info` returns `flags = {}` rather than a NULL row for an all-zero page (`cd4868a5700` is `Backpatch-through: 10`, and its 12-branch backport is first tagged `REL_12_11`, after the 12.2 pin), so the filed text counts two hand-appended zeroed blocks as **entry pages and reports 0 waste bytes** where 17.11 reports 2 new pages and 16,384, and the probe's downlink count reads 41 against 53; a `dd`-zeroed metapage aborts the whole multi-index census on 12.2 where 17.11 returns NULLs. **26 of 27 scored fixtures came out byte-identical on the two majors**, including all seven published ones and every filed figure, with identical tallies (lower bound 24 of 27, upper 25 of 27, upper-plus-pending 26 of 27), and so did the lifecycle, the three-VACUUM horizon sequence with `prune_xid` equal to the holder's `backend_xmin`, the five flush rounds, privileges, both timeouts, the standby refusals and rebuild equivalence. Two headline claims are corrected: `waste + slack` is **not** an upper bound when the dead-key population is large (43.43% against 50.00% reclaimed on an 800k-row `jsonb_path_ops` index carrying 819,770 dead entry tuples) or when a pending list is live, and a census **does** evict a hot working set once the target exceeds the cache. The one fixture that differs exposes a bigger result: **a fresh GIN build's size is a function of `maintenance_work_mem`** - 34,611,200 bytes at 64MB against 50,814,976 at 96MB and above, on both majors, with payload constant and all the movement in `entry_slack` - because `ginBuildCallback` dumps the accumulator on a budget measured with `GetMemoryChunkSpace`, and 17.11 fits about 7% more into the same setting. Open question 7 is closed: the round-five entry-tree explosion is slack exhaustion, entry slack falling by an almost constant 395 kB a round until the splits arrive at once. Also: 12.2's `pgstatginindex` **answers** for an invalid index where 17.11 refuses it (`13503eb5905`, `REL_17_0`), and the race-detector ranking depends on the writer, not the version - the metapage check caught 23 of 25 under four `fastupdate` writers where the size bracket caught 4.
- [Reading an Index's Entry Count From the Catalogs, for Every Index Type, in PostgreSQL 17 (unverified)](v17/questions/indexing/index-entry-count-from-catalogs.md) - `pg_class.reltuples` on the index's own row is the only catalog column in 17.11 that ever holds an index's entry count — ruled in against `pg_class`, `pg_index`, `pg_am`, `pg_statistic` and the extended-statistics catalogs, and against every `pg_stat_*`/`pg_statio_*` view, none of which reports a stored-entry population (`idx_tup_read` is a scan counter). What one unit means is set by the access method, not by the catalog: B-tree counts heap TIDs pre-deduplication (200000 against **2000** on-disk index tuples on a 200-duplicates-per-key index), hash silently drops every NULL-keyed row (150000 against B-tree's 200000 on the same 25%-NULL column), GIN sums extracted entries over rows *and* indexed columns (1178667 = 580766 + 597901, with one placeholder entry for NULL and one for `'{}'`), BRIN counts summarized page ranges (116 against 200000 rows), and GiST/SP-GiST/contrib-bloom count one entry per row. Publishes a catalogs-plus-`pg_stat`-views statement, byte-identical to the one tested, whose verdicts are only claims decidable from catalogs: two hard bounds (BRIN cannot exceed `ceil(table relpages / pages_per_range)`; GIN cannot fall below `rows x indnatts`) plus a `suspect` flag for equality with the table row estimate. Scored against `pageinspect` and plain-SQL ground truth immediately after a serial rebuild: **14 of 14 byte-exact**, reproduced from scratch in a second database, and 15 of 16 at 180,000 rows. Three writers disagree — `CREATE INDEX` writes `IndexBuildResult.index_tuples`, VACUUM writes `num_index_tuples` only for a non-NULL, non-estimated `amvacuumcleanup` result, and `ANALYZE` unconditionally overwrites every index with `ceil(tupleFract * totalrows)` — so one VACUUM replaces a GIN entry count with the heap row count (580766 -> 200000) and one `ANALYZE` flattens all seven access methods to the table estimate, taking BRIN from 116 to 180000 and re-adding to hash the 40,000 NULL rows it never indexed. A forgery probe (every value set to 777777, then a plain VACUUM) decides provenance per access method: btree, hash and GIN keep the forgery, GiST, SP-GiST, BRIN and bloom repair it, and `VACUUM (DISABLE_PAGE_SKIPPING)` then repairs GIN too — pinning the behaviour to `estimated_count = scanned_pages < rel_pages`. One BRIN index over one table yields four readings against 116 summaries on disk: 116 serial, 319 parallel (worker slices, nondeterministic at 116/222/325/328/311 over 0/1/2/4/8 workers and 317/319/328 across three four-worker builds), 12 for a partial index whose empty ranges are never counted, and 115 after a VACUUM that skips the trailing partial range. Also: GIN's metapage holds an accurate distinct-key count (100/98/198) that never reaches `pg_class`; a newly created index reads `0` while its table reads `-1`, because `RelationBuildLocalRelation` `palloc0`s `rd_rel` and `index_create` skips the `reltuples = -1` step tables get, so `0` is ambiguous between empty and never-built (measured on an empty-table index, a partitioned index and a failed-CIC invalid index); `-1` appears only after a storage reset; `float4` loses the low bit above 2^24, reading 20000000 for a measured 20,000,001 entries; and ordering `pg_stat_all_tables` timestamps is proven unsound twice, because `VACUUM (INDEX_CLEANUP OFF)` advances `last_vacuum` without writing index stats and a plain VACUUM can repair BRIN while leaving btree and hash at their `ANALYZE` values. Nine open questions, including a doc-versus-source discrepancy: `indexam.sgml` says the stats are reported by `VACUUM VERBOSE`, but the `ereport` carrying `num_index_tuples` is `DEBUG2` at every call site and v17's VERBOSE per-index line is page-only. Reviewed on 2026-08-27 against the same pin on a cluster created from scratch, since the original sandbox's data directory was gone: all 125 citations re-read and re-resolved (three labels corrected against their own line ranges), the published statement proven byte-identical to the tested one at 68 lines, and **every measured figure reproduced byte for byte** except the three that cannot be — the nondeterministic parallel BRIN count (a second sweep read 116/221/328/305/326 and three four-worker builds 326/330/325, bracketing 305-330), the two sample-derived `ANALYZE` readings on partial indexes, and wall-clock timestamps, which the page now states as an ordering instead. Four findings were added: the serial BRIN build's final `form_and_insert_tuple` has no empty-range guard, so a BRIN index on an empty table reads `1`; every AM allocates at least one page at build time (1 btree/GiST/bloom, 2 GIN, 3 SP-GiST/BRIN, 4 hash), which makes the statement's `zero: no storage yet` branch unreachable for a valid non-partitioned index; a non-partial BRIN undercount of 1 against 8 summaries on an all-dead 256-block heap; and a `ct.relpages = 0` guard that silently disables the BRIN bound. A new verdict census shows which branches actually fire, and that only two of them prove an overwrite: the BRIN bound (3 of 3 after `ANALYZE`) and the GIN bound, which needs `indnatts >= 2` because a single-column GIN floor is exactly the value an overwrite writes.

#### Storage and Vacuum

- [How Much I/O a VACUUM FULL Performs on a Multi-GB, Near-Empty Heap in PostgreSQL 17 (unverified)](v17/questions/storage-and-vacuum/vacuum-full-io-on-near-empty-heap.md) - Reads scale with the heap's full physical size and writes with the live data only: emptiness buys nothing on the read side, because the `VACUUM FULL` rewrite is `CLUSTER` without an index and its `SnapshotAny` sequential scan never consults the visibility map (zero `visibilitymap`/`VM_ALL_`/`all_visible` matches in `cluster.c` and `rewriteheap.c`; the only two in `heapam_handler.c` are an `#include` and the bitmap-scan callback). Measured on an isolated 17.11 server over four byte-identical 200-million-row fixtures (7,249,559,552 bytes / 884,956 blocks, 99.9% deleted with the tail kept so `VACUUM` cannot truncate, then `VACUUM` and `VACUUM FREEZE` leaving all 884,956 pages all-visible and all-frozen): `VACUUM FULL` read **884,956 blocks with zero buffer hits** and the block layer delivered **7,249,559,552 bytes, the file size to the byte**, in 2.99 s, while a plain `VACUUM` on the identical heap read **0 blocks** in 3.17 ms — the same map that let `VACUUM` read nothing let the rewrite read 6.75 GB. The v17-specific finding is the *shape* of those reads: the rewrite's scan now goes through the read stream, so 884,956 blocks arrived in **55,361 read system calls** (15.985 blocks each) at the default `io_combine_limit = 128kB`, and a six-point sweep on a 44,248-block fixture moved the call count 28,179 -> 976 across `8kB` -> `256kB` while `heap_blks_read + heap_blks_hit` stayed at exactly 44,248 every time. Also measured: the new heap at 885 blocks / 7,249,920 bytes (999.95:1 read-to-write), 28 `XLOG/FPI` records carrying 885 full-page images because `log_newpages` batches 32 per record, WAL collapsing 7,353,992 -> 110,008 bytes at `wal_level = minimal` with the reads unchanged, a warm-cache run with identical 884,956 reads and 55,361 calls but `read_bytes = 0` and 1.20 s, the zero-row extreme reading all 6.75 GB to produce a **0-byte** new heap in three write calls, an indexed run decomposing to 885,841 = 884,956 + 885 with `idx_blks_read` +1, `pg_stat_progress_cluster.heap_blks_scanned` reporting 0 of 884,956 for 427 of 434 samples yet ending at exactly 884,956 (v17's end-of-scan fix, back-patched to 12.6), and emptied pages left with `pd_lower = 24` and **zero** line pointers so `pgstattuple` reports 99.56% free. Documents the option rejections (`PARALLEL`, `DISABLE_PAGE_SKIPPING`, `BUFFER_USAGE_LIMIT`), the absence of any cost-based throttling, the bulk-write path that hides the new heap's extends from `pg_stat_io` and defers its fsync to the checkpointer, and a since-v12 section attributing ten changes to their first release tags.

#### Replication and WAL

- [How Bi-Directional Logical Replication Works in PostgreSQL 17, and All Related Commits by Minor Version (unverified)](v17/questions/replication-and-wal/bidirectional-logical-replication.md) - Explains how two v17 nodes replicate to each other without loops: subscriptions created with `origin = none` make the publisher's `pgoutput` origin filter skip WAL changes that carry a replication origin ID, which is exactly the tag every apply worker stamps on the transactions it replays; covers the subscription `origin` parameter and `pg_subscription.suborigin`, WAL origin tagging (`XLOG_INCLUDE_ORIGIN` -> `XLogRecordAssemble`), decode-time filtering (`pgoutput_origin_filter`, `DecodeTXNNeedSkip`), the initial-sync `copy_data`/`origin = none` WARNING, setup pattern, and limitations (manual conflicts, no DDL/sequence replication); plus the complete related commit history grouped by minor version: 16-cycle feature commits, the 17.0 filter-state fix, the 17.5 partition-aware WARNING fix, and the 17.10 CVE-2026-6638 SQL-injection fix, with foundations, incidental, and adjacent commits listed separately.

#### Server Administration

- [PostgreSQL 17 Contrib Extensions (unverified)](v17/questions/server-administration/contrib-extensions.md) - Lists all 53 control-file-backed contrib extensions in PostgreSQL 17, with cited explanations covering install scope, data types, index/search helpers, diagnostics, FDWs, procedural-language transforms, and SPI trigger examples.
- [GUC Default-Value Changes Since PostgreSQL 12 (unverified)](v17/questions/server-administration/guc-default-changes-since-v12.md) - Summarizes the seven GUCs present in both v12 and v17 whose built-in default changed (with old/new value, introducing major version, and restart/reload/session apply scope), verified against v17 `guc_tables.c`/`config.sgml`/`postgresql.conf.sample` and pinned to introducing versions via the v17 checkout's own commit history; separates out sample-only, type, added/removed-setting, and range-only changes that are not default-value changes, with explicit test-coverage notes.

### PostgreSQL 14.24

- [v14/index](v14/index.md) - Active version landing page. Source checkout pinned to the `REL_14_STABLE` tip commit `a92fbdfb830046e907813e9067b2c9de9708d600` (`REL_14_24-6-ga92fbdfb830`, six commits past the 14.24 release stamp; source version 14.24).
- [PostgreSQL 14 Codebase Navigation Guide (unverified)](v14/codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v14 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.

#### Query Planning

- [Performance Implications of Functions and Procedures in a WHERE Clause in PostgreSQL 14, and How to Minimize the Overhead (unverified)](v14/questions/query-planning/functions-procedures-in-where-clause.md) - Fully source-reviewed walkthrough of procedure, set-returning/aggregate/window, and (since 14.24, `155dacbc547`) `internal`-typed argument/result rejection in `WHERE`, the utility `CALL` path and its separately evaluated arguments, and scalar-function multiplicity across residual scan tuples and join pairs. Covers short-circuiting, `STRICT` null skips, SQL inlining, constant folding, pseudoconstant gating and rescans, index runtime keys, cost/selectivity and expression statistics, plain-index search-key limits plus expression indexes/generated columns/planner support, the distinct security-promotion and runtime-ordering rules (including the leakproof cost cutoff), parallel safety, JIT, `track_functions`, generated `pg_proc_d.h`, extension hooks/bitcode, tests, and source-backed mitigations.

#### Storage and Vacuum

- [How MultiXact Works in PostgreSQL 14, and How Foreign Keys and Other Operations Degrade Performance When the Local Cache Spills to Secondary Storage (unverified)](v14/questions/storage-and-vacuum/multixact-foreign-keys-cache-spill.md) - Fully source-reviewed explanation of MultiXact as the shared-row-lock mechanism: a `MultiXactId` in a tuple's `xmax` standing for a set of `(TransactionId, MultiXactStatus)` members, the two SLRUs (offsets/members) backed by `pg_multixact/`, the shared `MultiXactStateData` and per-backend oldest-member/oldest-visible arrays, the create path (`MultiXactIdCreate`/`MultiXactIdExpand` -> `MultiXactIdCreateFromMembers` -> `GetNewMultiXactId`/`RecordNewMultiXact`, writing offsets then members under exclusive SLRU locks), and the read path (`GetMultiXactIdMembers`); the backend-local 256-entry, per-transaction LRU cache (`mXactCacheEnt`, `MAX_CACHE_ENTRIES`, the `MXactContext` under `TopTransactionContext`, and no cross-backend sharing), whose LRU tail eviction only `pfree`s memory rather than spilling an entry, plus the two-level miss path from local cache to the shared 8-offset/16-member SLRU pools and only then, on an SLRU miss, a filesystem `pg_pread` that may be served by the OS page cache; how foreign keys create MultiXacts (RI check `SELECT ... FOR KEY SHARE OF x` -> `heapam_tuple_lock` -> `heap_lock_tuple` -> `compute_new_xmax_infomask`, with the hot-parent-row escalation and the `fk-contention` isolation test) plus the other creators (compatible explicit row locks, UPDATE/DELETE of already-locked rows, and VACUUM MultiXact freezing); three distinct degradation paths (hot-row and full-member-array creation work; local-cache misses reaching exclusive SLRU control locks, per-buffer waits, and possible file reads/writes; and member/ID growth driving aggressive vacuum plus the separate MXID-counter and member-space hard stops); observability through `pg_stat_slru`, exact generated `pg_stat_activity` wait names, `mxid_age()`, and targeted `pg_get_multixact_members()`, with explicit attribution limits; and the vacuum GUCs plus per-table storage parameters with apply scopes (`vacuum_multixact_freeze_min_age`, `vacuum_multixact_freeze_table_age`, and `vacuum_multixact_failsafe_age` session/transaction-scoped with configuration-default changes taking reload; `autovacuum_multixact_freeze_max_age` restart-only; per-table `autovacuum_multixact_freeze_*` reloptions via `ALTER TABLE`), plus build/generated/WAL/contrib boundaries and the seven MultiXact isolation specs passing on the previous 14.23 pin; v14 has no GUC to resize the SLRU buffers or local cache.

#### Server Administration

- [Row-Level Security (RLS) in PostgreSQL 14: Implementation, Scalability and Performance, and Settings (unverified)](v14/questions/server-administration/row-level-security-rls.md) - Fully source-reviewed implementation and follow-up analysis covering rewrite/WCO layering (including `ON CONFLICT`), policy and generated-catalog storage, relcache ordering, bypass/identity rules, index/pruning/partial-index, selectivity/cost and parallel-safety effects, and scalar-subquery InitPlans as lazy zero-or-once evaluations per surviving occurrence and outer-plan execution. The function-wrapper review covers RLS rewrite copies, residual filters, runtime keys and rescans, lossy rechecks, skew-sensitive estimates, partial indexes, volatility, privileges/leakproofness, and exact-pin call-count tests. Also covers aggregation mitigations, conditional MultiXact paths, and COPY/CTAS/RI/error boundaries. Distinguishes logical-replication initial-sync RLS from ongoing decoded WAL and subscriber apply, and records that 14.24 closed the former same-role plan-cache staleness window: `f4174aa84a3` (CVE-2026-14666) registers `PlanCacheRoleCallback` on `pg_auth_members`/`pg_authid`/`pg_database`, so membership, `INHERIT`, `BYPASSRLS`, `SUPERUSER`, and database-owner changes now invalidate role-dependent saved plans without `DISCARD PLANS`, with the superseded 14.23 reproduction and the two residual scopes (saved plans only; other databases' `pg_database` rows ignored) stated explicitly. Also covers the new 14.24 policy-expression type-`USAGE` requirement (`1a358b8f2a2`, CVE-2026-6470). Includes all direct GUC, table, role, policy, hook, inspection, dump/restore, and replication controls plus explicit test gaps.

### PostgreSQL 12.2

- [v12/index](v12/index.md) - Legacy version landing page. Source checkout pinned to `REL_12_STABLE` commit `45b88269a353ad93744772791feb6d01bc7e1e42`.
- [PostgreSQL 12 Codebase Navigation Guide (unverified)](v12/codebase-navigation-guide.md) - Reviewed root-level map of the pinned v12 tree: build ownership; backend and standalone entry; simple and extended Parse/Bind/Execute/Sync flow; raw parse, analysis, RIR rewrite, planning, portal strategies, and full executor lifecycle; utility/event-trigger routing and error recovery; plan-cache, portal, planner, executor, relcache, GUC, and AM structures; custom AM registration and packaged/core extension boundaries; generated catalog/parser/fmgr/LWLock artifacts; and regression, isolation, TAP, module, and doc surfaces.

#### Query Planning

- [Foreign-Key Join Optimization for Two-Table Joins (unverified)](v12/questions/query-planning/fk-join-optimization-two-tables.md) - How the v12 planner uses foreign-key constraints when joining two tables.
- [Query Planner Statistics Sources in PostgreSQL 12 (unverified)](v12/questions/query-planning/query-planner-statistics-sources.md) - Fully source-reviewed distinction between cumulative monitoring and planner input: no direct `pg_stat_all_tables`, `pg_stats`, or `pg_stats_ext` feedback; direct `pg_class`, `pg_statistic`, and built extended-statistics data; current physical size; ordinary-versus-partial index sizing; inherited and expression-index statistics, including partial-index correlation; missing/stale/security/error behavior; auto-analyze's indirect collector path and planner endpoint probes that increment monitoring counters without consuming them; index/FK/constraint/type/operator/function metadata; table/index AM, FDW, hook, contrib, generated-header/syscache/relcache boundaries; and direct regression coverage plus explicit negative-test absence.
- [Planner Penalties for Bloated Indexes in PostgreSQL 12 (unverified)](v12/questions/query-planning/bloated-indexes-query-planner.md) - Explains how v12 penalizes B-tree bloat indirectly through physical index pages, B-tree height, cache/page-cost modeling, and executor leaf-page walking; scopes the mechanisms across access methods via the `amcostestimate` dispatch (page-count penalty shared by B-tree/hash/GiST/SP-GiST through `genericcostestimate()`; measured tree-height penalty B-tree-only, with GiST/SP-GiST charging the same descent formula on a page-count-estimated height and hash charging no descent cost; GIN/BRIN use separate models); expands how `RelationGetNumberOfBlocks()` obtains `index->pages` through storage-manager file-size probes rather than index-page scans; adds a proposed `avg_leaf_density` / `leaf_fragmentation` triage heuristic for REINDEX candidates. A filed follow-up answers when the planner discards a GIN index and uses a B-tree instead, separating three gates: clause matching against the GIN operator family in `match_opclause_to_indexcol()` (with the `get_index_clause_from_support()` escape hatch and the core `pg_amop.dat` GIN memberships that carry no `<`/`<=`/`>=`/`>`); plan shape from GIN's AM flags (`amgettuple = NULL` so bitmap-only, no `amcanorder`, `amcanreturn = NULL`, `amsearchnulls`/`amsearcharray`/`amcanparallel` false), which rules out plain index scans, `ORDER BY` pathkeys, index-only scans, `IS NULL`, and native `IN (...)`; and cost, where `gincostestimate()` charges `random_page_cost` for every pending, entry, and data page with no descent shortcut while `genericcostestimate()` charges the B-tree a pro-rata page share. Covers `choose_bitmap_and()` / `add_path()` pruning, the 4X stale-metapage-statistics fallback, the keyless full-index path a partial GIN index can still produce via `amoptionalkey` and `GIN_SEARCH_MODE_EVERYTHING`, the four `CREATE INDEX` AM rejections (unique, INCLUDE, ASC/DESC, exclusion), where GIN still wins, eight settings with exact apply scopes, and the explicit absence of any upstream GIN-versus-B-tree plan comparison or `gincostestimate()` coverage. Exact-pin measurements on 12.2 price the same `n = 42` predicate at `12.22` through GIN and `4.65` through a B-tree on one table with identical statistics — even though the GIN index is 279 pages against 826 — and show a 1177-page `fastupdate` pending list moving GIN's index cost from `12.45` to `4720.55` so the planner drops it from the `BitmapAnd` entirely, with `393 * random_page_cost` reproduced exactly against `pgstatginindex` and `gin_clean_pending_list()`.
- [Comprehensive plan_cache_mode Analysis in PostgreSQL 12 (unverified)](v12/questions/query-planning/plan-cache-mode.md) - Fully reviewed analysis of the v12 `plan_cache_mode` GUC: the enum/`PGC_USERSET`/`GUC_EXPLAIN` definition and exact apply scope, the eight ordered rules in `choose_custom_plan` (with `force_*` sitting behind the one-shot, no-parameter, and transaction-statement rules), the `auto` cost arithmetic including the custom-only `1000 * cpu_operator_cost * (nrelations + 1)` planning charge, the `GetCachedPlan` correction step that builds a generic plan then discards the decision, `PARAM_FLAG_CONST` folding and `var_eq_const` versus `var_eq_non_const` selectivity, the `CachedPlanSource` heuristic fields, every caller that reaches the decision, the retention of `generic_cost`/`num_custom_plans` across invalidation and `DISCARD PLANS`, the per-execution `AcquireExecutorLocks` cost of a generic plan, v12 observability limits (`EXPLAIN EXECUTE`, `EXPLAIN (SETTINGS)`, and a `pg_prepared_statements` view with no plan counters), error paths, build/extension boundaries, the single direct regression test plus explicit gaps, and five ancestor commits from `e6faf910d75` to `ca0b3828504`. Exact-pin measurements on an isolated 12.2 server (with `auto_explain` for nested and protocol-level plans) reproduce the switch at execution 6, prove the rejected generic plan is cached by recovering a stale `Seq Scan` under `enable_seqscan = off`, show `force_custom_plan` failing to replan a parameterless statement, show PL/pgSQL static SQL switching at call 6 while dynamic `EXECUTE ... USING` ignores `force_generic_plan`, contrast `pgbench -M prepared` (5 custom then generic) with `-M extended` (always custom, unnamed statements reset the counters), and measure 2 versus 5 partition locks and 0.65 ms versus 0.006 ms of planning per execution.
- [Table Partitioning Optimizations and Configuration During Query Planning and Execution in PostgreSQL 12 (unverified)](v12/questions/query-planning/partitioning-planning-execution-optimizations.md) - Catalogs the five partitioning-related planner/executor optimizations in v12 and their configuration: partition pruning (plan-time via `prune_append_rel_partitions` before child expansion in `expand_partitioned_rtentry`, and run-time initial + per-scan pruning via `ExecFindInitialMatchingSubPlans`/`ExecFindMatchingSubPlans` on `Append`/`MergeAppend`), with per-strategy differences (HASH needs all keys and equality/`IS NULL`, no null/`DEFAULT` partition; LIST supports a null and `DEFAULT` partition; RANGE routes nulls to `DEFAULT`); constraint exclusion (`relation_excluded_by_constraints`, `partition`/`on`/`off` modes) as the plan-time-only technique for legacy inheritance; partitionwise join (`build_joinrel_partition_info` gate, matching `PartitionScheme` + equal bounds + partition-key equi-join) and partitionwise aggregate (`enable_partitionwise_aggregate`, FULL when `group_by_has_partkey` else PARTIAL); executor tuple routing (`get_partition_for_tuple` per strategy), cross-partition UPDATE row movement (DELETE+INSERT), and per-partition COPY multi-insert batching; plus the four session-scoped GUCs with defaults and the v12 limitation that execution-time pruning covers only `Append`/`MergeAppend`, not `ModifyTable`. Adds a follow-up analysis of locking and random-I/O sensitivity per optimization: only plan-time pruning removes read locks (it prunes before `expand_partitioned_rtentry` opens/locks partitions, whereas `find_all_inheritors` locks all inheritance children before constraint exclusion runs, and `AcquireExecutorLocks` locks every plan-time-surviving partition regardless of run-time pruning; write-path routing takes `RowExclusiveLock` lazily per touched partition), and the scan-eliminating optimizations gain most on high-latency random storage because avoided per-partition index heap fetches are priced at `random_page_cost` (4× `seq_page_cost`), while partitionwise join/aggregate mainly cut CPU/memory/cache misses (v12 costs hash-join spill as sequential and keeps hash aggregation in memory).

#### Indexing

- [Physical Index Statistics, Tuple Counts, and Bytes per Tuple in PostgreSQL 12 (unverified)](v12/questions/indexing/physical-index-statistics-tuple-counts-and-bytes.md) - Fully reviewed at the 12.2 pin. Maps shared `pg_class` and `pg_index` metadata plus persistent metapage, page, and tuple fields for B-tree, hash, GiST, GIN, SP-GiST, BRIN, and contrib Bloom. It separates stored counters, catalog estimates, and page-scan measurements; explains why `pg_relation_size() / reltuples` is allocated bytes per the current `reltuples` unit rather than a universal tuple size; and states which row, entry, summary, and byte calculations are exact, conditional, or unavailable without access-method-specific traversal. The review corrected the `pgstattuple` payload rule (B-tree and hash pass an already-`MAXALIGN`ed `IndexTupleSize()`, so `tuple_len` includes each item's alignment padding, and the bigint averages need a `numeric` cast), the `btm_last_cleanup_num_heap_tuples` writer (bulk delete as well as cleanup), the `ANALYZE` skip guard, `PageIndexTupleDeleteNoCompact()` space accounting, `get_raw_page()`'s superuser requirement, and the `pgstattuple` regression-coverage claim; it also quantifies the BRIN `revmapNumPages` off-by-one and adds the `pd_prune_xid` comment-versus-GIN discrepancy.
- [Calibrating a COMMENT-Stored Bytes-per-Table-Tuple REINDEX Threshold for Every Non-B-Tree Index in PostgreSQL 12 (unverified)](v12/questions/indexing/comment-stored-bytes-per-table-tuple-non-btree.md) - Stores `pg_relation_size(index) / table pg_class.reltuples` in each non-btree index comment at build time and calibrates the percentage increase that best predicts a worthwhile rebuild. Measured on an isolated 12.2 server across 132 cells (22 insert/update/delete workloads x 6 index shapes) with `REINDEX` as ground truth: GiST calibrates cleanly at any threshold in 20-30% (14 TP / 0 FP / 0 FN / 8 TN, and a fresh zero-bloat build drifts only -0.29% to +3.84% from 25k to 1.6M rows); hash (9-11%) and SP-GiST (29-32%) score 100% only while the row count is stable, because a *fresh* hash index's ratio spans -20.31% to +49.42% across the bucket-allocation staircase and SP-GiST's up to +33.08%; GIN needs 50% (`fastupdate = off`) or 75% (on); BRIN must never fire, since `REINDEX` reclaimed exactly 0.00% in 22/22 BRIN cells while the reading swung -87.50% to +9900.00%. Shows that deletes make every access method read `1/(1-f)-1` with no index information, that the GIN pending list moves the reading to +261.73% and a flush moves it *further* to +388.78% while delivering the whole 3,779 -> 1,336 block win a rebuild does not, that hash's build-time bucket estimate spreads the "stable" baseline 10.3% on identical data and leaves 87 unwritten pages that `pg_relation_size` counts but the filesystem never allocated, that BRIN's `pages_per_range` alone spreads the baseline 6.3x, and that stale `reltuples` flips one index between +84.71% and -7.64%; includes a partial-index false negative (-5.02% reading, 44.59% reclaimable), per-AM rebuild costs and query-block payoffs (hash and BRIN: none), and `COMMENT ON INDEX` mechanics verified on the pin.
- [How NULL Values Are Handled in PostgreSQL 12 Indexes (unverified)](v12/questions/indexing/null-values-in-indexes.md) - Explains NULL handling by index access method: B-tree storage, ordering, `IS NULL` search, and unique-index semantics; hash NULL-entry skipping; GiST tuple NULL markers; SP-GiST's separate NULL tree; GIN NULL categories and lack of generic `NullTest` matching; BRIN `hasnulls`/`allnulls` range summaries; and contrib Bloom's no-NULL-search behavior.
- [Pros and Cons of Partial Indexes in PostgreSQL 12 (unverified)](v12/questions/indexing/partial-indexes-pros-cons.md) - Explains partial-index benefits, tradeoffs, ordinary expression indexes as the baseline, partial indexes on expressions, planner predicate implication and expression-key matching, maintenance and operational restrictions, `ANALYZE` row-count and expression-statistics behavior, and how the planner costs partial indexes.
- [How pgstatindex Calculates B-Tree Index Statistics in PostgreSQL 12 (unverified)](v12/questions/indexing/how-pgstatindex-calculates-information.md) - Reviewed exact v12 calculation path: extension 1.4-to-1.5 wiring and privileges, true-root metadata, one-time main-fork length and full physical scan, half-dead/deleted/live page classification, captured `index_size`, physical `avg_leaf_density` including retained `LP_DEAD` storage, backward-link `leaf_fragmentation`, two-decimal formatting, concurrent-scan and integrity-checking limits, generated-header implications, and test gaps.
- [Measuring B-Tree Index Bloat With Core SQL Only in PostgreSQL 12 (unverified)](v12/questions/indexing/btree-index-bloat-core-sql-only.md) - Answers the no-contrib case by turning v12's deterministic B-tree build rule into SQL: because `_bt_buildadd` closes a leaf page as soon as `PageGetFreeSpace()` drops below `BLCKSZ * (100 - fillfactor) / 100`, the size a rebuild would produce is computable from `pg_class`, `pg_stats`, and `pg_options_to_table` alone, and bloat is the difference from `pg_relation_size()`. Four ranked core-only methods with exact-pin accuracy: a 26.7 ms catalog sweep (matched an actual `CREATE INDEX CONCURRENTLY` rebuild to the block on 10 of 14 fixtures, within 2 blocks on 3, −4.6% on one variable-width key); a `pg_column_size` slot measurement that cut that failure to +0.32% from a 1% `TABLESAMPLE`; an index-only-scan `BUFFERS` census that reproduced `pgstatindex.leaf_pages` exactly on all 12 eligible fixtures and `avg_leaf_density` to within 0.14 points with no contrib installed; and the exact sibling-rebuild probe with its CIC restrictions, invalid-index cleanup, WAL and locking costs. Also establishes that core has no page or FSM reader at all and that every relevant contrib module defaults to `superuser = true`, uses `pg_relation_size(idx,'fsm') > 0` as a free deleted-page flag, reads `VACUUM VERBOSE`'s exact page census (and the no-op-VACUUM gap from `_bt_vacuum_needs_cleanup`), and reports six measured failure modes including stale `reltuples` reporting 0.0% on a 89.9%-reclaimable index and a rebuild that made an all-duplicate index 6.6% larger. A filed follow-up compares the core-SQL results with `pgstatindex` over a 54-index matrix (9 bloat types x 3 scales x partial/non-partial): the model is exact against a real rebuild in 39 of 54 cells and within 5 blocks in 47, the index-only-scan census reproduces `leaf_pages` exactly in all 36 cells that pass the `Heap Fetches: 0` precondition and `avg_leaf_density` to within 0.15 points, and `blocks - leaf_est` recovers the non-leaf total exactly, including 2330 deleted pages. Scored as a rebuild-size predictor, `ceil(leaf_pages * avg_leaf_density / fillfactor) + internal_pages + 1` loses to the core-SQL model in 16 of 18 cells and is 994.4% wrong on an `LP_DEAD` fixture where `pgstatindex` reports a healthy 90.06 density on an index that a rebuild shrinks by 90.9%, because physical occupancy counts dead entries as used. The one large model failure is a partial index on a table deleted without VACUUM or ANALYZE (510 blocks, 92.6 points), since partial indexes cannot use `n_live_tup`; one plain `ANALYZE` drops that error to at most one block. A source-only correction then renames the statements' reporting identifiers off the word "bloat" — `wasted` to `wasted_space`, `bloat_pct` to `wasted_space_pct`, the tag to `wiki_btree_wasted_space_sweep`, and the Method C probe index to `wiki_wasted_space_probe` — and establishes why: v12's own definition of a bloated index is per-page ("many empty or nearly-empty pages" in `ref/reindex.sgml`, with `maintenance.sgml`'s partly-emptied-page mechanism), state this file-size-minus-model arithmetic cannot see, while "wasted space" is `ref/copy.sgml`'s phrase for what a maintenance command recovers; no SQL-visible interface in the tree says `bloat` and this checkout has no glossary at all. No number moves, because `AS` only labels and the two label changes cannot reach the expression `ORDER BY` key or the query ID (v12 jumbles the `Query` tree in contrib `pg_stat_statements`, ignores `resname`, and never sees the comment, which the lexer discards) — but the probe rename is an object name in five places and, because v12 keys a utility statement by a hash of its text, does give the `CREATE`/`DROP INDEX CONCURRENTLY` new `pg_stat_statements` entries.
- [Impact of B-Tree Leaf Density (60% vs 90%) on Index Scan Queries in PostgreSQL 12 (unverified)](v12/questions/indexing/leaf-density-60-vs-90-query-impact.md) - Reviewed analysis of the ~1.5x leaf-page ratio and its two channels: planner page-count scaling through `index->pages`/`numIndexPages` and the executor's one-buffer-per-right-link leaf walk (`_bt_steppage` / `_bt_readnextpage` / `_bt_getbuf` / `_bt_readpage`), plus how splits (not deletes) get an index to 60%, buffer-mapping and no-index-prefetch effects, per-query-shape impact for probes, ranges, bitmap and index-only scans, `PGC_USERSET` settings, contrib and generated-catalog-header boundaries, and the test gap. Exact-pin measurements over 1,000,000 keys reproduce 90.06 versus 59.90 density as 2733 versus 4116 leaf pages and 2738 versus 4121 warm buffers, with the whole 5552.01 cost gap equal to 1388 extra blocks at `random_page_cost = 4`; a wide-key fixture shows a 90-to-60 change raising `tree_level` from 2 to 3 and with it the descent charge; a random-insert fixture reaches 65.58% density with no deletions.
- [B-Tree Leaf Density vs Fragmentation Impact on Index Scan I/O in PostgreSQL 12 (unverified)](v12/questions/indexing/leaf-density-vs-fragmentation-index-scan-io.md) - Fully reviewed comparison of density-driven leaf-page count growth versus fragmentation-driven physical-order penalties: `pgstatindex` column definitions, one-buffer-per-right-link scan walking, the planner page-count model, per-level estimate tables and combined multipliers, and the v12 absence of index-page prefetch. Exact-pin measurements show 59.90 versus 90.06 density costing 50.5 % more warm index-only scan buffers while removing 49.95 % fragmentation at matched density moves them 0.5 %, with `pageinspect` jump-distance evidence that `leaf_fragmentation` understates disorder; also covers what both metrics hide (`LP_DEAD` occupancy, half-dead pages still in the chain, unlinked-but-priced deleted pages, `NaN`), session-scoped GUC apply scopes, and a validated survey query.
- [How CREATE INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](v12/questions/indexing/create-index-concurrently.md) - Reviewed comprehensive walkthrough of the v12 CIC parser/utility/generated-catalog path and `DefineIndex`: the four internal transactions, two heap scans, and three deliberate transaction-set waits; the `indislive`/`indisready`/`indisvalid` state-flag progression set non-transactionally by `index_set_state_flags`; a `maintenance_work_mem` follow-up covering the AM-dependent first-build and common validation-TID-sort budgets, B-tree planned-versus-launched participant shares and required tape runs, conditional GIN cleanup, the BRIN empty-enumeration exception, GiST's buffered temporary-file path, scoped sort/temp observability, and the workload-specific plateau; a broader performance-GUC matrix covering B-tree worker request and pool limits, AM-specific settings, v12 heap-scan and storage boundaries, WAL/checkpoint/commit costs, waits/timeouts, exact apply scopes, absent later-version controls, and commonly confused non-controls; the concurrent-specific preconditions/early exits (transaction-block ban, temp fallback ordering, `IF NOT EXISTS`, partitioned/system-catalog/exclusion bans); failure paths that can leave no index, an invalid index, or a valid index after a late command-end error; and a dedicated section mapping every step to the table locks (transaction- and session-level `ShareUpdateExclusiveLock`, the `WaitForLockers(ShareLock)` writer waits, and `WaitForOlderSnapshots`), with the `LockConflicts` rationale for why DML proceeds while another CIC/`VACUUM`/`ANALYZE`/DDL is blocked, plus regression and `multiple-cic` isolation test coverage, now with a resolved inter-builder trace showing same-table CIC serialization by `ShareUpdateExclusiveLock`, cross-table CIC/RIC old-snapshot waits, and the no-`xmin` final wait boundary, and a line-by-line first-build-scan tuple-visibility trace into the heap AM (`ii_Concurrent` selects a fresh MVCC snapshot over `SnapshotAny`, so the first scan omits `RECENTLY_DEAD` tuples and never sets `ii_BrokenHotChain`), and a resolved assessment that skipping prepared transactions in the two `WaitForLockers(ShareLock)` writer waits is not sufficient for index correctness (a prepared write that commits after the build is left unindexed, because the MVCC scans skip in-progress tuples, all three waits skip the prepared xact's invalid-VXID dummy proc, and `COMMIT PREPARED` does no index maintenance). Distinguishes the four deliberate core transaction barriers from additional waits in DDL event triggers, index predicates/expressions, AM callbacks, B-tree unique validation, and parallel-worker coordination; at the four core barriers it catalogs conflicting-lock holders at the initial `ShareUpdateExclusiveLock` acquisition (mapped to v12 commands via the `mvcc.sgml` lock table, with the autovacuum SIGINT-cancel/anti-wraparound exception and prepared-transaction lock retention), open writer transactions at the two `WaitForLockers(ShareLock)` waits (idle-in-transaction included; lock waiters, later writers, and prepared transactions skipped), and same-database old-snapshot holders at `WaitForOlderSnapshots` (long-running queries, REPEATABLE READ/SERIALIZABLE transactions, cursor-holding idle sessions, manual `ANALYZE`, other CICs; autovacuum/lazy-VACUUM, other databases, xmin-0 backends, and prepared transactions excluded), with the `pg_stat_progress_create_index` wait phases, and worked examples for a running `pg_dump` (blocks only the snapshot wait, for the dump's whole duration, timeouts disabled) and an hour-long open / idle-in-transaction session (blocks per what it did: target-table writes hit the writer waits, pinned snapshots hit the snapshot wait, reads-only `READ COMMITTED` sessions clear their xmin via `SnapshotResetXmin` and block nothing). Adds a comprehensive failure-scenarios section: the last persistent state transition fixes the outcome — no index before commit 1, invalid/not-ready before `SET_READY`, invalid/ready before `SET_VALID`, or a valid index despite a reported command error when `ddl_command_end` fails after `SET_VALID` — with the planner/executor/HOT/uniqueness cost of each leftover, the fact that ordinary ERROR/cancel failures release (do not leak) the session lock, the resolved crash/immediate-shutdown recovery outcome (below), regression evidence (a failed unique build left `INVALID` and retained through `VACUUM FULL`), and `DROP INDEX` / `REINDEX INDEX CONCURRENTLY` recovery, and a resolved walsender/replication-slot Wait 3 assessment (a `hot_standby_feedback` standby and any replication slot hold the dead-row removal horizon through `GetOldestXmin`/`RecentGlobalXmin`, not a per-backend xmin, so they never enter `WaitForOlderSnapshots`; a physical walsender is additionally excluded by database and by the valid-VXID gate, and only a logical walsender mid-`REPEATABLE READ` snapshot export is eligible — as an ordinary in-database transaction), and a resolved crash / immediate-shutdown analysis (the `index_set_state_flags` flips are xidless `heap_inplace_update` writes that `RecordTransactionCommit` commits asynchronously and `heap_xlog_inplace` redoes physically, so replay lands on one of the four flag outcomes and cannot lose completed build data while retaining durable SET_VALID for the reviewed in-tree AMs — B-tree via outside-buffer `smgrimmedsync`, hash/GiST/SP-GiST/GIN/BRIN via shared-buffer WAL, and contrib Bloom via `GenericXLog`; the validate scan obeys WAL-before-data, unlogged heap/index forks reset together, the separate prepared-transaction gap is not repaired by recovery, and third-party AM durability remains outside the core-source proof); the `REINDEX INDEX CONCURRENTLY` mechanics now have their own page (below).
- [How REINDEX INDEX CONCURRENTLY Is Implemented in PostgreSQL 12 (unverified)](v12/questions/indexing/reindex-index-concurrently.md) - Comprehensive walkthrough of the v12 `REINDEX INDEX CONCURRENTLY` implementation in `ReindexRelationConcurrently`: the six phases (create the `_ccnew` copy via `index_concurrently_create_copy`, build, validate, swap, set-dead, drop) and five wait points — the same three CIC waits (two `WaitForLockers(ShareLock)` waits over the `ShareLock` conflict set and `WaitForOlderSnapshots`) plus two extra `WaitForLockers(AccessExclusiveLock)` reader-capable waits before marking the old index dead and before dropping it; the shared build/validate core; the atomic `index_concurrently_swap` (new marked valid + old invalid, `_ccnew`→original and original→`_ccold`, with constraints/triggers/comment/dependencies and per-index cumulative stats moved); `ShareUpdateExclusiveLock` held at transaction and session level on the old index, new index, and heap; dispatch/restrictions (no transaction block, recovery-forbidden, temp fallback, direct system-catalog error vs schema/database skip, partitioned-table skip vs partitioned-index error, exclusion error-vs-skip, invalid-index skip-vs-allow, toast rebuilt); two-index state flags; per-phase failure outcomes with planner-vs-executor invalid-index overhead, regression recovery evidence, and the dedicated `reindex-concurrently` isolation spec; a dedicated section confirming that a failure never leaves the original `index_name` invalid for a healthy index (the phase-4 swap atomically renames and re-validates in one transaction, so the invalid leftover is always the differently-named `_ccnew` or `_ccold`, and you never go from a bloated-but-valid index to no usable index), the sole exception being an index already invalid before RIC ran; a source-backed GUC-performance follow-up covering AM-dependent build/validation memory, B-tree-only worker controls, v12 bulk-read/synchronized-scan boundaries, temporary and fixed output storage, B-tree immediate sync plus WAL/checkpoint/commit costs, all five timeout-sensitive waits, exact apply scopes, absent later-version controls, and commonly confused non-controls; a dedicated section on running RIC against an already-invalid index (rebuilt/repaired when named directly, skipped with a warning when reached via `REINDEX TABLE`/`SCHEMA`/`DATABASE`); multi-index batching; and the source-vs-view discrepancy where phase 6 reports `WAIT_4`, so `waiting for readers before dropping` is never emitted.
- [Can ALTER TABLE ... ATTACH PARTITION Drop Indexes in PostgreSQL 12? (unverified)](v12/questions/indexing/attach-partition-index-drops.md) - Answers that `ALTER TABLE ... ATTACH PARTITION` never drops an index from the table being attached: `AttachPartitionEnsureIndexes` is strictly match-or-create — for each partitioned parent index it re-parents a compatible existing partition index via `IndexSetParentIndex` (kept, not rebuilt) or creates a cloned one via `DefineIndex`, while incompatible/extra indexes on the partition are left in place (regression-proven by `indexing.out`: four incompatible indexes retained plus one added). Confirms there is no `performDeletion`/`RemoveInheritance` in the attach path (those live in DETACH/DROP CONSTRAINT/trigger/sequence paths) and that the pre-index `CreateInheritance` step only merges CHECK constraints (`MergeConstraintsIntoExisting`). Notes that v12's matching is looser than later minors (no `indisvalid` skip, constraint check is existence-only, not type) but still only re-parents or creates — never drops. Distinguishes the three look-alikes (an absorbed/re-parented index losing independent droppability, the documented manual drop of the now-redundant CHECK constraint, and a later cascading parent `DROP INDEX`), shows `ALTER INDEX ... ATTACH PARTITION` (`ATExecAttachPartitionIdx`) likewise only re-parents or errors (`refuseDupeIndexAttach`, "index definitions do not match"), and gives the docs' pre-build workflow (plain `CREATE INDEX`, since v12 bans partitioned CIC) to avoid a costly build.
- [All Outcomes That Leave an Invalid Index in PostgreSQL 12, Including a Failed CREATE INDEX CONCURRENTLY (unverified)](v12/questions/indexing/invalid-index-outcomes.md) - Enumerates the complete, closed set of operations that persist `pg_index.indisvalid = false` in v12, derived from the full list of `indisvalid=false` writers in the pinned checkout: a failed/cancelled/crashed `CREATE INDEX CONCURRENTLY` (invalid not-ready vs invalid ready), a failed `REINDEX CONCURRENTLY` (invalid `_ccnew` before the swap or `_ccold` after; a healthy original name is never left invalid), a failed/interrupted `DROP INDEX CONCURRENTLY` (marked invalid then dead across commits; the clear-valid step is retryable by design), an incomplete partitioned parent index (`CREATE INDEX ON ONLY` with partitions sets `INDEX_CREATE_INVALID`, or attaching an invalid child marks the parent invalid; `validatePartitionedIndex` flips it valid once every partition has a valid match), and `pg_upgrade` from <= 9.6 which runs `UPDATE pg_index SET indisvalid=false` for hash indexes in the new cluster and writes `reindex_hash.sql`. Includes the persistent `(indislive, indisready, indisvalid)` state table (and why `(f,t)` is impossible), the contrast that single-transaction `CREATE INDEX`/`REINDEX`/`DROP INDEX` roll back cleanly with no leftover, what an invalid index costs (planner skips it, executor still maintains it when `indisready`, VACUUM still processes it) and which commands reject it (`CLUSTER`, `REFRESH MATERIALIZED VIEW CONCURRENTLY`, `ADD CONSTRAINT ... USING INDEX`, `REPLICA IDENTITY`, bulk `REINDEX CONCURRENTLY`), and how to clear each outcome; cross-links the dedicated CIC and RIC pages.

#### Storage and Vacuum

- [How Much I/O a VACUUM FULL Performs on a Multi-GB, Near-Empty Heap in PostgreSQL 12 (unverified)](v12/questions/storage-and-vacuum/vacuum-full-io-on-near-empty-heap.md) - I/O accounting for `VACUUM FULL` on a bloated-but-empty heap, **now measured on a 12.2 server built from the pin**. A 200,000,000-row heap of 884,956 blocks (7,249,559,552 bytes, 226 tuples/page) had 99.9% of its rows deleted with the physically last rows kept so `VACUUM` could not truncate; `VACUUM` and `VACUUM FREEZE` then left all 884,956 pages all-visible and all-frozen at unchanged size. `VACUUM FULL` read **every one of the 884,956 blocks** with 0 buffer hits and the block layer delivered **7,249,559,552 bytes**, the file size to the byte, in 2.15 s — while a plain `VACUUM` on the identical heap read **0 blocks** in 17 ms. The extreme case, the same 6.75 GB heap with `vacuum_truncate = off` and **zero rows**, still read all 884,956 blocks to produce a **0-byte** new heap. Four runs agree byte-for-byte on `heap_blks_read`, `rchar` (7,250,034,354), `syscr` (885,019) and WAL (84,720); a warm page cache changed only `read_bytes` (0) and the clock. Also measured: **885 `XLOG/FPI` records for 885 new pages** under `pg_waldump` (7,377,944 bytes at `replica` against 84,720 at `minimal`), `heap_blks_scanned` reporting 0 of 884,956 for 49 of 51 samples and for all 94 samples on the rowless heap, an index rebuild that reads only the new 885-block heap with `idx_blks_read` 0, `VACUUM FULL` at 2.05 s against a cost-delayed `VACUUM (DISABLE_PAGE_SKIPPING)` at 57.27 s over identical bytes, the old relfilenode left at 0 bytes with its higher segments already unlinked and removed at `CHECKPOINT`, and 226 of 226 line pointers retained per emptied page (`pd_lower` 928, `pgstattuple` free 88.53% not ~100%). The source analysis behind it: reads scale with the physical file and nothing else: `vacuum_rel` hands `FULL` to `cluster_rel` with `InvalidOid`, so `use_sort` is false and the copy callback opens a plain `SnapshotAny` `table_beginscan` that reads every block once through `ReadBufferExtended`, with zero `visibilitymap` and zero `PrefetchBuffer` references in `cluster.c`, `heapam_handler.c` and `rewriteheap.c`. Writes, WAL (`XLogIsNeeded()` gated, one `log_newpage` FPI per new page), the closing `heap_sync`, and the index rebuilds all scale with the surviving rows instead, and reclaiming the old multi-GB relfilenode is filesystem metadata work — dirty old-heap buffers are dropped unwritten, the first segment is truncated with a deferred unlink, later 1 GB segments are unlinked at once. Adds the `raw_heap_insert` packing rule with a `floor(8168 / (MAXALIGN(t_len) + 4))` capacity derivation, a 4 GB worked example (184 kB to 2.5 MB of writes, roughly 1,700:1 to 22,800:1), the 32-buffer `BAS_BULKREAD` ring and its `StrategyRejectBuffer` escape hatch, hint bits as the only source of old-heap writes, why no `vacuum_delay_point` throttles the rewrite, and the doc-versus-source conflict over "extra disk space approximately equal to the size of the table". The review corrected three substantive claims: `heap_blks_scanned` is written once per *returned tuple* as `rs_cblock + 1` and never squared up at end of scan, so it under-reports exactly this shape and `pg_statio_all_tables.heap_blks_read` is the honest counter; vacuumed-empty pages keep their line pointer arrays, since `PageRepairFragmentation` "doesn't remove unused line pointers", so `PageGetMaxOffsetNumber` is not 0 and `heapgettup` walks the array rejecting entries on `ItemIdIsNormal`; and the two published monitoring statements needed a `BEGIN`/`COMMIT` wrapper, because a top-level `SET LOCAL` only raises `WarnNoTransactionBlock` and leaves the query untimed.
- [How Reliable the Catalog-Statistics Table and TOAST Bloat Query Is in PostgreSQL 12 (unverified)](v12/questions/storage-and-vacuum/catalog-statistics-bloat-query-reliability.md) - Reviews and scores the widely circulated `pg_stats`/`pg_class` bloat-estimate statement — the `constants`/`no_stats`/`null_headers`/`data_headers`/`table_estimates`/`estimates_with_toast` CTE chain — on an exact-pin 12.2 server, against exact `pgstattuple` and against a real `VACUUM FULL` used as arbiter. **Verdict: a usable screen and a bad measurement.** Over 21 scored fixtures its `pct_bloat` lands within 1 point of the rewrite on 13 and averages **9.47 points** of absolute error, which is slightly *better* than `pgstattuple`'s own `dead + free` reading against the same rewrites (**10.79**, including one −86.41 miss). As a triage screen against "a rewrite returns ≥ 10 MB and ≥ 25%" it scored **9 true positives, 2 false positives, 5 false negatives, 9 true negatives**. Every large error is systematic and named: it reads **0 where a rewrite returns 50.00%** on a table deleted but not re-analyzed (`relpages`/`reltuples` are planner estimates, not DML-maintained); **−25.05** because `nullhdr2 = MAX(null_frac) * MAXALIGN(24 + n/8)` charges a whole second aligned header (32 bytes) where the engine charges 8, measured as 68.02 modelled bytes/tuple against a real `t_len` of 40; **−11.98** because `ceil(toast.reltuples / 4)` is exact for 6,400-byte payloads (predicted 8,192,000 bytes, rewrite produced 8,192,000) but over-predicts by 1.5x when a 2,100-byte payload puts 6 chunks on a page; **−9.00** on an inheritance parent, where `analyze_rel`'s second inherited pass gives 4 `pg_stats` rows for 2 attributes and doubles `datawidth` (24.00 against 12.00 on a byte-identical child); **+21.00** from per-column alignment padding `sum(avg_width)` cannot see (60.00 modelled against 72.00 real); **+33.00** from dividing by `bs - 20` with no per-page flooring (2.982 implied tuples per page where 2 fit); and **+31.00** from having no `fillfactor` term at all. Documents five relations the report never gives a number for — materialized views (absent from `information_schema.columns`), partitioned parents (absent from `pg_stat_user_tables`), any table with one `STATISTICS 0` column, any never-analyzed table, and empty tables — and proves that **no `can_estimate = false` row can ever pass the published `WHERE` clause**, so a 31.88 MB table with 74.99% reclaimable is assembled through six CTEs and then discarded. Also: the one shape where the query beats the exact function (a dropped 500-byte column reads **92** against a **92.42%** rewrite while `pgstattuple` reads **6.01**, because a rewrite nulls dropped attributes); the never-vacuumed TOAST relation whose `relpages = 0` makes a 31 MB table report `table_mb = 0.203`, with the source path showing `ANALYZE` never touches TOAST counters and autovacuum only vacuums TOAST on dead tuples or wraparound; three incompatible size bases in one report (`relpages * bs` excludes FSM/VM and the TOAST index, while the null branch's `pg_table_size` includes them, a 442,368-byte gap); the `bloat_data` duplicate-column `ambiguous` error; and the operational surface — one `AccessExclusiveLock` on any `pg_table_size`-branch table kills the whole statement at `lock_timeout` while a lock on an estimated-branch table does not, a role with `SELECT` on 1 of 26 tables gets a clean-looking one-row report, and run time goes 36 ms at 27 tables to 224 ms at 427, dominated by `information_schema.columns` rather than by `pg_table_size` (analyzing everything made it *slower*).
- [Measuring Heap and TOAST Bloat With pgstattuple_approx in PostgreSQL 12 (unverified)](v12/questions/storage-and-vacuum/pgstattuple-approx-heap-and-toast-bloat.md) - A single runnable statement, configurable by schema and table name, that reports the main heap and its TOAST heap as separate rows with physical size, live and dead tuples, free space, scanned percentage, the effective `fillfactor` defaulting to 100, the `100 - fillfactor` reserve in bytes, and estimated bloat as dead space plus free space above that reserve — all in human-readable units. **The TOAST row cannot come from `pgstattuple_approx`**: it accepts only `RELKIND_RELATION` and `RELKIND_MATVIEW`, so a TOAST relation returns `ERROR: "pg_toast_16405" is not a table or materialized view`, and the statement measures that row with the exact `pgstattuple`, which does accept `RELKIND_TOASTVALUE`. The TOAST row's fillfactor is hard-coded to 100 on source grounds, not by convention: `heap_reloptions()` overwrites the parsed value with 100 for every `RELKIND_TOASTVALUE` relation, `toast.fillfactor` is refused as an unrecognized parameter, and `toast_save_datum()` inserts chunks through plain `heap_insert()`, so `saveFreeSpace` is zero. Safe by construction on the two edge cases the question names: the `reltoastrelid <> 0` filter sits one query level below the lateral call, because `pgstattuple(0)` raises `could not open relation with OID 0` and would abort the statement, and `nullif`/`coalesce` make a zero-length relation report `0.00` rather than NULL or a division error. Tested against `VACUUM FULL` on an isolated 12.2 server over ten fixtures: heap rows with real bloat read 1.8 to 4.0 points low (page headers and retained line pointers), the requested reserve-subtraction formula is algebraically identical to the rewrite-matching ratio form at `fillfactor = 100` and agreed to the digit on 12 of 12 such rows, but **understates by 15.54 points** on a bloated `fillfactor = 70` table (34.46 against a 50.00 truth, where the ratio form reads 49.22) because it charges the reserve against the pre-rewrite size. On TOAST relations the estimate runs 0.2 to 9.5 points high for a reason specific to TOAST: **a never-deleted TOAST relation reported 19.58% free and `VACUUM FULL` reclaimed 0.00%**, reconciled to the byte from `TOAST_MAX_CHUNK_SIZE` = 1,996 (measured chunk lengths 1,996/1,996/1,996/412, 6,544 occupied bytes and 1,604 free per page), while a payload that is an exact multiple of the chunk size read 0.24% — so a TOAST threshold must be baselined against value geometry, whereas the exact `dead_size` has no such confound. Also measured: the cost split (45 buffer accesses and 0 heap reads for the all-visible heap branch against 10,006 for a 5,000-page TOAST scan, exactly two per page because the free-space loop re-reads each block; 51x time and 690x physical reads between the two functions on the same 27 MB heap), `scanned_percent` truncating to 0 with 1 page of 5,000 scanned, 10,000 dead TOAST chunks appearing before any VACUUM because `heap_delete()` calls `toast_delete()` in the same transaction, the `pg_stat_scan_tables` grant being the only privilege needed (the role still cannot `SELECT` from the table), and the VACUUM warning in one table: a trailing delete let plain `VACUUM` truncate a TOAST relation from 16,384,000 to 8,192,000 bytes while an interior delete of the same row count changed nothing until `VACUUM FULL`. Includes an interpretation guide for high dead space, reusable free space, intentional fillfactor headroom, TOAST bloat, tiny-relation last-page false positives, and zero-row results.

#### Replication and WAL

- [How wal_sender_timeout Is Used and What It Impacts in PostgreSQL 12 (unverified)](v12/questions/replication-and-wal/wal-sender-timeout.md) - Source-backed sender-silence timeout: the v12 per-connection `PGC_USERSET` change and live reload, process-local reply clock, valid `r`/`h`/`CopyDone` semantics, normal half-time request plus logical-WAL-wait and shutdown exceptions, cooperative server-stall accounting, and full-deadline disconnect. Covers physical standbys, cascading senders, `pg_receivewal`, the streaming side of `pg_basebackup`, logical apply and table-sync catch-up, `pg_recvlogical`, receiver-side settings, non-streaming exclusions, monitoring limits, and synchronous-commit blocking. The slot follow-up maps every `pg_replication_slots` field after timeout, persistent-versus-temporary outcomes for timeout, EOF/error, orderly `CopyDone`, and crash recovery, and `restart_lsn`-based WAL retention through checkpoint/restartpoint removal, unreserved slots, on-disk state, `wal_keep_segments`, and archive boundaries; includes build/module boundaries, direct-test gaps, and adjacent slot tests. Fully re-reviewed at the 12.2 pin on 2026-07-29: every citation re-resolved against the pinned files, the `pg_stat_replication` superuser/`pg_read_all_stats` detail gate and the `immediately_reserve` default added, and an exact-pin server run reproduced the persistent-slot reload timeout (client sees only a closed connection), the temporary-slot drop, per-connection precedence over a lower configuration-file value, and the statistics privilege split.

#### Observability

- [EXPLAIN ANALYZE BUFFERS Output in PostgreSQL 12 (unverified)](v12/questions/observability/explain-analyze-buffers-output.md) - Detailed field-by-field explanation of `EXPLAIN (ANALYZE, BUFFERS)` shared, local, temp, and I/O timing output in PostgreSQL 12.
- [How pg_stat_statements Works and Which Settings Affect It in PostgreSQL 12 (unverified)](v12/questions/observability/pg-stat-statements.md) - Explains the v12 extension's in-extension query jumbling, hooks, shared-hash and external query-text storage, readout, permissions, persistence, and its four GUCs (`max`, `track`, `track_utility`, `save`) plus `shared_preload_libraries` and `track_io_timing`.

#### Server Administration

- [PostgreSQL 12 Database Health Checklist (unverified)](v12/questions/server-administration/database-health-checklist.md) - Deep source-backed checklist for monitoring access and snapshot semantics; client capacity, real blockers and prepared transactions; database, maintenance, XID/MultiXact, checkpoint/background-writer, WAL/archive/replication/slot, checksum, query, and relation signals; expanded crash, WAL I/O, replication, autovacuum, authentication, timeout, and corruption log families; exact restart/reload/session scopes; and test/build boundaries. Fully re-reviewed at the 12.2 pin on 2026-07-29: all 345 citations re-resolved, the `ForwardSyncRequest` symbol name, the `analyze_rel`/`relation_needs_vacanalyze` ranges, the swapped `auth.c` labels, the `hot_standby_feedback` and progress-vacuum doc targets fixed, per-GUC apply-scope evidence completed for the replication row, and `varsup.c`, `pgstat_recv_recoveryconflict`, and the checkpoint-message sites added. An exact-pin primary plus streaming standby reproduced the `pg_monitor` progress-field gate, `confl_snapshot` with its recovery-conflict cancellation, the PID-zero prepared-transaction blocker, lock-wait and deadlock logging, temp-file accounting, and the `pg_settings`/`pg_stat_statements` redaction boundaries.
- [psql Environment Variables and Timeout Settings in PostgreSQL 12 (unverified)](v12/questions/server-administration/psql-environment-variables-and-timeouts.md) - Lists psql-specific frontend variables, libpq connection/session environment variables inherited by `psql`, and the session impact of `statement_timeout` and `lock_timeout` via `SET`, `SET LOCAL`, and `PGOPTIONS`.
- [Extracting Declarative Range Partition Bounds With SQL and No Regex in PostgreSQL 12 (unverified)](v12/questions/server-administration/extract-range-partition-bounds-without-regex.md) - v12 exposes no typed partition bound, so extraction must decompose the `pg_get_expr(relpartbound, oid)` text that `psql \d` and `pg_dump` also consume — and the deparser's fixed grammar (`FOR VALUES FROM (` … `) TO (`, `, ` separators, bare `MINVALUE`/`MAXVALUE`, never a `::type` or `COLLATE`) makes that possible with `string_to_array`/`strpos`/`substr` alone. Gives a quote-masking recipe that splits the text on the quote character, masks literal contents to equal-length runs, then locates separators in the mask; it scored 95 of 95 control elements on 45 fixture partitions where the obvious `split_part` version got 9 of 41 partitions wrong (values containing `) TO (`, `, `, or an escaped quote). Documents the per-type literal rules from `get_const_expr` (negative `int4` and integral `numeric` quoted, `boolean` as an SQL keyword), the six `PGC_USERSET` settings that change the text with measured before/after (`DateStyle`, `TimeZone`, `IntervalStyle`, `extra_float_digits`, `bytea_output`, `standard_conforming_strings`), the backslash-doubling failure under `standard_conforming_strings = off` plus an `E''`-string fix that scores 95 of 95 either way, and boundaries: no locks on partitions (14 catalog `AccessShareLock`s at 1048 partitions, 39.9-44.9 ms), any unprivileged role can read every bound, DETACH erases it, `pg_get_expr` returns NULL for a concurrently dropped partition while `pg_get_expr(…, 0)` still deparses, and a 9600-character bound fails at DDL time because `pg_class` has no TOAST table.

## Maintenance Tooling

- `scripts/recent_log` - recent wiki activity.
- `scripts/wiki_lint` - wiki health checks.

## Maintenance Notes

- Update this page whenever a wiki page is created or substantially changed.
- Keep version-specific entries tagged with their PostgreSQL major version.
- Prefer links to version landing pages, such as `vNN/index`, once versions exist.
