# PostgreSQL 14.23

## Source Pin

- Branch: `REL_14_STABLE`
- Commit: `5c00f4e2e3bcee6931ae93429d53f7c2a4f46156`
- Status: `active`
- Source path: `raw/postgres-14/`
- Added: 2026-06-30

## Coverage

Behavioral claims cite the matching pinned checkout under `raw/postgres-14/`.

- [PostgreSQL 14 Codebase Navigation Guide (unverified)](codebase-navigation-guide.md) - Mandatory root-level question-style map for navigating the pinned v14 source tree: layout, SQL statement flow, utility dispatch, generated/catalog artifacts, key structs, contrib boundaries, tests, and docs.
- [Row-Level Security (RLS) in PostgreSQL 14: Implementation, Scalability and Performance, and Settings (unverified)](questions/row-level-security-rls.md) - Explains RLS as a rewrite-time feature (`pg_policy` storage, relcache `rd_rsdesc`, `check_enable_rls` bypass logic, `get_row_security_policies` producing security-barrier `USING` quals and `WITH CHECK` options, permissive-OR/restrictive-AND combination and default-deny, command/role matching, the planner `security_level`/leakproof qual ordering, executor `ExecWithCheckOptions`, plan-cache `dependsOnRLS` re-planning, partition/inheritance handling, and COPY/CTAS/RI/error-leak boundaries); the source-evident scalability and performance issues (per-row evaluation, leakproof ordering blocking index use, policy subqueries, qual-tree growth with many policies, plan-cache churn on role/`row_security` change, aggregate statements consuming RLS-filtered subplans with mitigations around indexable policies, partition pruning/partitionwise aggregation, stable plan-cache environments, and trusted summary/reporting paths, FK bulk-check downgrade and related per-row `FOR KEY SHARE`/MultiXact risk, partition fan-out); a query-vs-policy planning subsection (policy `USING` quals are prepended to `securityQuals` and transferred into `baserestrictinfo` at the lowest security level, so the planner already uses them for index scans and partition pruning without restating them in the query, while query-text copies land at the maximum security level and help only if leakproof); a subsection on wrapping a function in a scalar sub-SELECT (`(SELECT auth.uid())` instead of `auth.uid()`) so an uncorrelated call becomes an InitPlan evaluated once per query and cached in a `PARAM_EXEC` parameter rather than re-invoked per row, with the caveats that only uncorrelated sub-SELECTs qualify, index-qualifiable comparisons already evaluate a `STABLE` function once per scan, and it is a once-per-execution cache; a dedicated RLS-and-the-plan-cache section (RLS-aware plan caching keyed on role + `row_security`, the role-independent relcache `rd_rsdesc` policy cache, the scenarios where caching does not help, `plan_cache_mode` forced generic/custom planning, the SQL `DECLARE CURSOR` non-cache path, and the no-cache re-analyze/rewrite/re-plan cost); and all related settings (the `row_security` GUC and pg_dump default, the `plan_cache_mode` GUC, the `ALTER TABLE` table flags, the `BYPASSRLS` role attribute, the `CREATE`/`ALTER POLICY` options, and the two extension hooks).
- [Performance Implications of Functions and Procedures in a WHERE Clause in PostgreSQL 14, and How to Minimize the Overhead (unverified)](questions/functions-procedures-in-where-clause.md) - Explains that procedures (`prokind='p'`) cannot appear in a WHERE clause at all (parser rejects them; only `CALL` invokes them) and that row-dependent or volatile functions in a WHERE clause are per-row costs (`ExecScan` qual loop -> `ExecQual` -> `EEOP_FUNCEXPR`), while no-`Var`, non-volatile quals can run once as pseudoconstant gating `Result` filters; covers volatility (`VOLATILE` per row where needed, `STABLE` once-per-scan only as an index-scan comparison value or no-`Var` one-time qual, `IMMUTABLE` constant-folded with constant args), the cost model (`procost * cpu_operator_cost`, with omitted `CREATE FUNCTION COST` defaulting to 1 for C/internal functions and 100 otherwise) and fixed selectivity guesses for function quals (0.3333333 for a bare boolean function, 0.5 for other boolean expressions), why a function on a column defeats a plain index and needs an immutable expression index or stored generated column, leakproof/security-barrier qual ordering, and parallel-safety effects; and the mitigations (correct volatility labeling, expression indexes, scalar sub-SELECTs for intended once-per-execution caching, SQL-function inlining, accurate `COST`/`ROWS`, planner `SUPPORT` functions, `LEAKPROOF`/`PARALLEL SAFE` labeling, and filtering rows first).
- [How MultiXact Works in PostgreSQL 14, and How Foreign Keys and Other Operations Degrade Performance When the Local Cache Spills to Secondary Storage (unverified)](questions/multixact-foreign-keys-cache-spill.md) - Explains MultiXact as the shared-row-lock mechanism (a `MultiXactId` in `xmax` standing for a set of `(xid, MultiXactStatus)` members, the offsets/members SLRUs backed by `pg_multixact/`, the `MultiXactStateData`/per-backend oldest arrays, the create path `MultiXactIdCreate`/`Expand`/`CreateFromMembers` -> `GetNewMultiXactId`/`RecordNewMultiXact`, and the read path `GetMultiXactIdMembers`); the backend-local 256-entry per-transaction LRU cache (`mXactCacheEnt`/`MAX_CACHE_ENTRIES`, `MXactContext`, `mXactCachePut` tail eviction) and how a lookup miss falls back through the tiny 8-offset/16-member SLRU pool to the on-disk `pg_multixact/` files (`SlruPhysicalReadPage` -> `SLRURead`); how foreign keys create MultiXacts (RI `FOR KEY SHARE` -> `heap_lock_tuple` -> `compute_new_xmax_infomask`) plus other operations (compatible explicit row locks, UPDATE/DELETE of locked rows, VACUUM freezing); the three degradation paths (cache spill to secondary storage, SLRU control-lock/per-buffer/I/O contention on MultiXact SLRUs, and member-space growth driving aggressive vacuum and `multixact "members" limit exceeded`); observability via `pg_stat_slru`/wait events/`mxid_age`; and the vacuum GUCs and per-table storage parameters with apply scopes.
