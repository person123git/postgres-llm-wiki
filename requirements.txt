# Python dependencies for the PostgreSQL engine wiki tooling.
#
# Scripts under scripts/ run inside .wiki-runtime/venv/, created with
# `python3 -m venv .wiki-runtime/venv`. Add new dependencies here, with a
# pinned version, before importing them from any script.

# No Python package dependencies are currently pinned.
